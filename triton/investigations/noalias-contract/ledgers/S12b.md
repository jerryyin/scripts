STATUS: done
CONCLUSION: On the now-compiling F4 gluon PRIMARY (`_fused_qk_rope_cat_and_cache_mla_kernel`), noalias buys NOTHING: the primary index `gl.load(slot_mapping_ptr + pid_b)` is a UNIFORM SCALAR `<1 x i64>` load that is ALREADY selected as `s_load_b64` in the baseline (with `!amdgpu.noclobber` already set, readonly inferred, single entry use, no clobbering store, no loop) — adding `noalias_args=["slot_mapping_ptr"]` leaves the code section (asm lines 1-907) BYTE-IDENTICAL, changing only ONE arg-descriptor metadata block (`+ .actual_access: read_only`, 3-line diff, zero instructions), FFM-correct in both; this confirms S12's Run-1 prediction (the uniform-scalar leg of the dichotomy) directly on the gluon primary. The sibling reshape kernel's `gl.load(slot_mapping_ptr + t_offs, mask=t_mask)` is the divergent-VECTOR leg (S5 ceiling → VMEM, noalias-inert) — not re-measured here but source-confirmed. **NO-SHIP noalias for F4 gluon primary.**

# S12b — F4 fused_kv_cache gluon primary: re-measure noalias now that R3 made it compile

**Cell:** S12b (Run 2, Phase B, Family F4). **Blocked-by:** R3, S4, S5 (all done; read from disk).
**Question:** Re-measure the F4 gluon PRIMARY `fused_kv_cache.py`
(`_fused_qk_rope_cat_and_cache_mla_kernel`) now that R3 cleared the compile break: noalias
mechanism + asm delta + FFM-correct. S12 (Run-1) predicted slot_mapping is EITHER a uniform scalar
already-`s_load` at baseline (⇒ noalias inert) OR a divergent vector (⇒ never `s_load`). Confirm
WHICH on the now-compiling kernel: toggle noalias, dump asm histogram + in-loop rfl, FFM-correct.

---

## Frozen experiment

- **aiter tree/SHA:** `/root/aiter` @ `93d8ffb8e2a101f7451653b8e3b9e12b61334e46`. R3's 3 compile-fix
  edits present (async_gather 3-arg in `_issue_tdm_gather_2d` at line 110; MLA-cat launcher
  `cos.shape[0]` MAX_EMBD_POS at `fusions/fused_kv_cache.py:277`; pa_decode edits). MY noalias
  toggle was added then reverted — tree left in the R3 state (only R3's intended files modified,
  verified `git diff`). No commits.
- **Triton tree/SHA:** `/root/triton` @ `ba4fd67b8ed28d37935f95ddbe8717853de7212d`
  (branch `users/jerryyin/moe-gather-sload-contract`; #120 contract landed). NOT rebuilt — the
  contract is a Python `@gluon.jit(noalias_args=[...])` annotation toggle (no C++ change), as S4/S5.
- **Target / vehicle:** gfx1250 (the mechanism/asm target per PLAN). asm/IR obtained from the
  FFM-compiled gfx1250 output (compile is arch-deterministic; FFM supplies the launch device for the
  correctness check only, no timing). FFM-lite env `/zyin/rocdtif-7.13-am+ffmlite-mi400-r6.06/ffmlite_env.sh`
  presents `arch_info.get_arch() == gfx1250`, so the launcher selects the gluon MLA-cat kernel
  (`fused_kv_cache.py:255-256`: `if DEVICE_ARCH == "gfx1250": _kernel = gluon_...`).
- **Isolation:** `TRITON_CACHE_DIR=/tmp/tc-S12b-{base,na}` `TRITON_DUMP_DIR=/tmp/td-S12b-{base,na}`
  `TRITON_ALWAYS_COMPILE=1 TRITON_KERNEL_DUMP=1`; never shared `~/.triton/cache`. GPU section held
  `~/scripts/tools/gpu-lock` (`flock /data/lock/amd-gpu.lock`), `HIP_VISIBLE_DEVICES=0` (one of 8).
  pytest: `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTEST_PLUGINS=ffm_teardown PYTHONPATH=/root/scripts/tools`.
- **Kernel + index load located:** `aiter/.../gfx1250/fusions/fused_kv_cache.py`
  `_fused_qk_rope_cat_and_cache_mla_kernel`, the primary index load line 483:
  `pid_slot = gl.load(slot_mapping_ptr + pid_b).to(gl.int64)` where `pid_b = pid % B` (program-id
  derived ⇒ uniform per program). `slot_mapping_ptr` is the 13th pointer arg (0-indexed 12).
- **A/not-A kill switch:** BASE = as-shipped (R3-fixed, no noalias). NA = the exact string
  `noalias_args=["slot_mapping_ptr"]` added to the `@gluon.jit` decorator of the MLA-cat kernel only.
  Same installed triton; only difference = one `noalias` token (verified by IR diff below).
- **Reduced size (smallest that exercises the path):** pytest node
  `test_fused_qk_rope_cat_and_cache_mla[False-cache_dtype0-True-64-False-1-16384-512-64-1-16-1]`
  = bf16 cache, T=1, KH=1, QH_per_KH=16, D_pe=64, D_lora=512 (the R3 smallest-exercising shape).
- **Metric:** IR arg attrs (`noalias`/`readonly`); the slot_mapping load's `!amdgpu.noclobber`;
  ISel of the index load (`s_load` vs VMEM); full amdgcn load histogram; in-loop
  `v_readfirstlane`; whole-asm md5 + diff; FFM PASS.
- **Sanity:** both BASE and NA FFM `1 passed, 7 warnings` (test's own `check_kv_buffer`); contract
  is pure codegen ⇒ FFM numerics identical by construction. Tree restored, `git diff` = R3 only.

---

## HOW-facts (real IR/asm, not paraphrased)

### FACT 0 — the gluon primary NOW COMPILES + is FFM-correct (R3 cleared the S12 blocker)
S12's Fact 0 blocked here (launcher off-by-one + async_gather arity). Both are fixed by R3. This
session: BASE FFM `======================== 1 passed, 7 warnings in 6.25s ========================`
and NA FFM `1 passed, 7 warnings in 6.11s`. Kernel dumps
(`_fused_qk_rope_cat_and_cache_mla_kernel.{amdgcn,llir,ttgir,hsaco}`) emitted in both.

### FACT 1 — the primary index (`slot_mapping`) is a UNIFORM SCALAR `<1 x i64>` load
BASE post-opt LLVM `.llir`, arg `%12` = `slot_mapping_ptr` (13th ptr arg), and its load:
```
13: ptr addrspace(1) nofree readonly captures(none) %12      ; readonly INFERRED without noalias
%46 = load <1 x i64>, ptr addrspace(1) %12, align 8, !dbg !21, !amdgpu.noclobber !20
```
It is `<1 x i64>` at offset 0 (`slot_mapping_ptr + pid_b`, `pid_b` uniform), used ONCE at entry to
form the kv_cache STORE address — no loop reload, no store precedes it. Crucially it ALREADY carries
`!amdgpu.noclobber` in the BASELINE (S4's scalarization gate leg), because `readonly` is inferred by
FunctionAttrs and no store can clobber a single entry load. This is the UNIFORM-SCALAR leg of the
S12 dichotomy — NOT the divergent-vector leg.

### FACT 2 — BASELINE already selects `s_load_b64`; whole kernel has ZERO rfl and NO VMEM
BASE amdgcn load histogram (whole kernel):
```
 1 s_load_b256 | 6 s_load_b32 | 1 s_load_b512 | 4 s_load_b64 | 1 s_load_b96
```
No `global_load`/`buffer_load`/`flat_load` at all; `v_readfirstlane` count = 0; 3 `s_cbranch`
(the guard branches, not a hot re-lift loop). The slot_mapping `<1 x i64>` load selects
`s_load_b64 s[12:13], s[12:13], 0x0` (asm line 52) in the BASELINE — already scalar SMEM.

### FACT 3 — noalias changes ONE metadata block, ZERO instructions (A/not-A)
Only IR delta (whole-`define` diff), exactly one token on `%12`:
```
< ptr addrspace(1)         nofree readonly captures(none) %12
> ptr addrspace(1) noalias nofree readonly captures(none) %12
```
The slot_mapping load still `load <1 x i64> ... !amdgpu.noclobber` in NA (noclobber was already
there in BASE — noalias adds nothing to it). amdgcn load histogram BYTE-IDENTICAL BASE vs NA
(same `1 s_load_b256 | 6 s_load_b32 | 1 s_load_b512 | 4 s_load_b64 | 1 s_load_b96`); NA rfl = 0.
The ENTIRE amdgcn diff is 3 lines, all in the kernel-arg descriptor metadata:
```
908c908,909
<       - .address_space:  global
---
>       - .actual_access:  read_only
>         .address_space:  global
```
The code section (asm lines 1-907) is BYTE-IDENTICAL (`diff` = empty). The slot_mapping load is
`s_load_b64 s[12:13], s[12:13], 0x0` in BOTH (asm line 52). md5 differs (f58c928.. vs dd4a7c8..)
ONLY because of the 3-line metadata block. FFM `1 passed` in BOTH.

### FACT 4 — the sibling reshape kernel is the DIVERGENT-VECTOR leg (source-confirmed)
`_fused_qk_rope_reshape_and_cache_kernel` (line 1100/1441):
`slot_mapping_load = gl.load(slot_mapping_ptr + t_offs, mask=t_mask, other=-1)` where `t_offs` is a
`gl.arange`-based per-lane vector ⇒ divergent VECTOR load ⇒ S5 ceiling (no sub-dword/vector SMEM
scalar form; divergent addr forces VMEM regardless of noalias) ⇒ noalias-inert, same as S12's
non-gluon `gather_kv_b_proj` read-gather (S12 Fact 1/2). Not separately toggled this cell (the
primary is the MLA-cat gluon kernel that S12 could not compile); the mechanism verdict is
over-determined by S5's truth table and by S12's already-measured non-gluon vector case.

---

## Applying the S3/S4/S5 canon (why the mechanism cannot pay off on the gluon primary)

- **S4 canon** (noalias ⇒ AA NoAlias ⇒ `!amdgpu.noclobber` ⇒ scalarize ⇒ `s_load`, killing per-iter
  re-lift) requires a global load that is (i) uniform, (ii) would OTHERWISE be treated as clobbered
  by an in-function store, AND (iii) reused across a loop. The MLA-cat `slot_mapping` load satisfies
  (i) but FAILS (ii) — it is already `!amdgpu.noclobber` in BASE (no store precedes a single entry
  load; readonly inferred) — and FAILS (iii) — used once, no loop. So noalias supplies a leg that is
  already satisfied. This is exactly why F4 differs from F1-prefill (F1's win killed a
  PER-K-ITERATION re-lift of a loop-invariant index across an in-loop store; F4's uniform index has
  neither the loop nor the interfering store).
- **S5 truth table:** the uniform-scalar `<1 x i64>` load meets {uniform, noclobber, align≥8≥4,
  global, scalarize-on, dword-granular} ⇒ `s_load_b64` — reached in BASE (noalias not the
  differentiator, S5 Fact E corollary: readonly alone makes a single-entry load noclobber). The
  reshape's divergent vector is disqualified twice (S5 Fact F divergent + Fact C sub-block vector).
- **S3 soundness (applied to any FUTURE annotation):** were noalias added, the two-part contract
  must hold: (1) readonly — `slot_mapping` is a read-only input (already inferred), and (2) noalias —
  it must not overlap a writable arg (`kv_cache_ptr` %11 is writeonly, distinct tensor ⇒ holds). But
  Fact 3 shows zero codegen benefit, so taking on the S3 silent-miscompile liability (stale-index
  CSE + gfx1250 K$ incoherence vs the kv_cache vector store) buys nothing. NET: do not annotate.

---

## Hypotheses + refutation trail

- **H1 "the gluon primary slot_mapping is a divergent vector (⇒ never s_load, noalias inert on that
  ground)."** REFUTED for the MLA-cat primary: it is `<1 x i64>` uniform, selects `s_load_b64` in
  BASE (Fact 1/2). (TRUE for the sibling reshape kernel — the OTHER leg, Fact 4.)
- **H2 "the uniform scalar slot_mapping NEEDS noalias to become s_load."** REFUTED. It is ALREADY
  `s_load_b64` in BASE with `!amdgpu.noclobber` already set (Fact 2); noalias changes only one
  metadata block, zero instructions (Fact 3). readonly-inferred + single-entry-use suffices.
- **H3 "the md5 difference proves a real codegen delta."** REFUTED. md5 differs only because of the
  3-line `.actual_access: read_only` arg-descriptor metadata; asm code lines 1-907 are byte-identical
  and the load histogram + rfl are identical (Fact 3).
- **H4 "R3's compile fix might have changed the slot_mapping load's nature vs the S12 non-gluon
  prediction."** REFUTED — the now-compiling gluon MLA-cat matches S12's non-gluon `cat_and_cache_mla`
  Fact 3/4 exactly (uniform scalar, already s_load, 3-line metadata-only noalias diff). Prediction
  confirmed on the real gluon primary, not inferred.

---

## CLAIM STATUS

**CLAIM (answered, irrefutable at IR/asm + FFM-correctness level):** On the now-compiling F4 gluon
primary `_fused_qk_rope_cat_and_cache_mla_kernel`, the primary index `slot_mapping` load is a UNIFORM
SCALAR `<1 x i64>` that is ALREADY selected as `s_load_b64` in the baseline (noclobber already set,
readonly inferred, single entry use, no loop). Adding `noalias` leaves the asm code byte-identical
(only a 3-line arg-descriptor metadata block changes; zero instructions; load histogram + rfl=0
unchanged), FFM-correct in both. This is the uniform-scalar leg of the S12 dichotomy, confirming
S12's prediction directly on the gluon primary. The sibling reshape kernel is the divergent-vector
leg (S5 ceiling, noalias-inert). **Ship recommendation: NO-SHIP noalias for F4 gluon primary** — no
codegen win to justify the S3 silent-miscompile liability.

**Counter-experiments that would refute (and their results):**
1. "Add noalias to `slot_mapping_ptr` and observe an instruction/load-selection delta." → asm code
   lines 1-907 byte-identical; only 3 lines of arg-descriptor metadata differ; load histogram
   identical; rfl 0→0. NOT REFUTED.
2. "Show slot_mapping is a divergent vector / VMEM load in the gluon primary that noalias could fix."
   → it is `<1 x i64>` uniform `s_load_b64` in BASE (Fact 1/2). NOT REFUTED.
3. "Show the baseline load lacks `!amdgpu.noclobber` so noalias is what supplies it." → BASE load
   already `!amdgpu.noclobber` (Fact 1). NOT REFUTED.
4. "Show an FFM numeric difference between A and not-A." → both `1 passed` (pure codegen). NOT REFUTED.

---

## Remaining unknowns (hand-offs, not blockers)

- **Reshape kernel A/not-A not separately toggled.** The MLA-cat gluon kernel is the "primary" S12
  could not compile and is fully measured here. The reshape kernel's divergent-vector slot_mapping
  is over-determined as noalias-inert by S5 (Fact F+C) and by S12's already-measured non-gluon vector
  case (gather_kv_b_proj Fact 1/2). A direct toggle would add confirmation but no new mechanism.
- **Larger shapes / QUANT_KV(uint8-NVFP4) / shuffled paths not run** (smallest bf16 T=1 only, per
  PLAN). The slot_mapping load's uniform-scalar shape (`slot_mapping_ptr + pid_b`, pid_b uniform) is
  size- and dtype-independent, so the "already-s_load, noalias-inert" verdict is not a small-size
  artifact.
- **No runtime numbers** (this is a mechanism/asm/FFM-correctness cell). Per PLAN Run-2 note: a
  byte-identical asm code section ⇒ identical runtime by construction, so no AM/gfx950 timing is
  warranted for the gluon primary — asm-identity is the stronger, noise-free proof of zero impact.
  No follow-up RT cell needed (no surprise delta to chase).

## Harness friction (for the orchestrator)
- Same FFM-lite friction as S12/R3: canonical `/am-ffm/ffmlite_env.sh` is ABSENT on this host; the
  WORKING env is `/zyin/rocdtif-7.13-am+ffmlite-mi400-r6.06/ffmlite_env.sh` (mi450/gfx1250 topology).
- `PYTEST_PLUGINS=ffm_teardown` requires `PYTHONPATH=/root/scripts/tools` (the plugin is not on the
  default path); without it pytest aborts with `No module named 'ffm_teardown'`. Combined with
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` (a `pylama` pytest11 plugin otherwise raises
  PluginValidationError).
- Piping the pytest run through `grep` under `flock` was detached to a background task by the harness;
  a clean full-log capture (`> file 2>&1` then grep) is the reliable pattern.
