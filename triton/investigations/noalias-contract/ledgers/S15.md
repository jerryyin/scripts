STATUS: done
CONCLUSION: Deliverable A assembled at REPORT_A_mechanism.md from ledgers S1/S4/S5/S6/S7/S8 with no self-contradiction found; per-opt verdict is — memory-invariance->scalar s_load (T1/T2): noalias NECESSARY and SUFFICIENT jointly with default-on -amdgpu-scalarize-global-loads (readonly and !invariant.load NEITHER); LoadStoreVectorizer + SILoadStoreOptimizer widening (T4): NECESSARY and SUFFICIENT (SLP NEITHER); GVN/CSE (T5): SUFFICIENT in general but NEITHER in the shipped kernels (no firing site — all stores are LDS/addrspace(3), already NoAlias to the addrspace(1) gather loads); DSE/MemCpyOpt/alias-scheduling (T5): NEITHER; global_load=>buffer_load/s_buffer_load (T3): NEITHER (buffer selection is offset-range-gated in Triton MLIR, aliasing never consulted).

# S15 — Synthesis A: assemble the kernel-agnostic mechanism research report

**Cell:** S15 (Phase 3, Synthesis). **Blocked-by:** S4-S8 (all done; S1 done).
**Question:** Assemble Deliverable A (kernel-agnostic MECHANISM RESEARCH REPORT) from the
T-topic ledgers: Triton's default aliasing story (S1), then per LLVM/AMDGPU optimization
(invariance/LICM, s_load scalarization, buffer_load, vectorization/coalescing,
GVN/DSE/CSE/scheduling) state whether noalias is NECESSARY/SUFFICIENT/NEITHER, each backed
by ledger before/after IR+asm evidence and the counter-experiment that failed to refute it.

---

## Frozen experiment (synthesis of committed ledgers only — no new runs)

- **Inputs (read-only, on disk):** `ledgers/{S1,S4,S5,S6,S7,S8}.md` — the T0/T1/T2/T3/T4/T5
  ledgers. No experiment re-run: this cell is a synthesis of committed conclusions, permitted
  by the task spec unless a ledger is self-contradictory (checked below — none found).
- **Provenance carried through from those ledgers (all agree):**
  - Triton tree/SHA `/root/triton` @ `ba4fd67b8ed28d37935f95ddbe8717853de7212d`
    (branch `users/jerryyin/moe-gather-sload-contract`; #120 contract landed).
  - Stock LLVM pins `56421f92` (primary) + `62b7cf96` (control), both LLVM 23.0.0git;
    every decisive case identical on both -> not a version artifact.
  - Target gfx1250, compile-only (pre-opt LLVM_IR_ENABLE_DUMP / triton-opt conversion /
    stock opt+llc). Reference kernel F1 `_moe_gemm_a8w4_prefill` (GatherIndx = arg %19).
  - Kill switch: BEFORE vs AFTER differ by exactly one `noalias` token on %19; post-opt
    .llir otherwise byte-identical (325 loads / 32 stores / 0 calls in both).
  - FFM correctness: F1 prefill PASS, rel_err=0.01001 identical contract-on/off.
- **Isolation for this cell:** `TRITON_CACHE_DIR=/tmp/tc-S15 TRITON_DUMP_DIR=/tmp/td-S15
  TRITON_ALWAYS_COMPILE=1` exported; no GPU/FFM run this cell (synthesis only), so no
  gpu-lock taken. No triton rebuild, no git write.
- **Deliverable written:** `../REPORT_A_mechanism.md` (305 lines).
- **Metric:** completeness — every T-topic (T0-T5) represented; every optimization named
  in the cell question has a necessary/sufficient/neither verdict + a cited ledger
  before/after evidence + the refuted counter-experiment.

---

## HOW-facts (what each source ledger establishes — verbatim anchors, not paraphrase)

- **S1 (T0):** pre-opt define shows GatherIndx as `ptr addrspace(1) noalias %19`, every
  other kernel ptr bare; grep of readonly|writeonly|align|dereferenceable|captures|noundef
  |nonnull|nocapture on the pre-opt define = empty. Frontend `KernelParam(noalias=False)`
  default; single backend sink `handlePointerContractArgs` (Utility.cpp:2233). Only 1
  AITer file opts in. => default is bare/may-alias.
- **S4 (T1):** post-opt %19 = `noalias nofree readonly captures(none)` AFTER vs
  `nofree readonly captures(none)` BEFORE (readonly in BOTH). Gather load
  `%187 = load <1 x i16> ... align 2` byte-identical both; `!invariant.load` count 0 both.
  Path: `AMDGPUAnnotateUniformValues.cpp:67 visitLoadInst` -> `!amdgpu.uniform` +
  (via `isClobberedInFunction`->`isReallyAClobber`->`AA->alias`=NoAlias) `!amdgpu.noclobber`
  -> scalarizer -> `s_load_u16`. 2x2: s_load/rfl=0 iff noalias AND scalarize-on. rfl 16->0.
- **S5 (T2):** `SIISelLowering.cpp:13148-13158` five-condition gate;
  `isMemOpHasNoClobberedMemOperand` line 2335 = `MONoClobber`. Sub-dword vector has no SMEM
  form (`SMInstructions.td:329-332`, result class SReg_32; no s_load_u16x4). DAG
  `t20: v8i16 ... invariant "amdgpu-noclobber"` still `flat_load_ushort x8`. Truth table +
  6 refuted counter-experiments on both pins. Fact E `@i32_mayalias` vs `@i32_noalias_store`
  = ISel-grain noalias kill switch.
- **S6 (T3):** isolated llc noalias-toggle identical `global_load_b32`. `canUseBufferOps`
  (ConvertToBufferOps.cpp:135-173) gated on splat+offset-width+`isFuncArgWith32bitPtrRange`
  /`isByteOffsetSmallerThan2GB`; grep noalias|mayAlias|aliasInfo = 0. Fact 5 A/not-A:
  @noalias_dyn stays tt.load, @range_dyn -> amdg.buffer_load. Only LLVM buffer pass lowers
  pre-existing addrspace(7). s_buffer_load never emitted by Triton.
- **S7 (T4):** LSV 2x2 `load <2 x float>` count c_none=0/c_ponly=1/c_qonly=1/c_both=1
  (necessary + sufficient on either base). MI before/after: `S_LOAD_DWORD x2` ->
  `S_LOAD_DWORDX2` under noalias; may-alias post-store load stays vector
  `GLOBAL_LOAD_DWORD_SADDR`, not merged. Both pins, LSV on/off. SLP = wrong pass.
- **S8 (T5):** isolated gvn/newgvn/dse/memcpyopt on the identical-except-noalias pair =
  zero instruction-level diff; -O3 -stats diff empty incl. `aa 853 MayAlias/5365 NoAlias`.
  Fact D: 32 stores all addrspace(3) LDS, 0 global stores -> addrspace(1) vs (3) NoAlias by
  construction. Fact G synthetic: gvn forwards under noalias (2->1 load). Fact E asm
  histogram s_load 9->41 / global_load 37->5 / rfl 37->3, 66 ds_ unchanged.

**Self-consistency check across ledgers (required before synthesizing):**
- "readonly is inferred in BOTH states / is not the switch" — consistent across S4 Hop1,
  S5 Fact E corollary, S8 Fact A. No contradiction.
- "the invariance carrier is backend `!amdgpu.noclobber`, not LLVM `!invariant.load`" —
  S4 Hop2/3 and S5 Fact A/E use the same MONoClobber flag; consistent.
- "the shipped F1 win is ISel scalarization, not a middle-end alias opt" — S4/S5 (it IS
  scalarization) and S8 Fact E (it is NOT GVN/DSE/etc) agree, non-overlapping scopes.
- S6 explicitly warns s_load (scalar global) != s_buffer_load — consistent with S4/S5
  never claiming buffer. No conflation.
- No ledger contradicts another; therefore no re-run was warranted (per task spec).

---

## Hypotheses + refutation trail (for the synthesis itself)

- **H1: "the report can state a single blanket verdict for noalias."** REFUTED by the
  evidence — the verdict is opt-specific and, for T5, further splits shipped-kernel
  (NEITHER) vs general-mechanism (SUFFICIENT). The report encodes both axes rather than
  collapsing them (honest theoretical-vs-observed, S8's own framing).
- **H2: "noalias => readonly is the canonical chain (naive)."** REFUTED across S4/S8:
  readonly is use-based inference present in both variants; the report leads with this
  correction so no downstream reader repeats the shorthand.
- **H3: "some ledger conflicts with another, forcing a re-run."** REFUTED by the
  self-consistency check above — all six agree; no re-run performed.

---

## CLAIM STATUS

**CLAIM (answered):** Deliverable A is assembled and internally consistent. Per-opt
verdicts (each cited + counter-experiment-refuted in the report):
- **Invariance/LICM -> scalar s_load (T1/T2):** noalias NECESSARY, SUFFICIENT jointly with
  default-on `-amdgpu-scalarize-global-loads`; readonly and LLVM `!invariant.load`/LICM
  NEITHER; the operative carrier is backend `!amdgpu.noclobber`.
- **s_load selection gate (T2):** noalias NECESSARY for the noclobber leg only where a
  store can clobber; never sufficient alone (align>=4 / uniformity / addrspace /
  sub-dword-vector ceiling are independent).
- **global_load=>buffer_load / s_buffer_load (T3):** NEITHER (offset-range-gated in Triton
  MLIR; aliasing never consulted; s_buffer_load never emitted).
- **Vectorization/coalescing (T4):** noalias NECESSARY and SUFFICIENT for LSV (IR) and
  SILoadStoreOptimizer (MI, indirectly via instruction selection); SLP NEITHER.
- **GVN/CSE/DSE/MemCpyOpt/scheduling (T5):** NEITHER in the shipped kernels (no firing
  site — all stores LDS); GVN load-forwarding SUFFICIENT as a general mechanism.

**Counter-experiment that would refute this cell:** "find a T-topic named in the cell
question that the report omits a necessary/sufficient/neither verdict for, or a verdict
unsupported by its ledger's before/after evidence, or a cross-ledger contradiction the
report papers over." -> Result: all of T0-T5 covered; every verdict traced to a named
ledger fact + refuted counter-experiment; self-consistency check found no contradiction.
Not refuted.

---

## Remaining unknowns (hand-offs, not blockers)

- **Family-rollout deltas (S9-S14 -> Deliverable B / S16):** whether the T4 widening and
  T5 GVN mechanisms have real firing sites in F2 (attention/PA index), F3-primary (GEMM
  A/B/C), F4 (KV-cache), F5 (norm) — the report states the mechanisms; measured per-family
  asm/runtime deltas are Deliverable B's job.
- **s_buffer_load reachability** and **wide-SMEM widening of the a8w4 u16 index** (i32-pack
  route) are design questions flagged by S5/S6, not mechanism facts — out of Deliverable A.
- **T6 soundness (S3) and T7 plumbing (S2)** are not yet done; they are Deliverable-B
  pillars (soundness verdict + annotate-arbitrary-arg surface), not needed for the
  kernel-agnostic mechanism report, but S16 will need them.

## Next unblocked cell
S16 (Synthesis B) — but it depends on S9-S14 (family rollout), which require S2/S3
(Phase 0) plus S4/S5, several still pending. S15 gates nothing further on its own.
