STATUS: done
CONCLUSION: S10 was WRONG. Decode's 8 in-loop v_readfirstlane are NOT a MachineLICM-only residual — they are a load-selection problem one hop upstream: the TDM descriptor's SGPR operand is fed by an index derived from the expert-metadata loads (ExptData/ExptHist/ExptOffs), which are VGPR-resident because those args lacked noalias; SIFixSGPRCopies then VALU-izes the (correctly scalar) descriptor and inserts the VGPR->SGPR readfirstlane round-trip. Expanding the decode contract to noalias_args=["GatherIndx","ExptData","ExptHist","ExptOffs"] scalarizes those loads to s_load, the index stays in SGPR, and the round-trip vanishes: in-loop rfl 8->0 (whole-fn rfl 0) on STOCK llc (both pins, no MachineLICM patch), FFM PASS rel_err 0.009413 cosine 0.999955. The frontend contract alone fixes decode; the LLVM patch is only a backstop.

# S10b — decode re-audit under the corrected "all uniform read-only args" protocol

Supersedes the decode verdict of ledgers/S10.md and REPORT/CONCLUSION recommendation #2.
Credit: user's independent investigation surfaced the metadata-load VGPR source; this cell
reproduces it end to end.

## Why S10 was wrong (the methodological miss)
S10 verified the `GatherIndx` load was already `s_load` and from that generalized "the
residual is NOT load-selection, only a convergent-hoist (MachineLICM) problem." That
stopped one hop short: it never traced the descriptor's VGPR operands through
`SIFixSGPRCopies` to the expert-metadata loads. The investigation-wide blind spot was
annotating only the *index* pointer; the decode round-trip is driven by *upstream* uniform
read-only loads (`ExptData/ExptHist/ExptOffs`) that stay in VGPR without their own noalias.

## Frozen experiment
- Kernel: `~/aiter/.../gfx1250/moe/moe_op_gemm_a8w4.py` `_moe_gemm_a8w4_decode`, gfx1250.
- Compile: FFM (`/zyin/rocdtif-7.13-am+ffmlite-mi400-r6.06/ffmlite_env.sh`) via
  `run_moe_gemm_ffm.py --kernel a8w4 --backend gluon --phase decode`, batch=64, experts=32/4,
  block_m=16, TRITON_ALWAYS_COMPILE=1, per-cell TRITON_CACHE_DIR.
- Measure: stock `llc` pins `56421f92` and `62b7cf96` (LLVM 23.0.0git), `-mcpu=gfx1250`,
  `-filetype=asm`; in-loop rfl via the awk loop-body counter. (The patched-only flag
  `-amdgpu-hoist-uniform-readfirstlane` is absent on stock llc and was NOT used — this is a
  pure stock-LLVM result.)
- Kill switch: the ONLY delta is the decode `noalias_args` set. Tree reverted after (clean).

## HOW-facts (measured this session)
- BASELINE `noalias_args=["GatherIndx"]` (cached S10 llir, 1 noalias on define):
  in-loop rfl = **8** on both pins (whole-fn 19 / 23). TDM row-index operand is scalar.
- AFTER `noalias_args=["GatherIndx","ExptData","ExptHist","ExptOffs"]` (recompiled; define
  carries **4** noalias; a 2nd specialization carried 3 where one arg was None/constexpr):
  in-loop rfl = **0**, whole-fn rfl = **0**, on stock llc.
- Correctness: FFM `RESULT: PASS`, rel_err 0.009413, cosine 0.999955 (matches user's numbers).

## Mechanism (consistent with S4, applied one hop upstream)
`noalias` on ExptData/ExptHist/ExptOffs -> FunctionAttrs readonly -> AMDGPU scalarizes the
uniform `gl.load(ExptData/ExptHist/ExptOffs)` to `s_load` (SGPR) -> `block_id`/`M`/`start_m`
and the whole index chain stay in SGPR -> `SIFixSGPRCopies` has no VGPR->SGPR copy to
legalize -> ISel's scalar descriptor (`S_AND`/`S_OR` -> `sgpr_128` -> `TENSOR_LOAD_TO_LDS`)
survives -> no VGPR excursion -> no convergent readfirstlane -> nothing for MachineLICM to
hoist. This is the same `noalias -> readonly -> s_load` chain S4 proved for the index load,
now on the metadata loads that were the true VGPR source.

## CLAIM STATUS
- Decode is fixed by the FRONTEND contract alone (all uniform read-only args feeding the
  index), on stock LLVM. **Necessary counter-experiment:** annotate only `GatherIndx` ->
  reverts to 8 (baseline, measured). Not refuted.
- The MachineLICM patch (db4972674) is a *backstop* (hoists the round-trip if the full
  contract isn't declared), not the required fix. Recommendation #2 corrected accordingly.

## Remaining unknowns / hand-off
- The `ExptOffsSum` arg was NOT added (user's set excluded it); result already 0, so it is
  not needed for decode. Not investigated further.
- This same blind spot (index-only annotation) invalidates the *method* behind the other
  no-op verdicts -> triggers the S-RA re-audit of F2/F3/F4/F5/F6 under the all-args protocol.
