STATUS: done
CONCLUSION: Run 2 closes every caveat except one: both non-compiling kernels are fixed (R3) and re-measured as no-ops (S9b/S12b), both open survivors (S17 causal_conv1d, S18 gmm) are proven byte-identical no-ops, so broadening noalias beyond F1 yields ZERO additional codegen wins across all six re-examined non-F1 groups (md5-matched gfx1250 `.text`); the ONE remaining open item is the F1 live cycle count — BOTH runtime tracks (AM for TDM, gfx950 for non-TDM) were built but blocked on this host by environment/ABI mismatches orthogonal to noalias (AM: `hsaKmtCreateQueueV2` segfault; gfx950: gfx1250-only torch `hipErrorInvalidImage`), so F1 impact remains quantified only by the S10 static ~5.7%-of-K-loop estimate; asm-identity is the stronger zero-impact proof for every no-op group. REPORT_C_runtime.md written; REPORT_B caveat replaced.

# S20 — Run-2 runtime-impact synthesis + caveat closure

## Frozen experiment

- **Cell:** S20 (Run 2, Phase D). Task: consolidate all Run-2 runtime + closure results
  into a rigorous impact report; fold real numbers into REPORT_B; state which caveats are
  now closed. **Synthesis of committed ledgers ONLY — no experiment re-run** (per PLAN
  rule: synthesis cells read committed ledgers, do not re-measure).
- **Blocked-by:** RT1, RT2, RT3, RT4, S9b, S12b — all present on disk (RT1/RT2 blocked;
  RT3/RT4/S9b/S12b done). Also read R1, R2, R3, S17, S18, and Run-1 S1/S4/S10 + REPORT_A/B.
- **Triton tree/SHA:** `/root/triton`, installed triton 3.8.0, branch
  `users/jerryyin/moe-gather-sload-contract` (#120 landed). Not rebuilt (contract is a
  Python annotation toggle).
- **AITer tree:** `/root/aiter` with R3's three source fixes applied by the R3 cell;
  S9b/S12b measured on that tree; all Run-2 cells report tree left clean, no commits.
- **LLVM pins:** stock 56421f92/62b7cf96 (compile-only ISel/AA); patched
  `/root/llvm-project/install` @ db4972674 (MachineLICM hoist) for the F1-decode leg only.
- **Runtime targets:** AM (`run_on_model.sh --backend am`) for TDM gfx1250 (F1);
  gfx950/MI355X for non-TDM (conv1d, gmm). Metric: warmup + median-over-reps where
  reachable; else asm-identity (md5 `.text`) per the PLAN Run-2 gating rule.
- **Isolation:** TRITON_CACHE_DIR=/tmp/tc-S20, TRITON_DUMP_DIR=/tmp/td-S20 (unused —
  no compile/run in a synthesis cell).
- **Deliverables written:** `REPORT_C_runtime.md`; appended "Run 2 — measured runtime"
  section to `REPORT_B_dossier.md` (replacing the "no runtime numbers exist" caveat);
  this ledger. REGISTRY.md NOT edited (orchestrated mode).

## HOW-facts (real ledger numbers — never paraphrased)

### Fact 1 — AM runtime harness blocked (R1/RT1/RT2), re-verified live in RT1
- AM emits per-dispatch cycle timing (HelloWorld: `cp_dispatch_busy_cycles`/`draw.log`).
- torch/Triton device probe segfaults rc=11/signal 11 on
  `libdtif.so: undefined symbol: hsaKmtCreateQueueV2` (AM ROCm-7.13 DTIF vs host
  torch-2.11/ROCm-7.14 KMT-thunk ABI) BEFORE any TDM compile/dispatch. FFM (gfx1250) on
  same tree passes -> fault isolated to AM. No AM cycle number for F1 prefill or decode
  obtainable. RT1/RT2 bailed clean.

### Fact 2 — gfx950 harness blocked (R2), but RT3/RT4 did not need it
- Only installed torch (2.11.0+rocm7.14) has `get_arch_list() == ['gfx1250']` -> every
  MI355X (gfx950) launch fails `hipErrorInvalidImage`. RT3/RT4 closed on asm-identity
  (S17/S18), not on the blocker.

### Fact 3 — F1 (the one real win) baseline codegen delta (Run-1 S4/S10)
- Prefill: in-loop v_readfirstlane 16->0; index load global_load_u16 -> s_load_u16 x32
  in prologue (shipped #120).
- Decode: index already s_load_u16 x16 under contract; residual 8 in-loop rfl -> LLVM
  patch db4972674 hoists 8->0, total rfl preserved 19->19, byte-identical descriptor
  operands. Static impact ~5.7% of a 141-instr TDM/MFMA-bound K-loop (S10). FFM decode
  rel_err 0.009413 cosine 0.999955.

### Fact 4 — F2 pa_decode_sparse re-measured (S9b): NO-OP
- One raw index load `kv_start/kv_end = gl.load(kv_indptr_ptr + t)` (i32x2, uniform,
  readonly arg %4) ALREADY `s_load_b64 s[56:57], s[4:5], 0x0` at baseline; `amdgpu.noclobber`
  count = 0 in BOTH variants. Other index rides TDM. noalias -> 1 metadata hunk
  (`.actual_access: read_only`), 0 machine instructions, rfl 64->64, both FFM-correct.

### Fact 5 — F4 fused_kv_cache gluon primary re-measured (S12b): NO-OP
- `gl.load(slot_mapping_ptr + pid_b)` uniform scalar `<1 x i64>` ALREADY
  `s_load_b64 s[12:13], s[12:13], 0x0` at baseline (`!amdgpu.noclobber` already set,
  readonly, single entry use, no loop). Code section lines 1-907 BYTE-IDENTICAL; only a
  3-line `.actual_access: read_only` metadata diff, 0 instructions, FFM-correct both.

### Fact 6 — F6 causal_conv1d (S17/RT3): NO-OP
- `.text` md5 `779cabac97a506207b0d3c4402f35969` for BOTH ON and OFF (empty text diff).
  Two int32 index loads already `s_load_b64` + `s_load_b32` at baseline (readonly-invariant,
  uniform addr). i8 has_initial_states stays global_load_u8 + 1 non-loop rfl in both —
  S5 sub-dword ceiling, not aliasing. Only delta: 3 `.actual_access: read_only` YAML lines.

### Fact 7 — F6 gmm (S18/RT4): NO-OP
- amdgcn instruction stream md5 `a45bf8925aca27e7174969c16ee51ded` IDENTICAL ON vs OFF;
  full LLIR diff = the single `noalias` define-line token. Loop-invariant reload NOT
  hoisted in either variant; S14 atomic-may-alias hypothesis refuted twice (AA proves
  NoAlias without the attr — addrspace(8) buffer-load vs addrspace(1) atomic; deleting
  the atomic still does not enable the hoist).

### Fact 8 — R3 fixes (the two non-compiling kernels)
- pa_decode_sparse.py (2 async_gather) + fused_kv_cache.py gluon reshape (1 async_gather):
  dropped removed `src_col_offset=0` positional (triton PR #61); all descriptors at
  column 0 -> no data moves. Controlled revert reproduces
  `'constexpr' object has no attribute 'shape'`.
- fused_kv_cache.py MLA-cat launcher: inserted missing `MAX_EMBD_POS=cos.shape[0]`
  (off-by-one dropped `kv_cache_stride_d`). Controlled revert reproduces
  `TypeError: missing 1 required positional argument: 'kv_cache_stride_d'`. All three
  compile + FFM-pass at smallest exercising shape.

## Per-shape runtime table (as required for runtime cells)

| shape | ON | OFF | delta% | n_reps | spread |
|-------|----|----|--------|--------|--------|
| F1 prefill/decode (any) | — | — | — (AM init segfault, R1/RT1/RT2) | 0 | n/a |
| F2/F4/F6-conv/F6-gmm (all) | asm-identical | asm-identical | 0 (byte-identical `.text`) | 0 (no timing needed) | n/a |

No live cycle rows: AM cannot init (F1); non-F1 groups are md5-identical so timing is
moot (RT3/RT4 gating rule). This is the honest limit of what this host resolved.

## Refutation trail
- *"Maybe a non-F1 group has a runtime delta the asm diff missed."* Refuted: for a pure
  codegen hint with byte-identical `.text` (md5-matched), runtime is identical by
  construction — there is no dynamic path that differs. Metadata YAML (`.actual_access`)
  is informational, not executed.
- *"Maybe the fixed kernels (S9b/S12b) now expose a win the Run-1 prediction missed."*
  Refuted: both re-measures reproduce the S9/S12 uniform-scalar leg exactly (baseline
  s_load_b64, noclobber-independent), NO delta.
- *"Maybe S17/S18 survivors fire once actually built."* Refuted: S17 md5-identical (the
  loads were already scalar); S18 md5-identical + hoist does not fire even with the
  atomic deleted.

## CLAIM STATUS
- CLAIM: Broadening noalias beyond F1 yields zero additional codegen/runtime wins on
  gfx1250. **STATUS: CONFIRMED** (measurement-grade: md5-matched `.text` for all six
  non-F1 groups). Counter-experiment that would refute: find any non-F1 kernel where the
  noalias token changes >=1 machine instruction (none found across F2-F6 + the two fixed
  kernels).
- CLAIM: F1 live runtime cannot be measured on this host. **STATUS: CONFIRMED** (AM ABI
  segfault re-verified live in RT1; gfx950 torch is gfx1250-only). Counter-experiment to
  lift it: a matched AM/torch stack, or a gfx950-capable torch, or a real gfx1250 device.

## Remaining unknowns
- F1 prefill/decode **live cycle count** — unquantified; S10 static ~5.7%-of-K-loop is the
  only estimate. Needs a matched AM/torch stack or real gfx1250 hardware.
- F2 unified_attention_2d/3d asm still extrapolated from mla (Run-1), not independently
  dumped — but same load class, and every dumped F2 leg is a no-op, so low risk.
