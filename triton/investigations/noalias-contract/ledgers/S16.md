STATUS: done
CONCLUSION: Deliverable B assembled from committed family ledgers: the F1-prefill noalias win does NOT generalize — of six AITer gfx1250 families, only F1 (prefill shipped in #120; decode closed by LLVM patch db4972674, modest) is a real win, F2/F3/F4/F5 are PROVEN no-ops (noalias produces zero or metadata-only asm change: F2 mla already s_load at baseline via readonly-invariant leg, F3 A/B via TDM ptrtoint + C buffer_store has no firing site, F4 read-gather is a divergent vector buffer_load and scatter-index already s_load, F5 is all-TDM with no raw-pointer load), and only F6 causal_conv1d (S5 s_load candidate, strongest) + F6 gmm (S8 LICM candidate) survive as UNMEASURED plausible wins needing new cells; plumbing (S2) already annotates ANY pointer incl. primary A/B/C with zero backend change, the only missing surface being user-declared readonly (deleted in #120, now LLVM-inferred only) which no scanned family needs.

# S16 — Synthesis B: broadened-implementation plan + per-kernel justification dossier + measured asm-delta table

**Cell:** S16 (Phase 3, Synthesis B). **Blocked-by:** S9-S14 (all done; read from disk).
**Question:** Assemble the broadened-implementation plan + per-kernel JUSTIFICATION DOSSIER +
measured asm-delta table (Deliverable B) from the family ledgers.

This is a **synthesis-only** cell: no new experiment, no GPU, no compile. Every claim is a
roll-up of a committed family ledger (S2, S3, S9, S10, S11, S12, S13, S14). The deliverable is
`REPORT_B_dossier.md` (written this session) + this ledger.

---

## Frozen experiment (inherited, verified consistent across all source ledgers)

- **Triton tree/SHA:** /root/triton @ ba4fd67b8ed28d37935f95ddbe8717853de7212d
  (users/jerryyin/moe-gather-sload-contract; #120 landed) — confirmed identical in S2/S3/S9-S14.
- **AITer tree/SHA:** /root/aiter @ 93d8ffb8e2a101f7451653b8e3b9e12b61334e46 — confirmed in
  S9/S12/S14 (S11 "working tree", S13 same tree). No divergence across ledgers.
- **Verified this session:** `git rev-parse HEAD` in both trees returns the above SHAs — the
  synthesis is over one frozen tree state, so the family results are comparable as one run.
- **Target:** gfx1250. **Correctness vehicle:** FFM-lite mi450
  (/zyin/rocdtif-7.13-am+ffmlite-mi400-r6.06/ffmlite_env.sh). No gfx1250 timing exists; gfx950
  host torch broken (hipErrorInvalidImage) so no runtime leg anywhere.
- **No new isolation dir needed** (no compile/GPU); TRITON_CACHE_DIR=/tmp/tc-S16 reserved per
  orchestration but unused (synthesis reads text only).

---

## HOW-facts (each traced to its source ledger's real IR/asm — not re-derived)

### Measured asm deltas (the core table)
- **F1 prefill (S3 Facts 2-3 + repro):** noalias -> LICM hoist -> !amdgpu.noclobber -> s_load;
  in-loop v_readfirstlane 16->0, index global_load_u16 -> s_load_u16 x32 prologue. Positive
  control that the toggle is live.
- **F1 decode (S10 Facts 1,4,5):** load already s_load_u16 x16 under contract (global_load_u16=0);
  residual 8 in-loop rfl at 8 on BOTH stock pins; patch db4972674 -> 0 by hoist, total rfl 19->19,
  descriptor op tensor_load_to_lds s[60:63],s[44:51],s[16:19],s[56:59] byte-identical.
- **F2 mla (S9 Facts 2,3):** baseline .LBB0_15 first insn `s_load_b32 s46, s[38:39], 0x0` at
  mla.py:2332:30 (already scalar); noalias diff = exactly 2 metadata lines
  (.address_space:global -> .actual_access:read_only), 0 instructions; noclobber count = 0.
- **F3 (S11 Facts 1,2,3):** buf_rsrc GVN loads 2 (mayalias) vs 1 (noalias) — mechanism reaches AA;
  tdm_ptrtoint bodies byte-identical — inert on A/B; c_store_only.s byte-identical — no firing site.
- **F4 (S12 Facts 1,2,3,4):** kv_indices = amdg.buffer_load x6 divergent vector, histogram
  byte-identical (93 buffer_load_b128|256 s_load_b128|... both); slot_mapping already
  `s_load_b32 s4,s[12:13],0x0`, noalias diff = 3 metadata lines. gluon primary fails to compile.
- **F5 (S13 Fact 3):** amdgcn md5 identical 8e683109c26c2192430ba5e0e449514c both variants; only
  post-opt delta is the noalias token; all traffic TDM (0 tt.load/tt.store).
- **F6 (S14):** causal_conv1d uniform i32 index loads at conv/causal_conv1d.py:70/71/88 (S5
  candidate); gmm group_sizes reload at gmm.py:112 in _work_stealing_gmm loop with in-loop
  atomic_add at :390 (S8 candidate). Both source-level triage; NOT dumped (S14 harness friction).

### Plumbing surface (S2 Facts A,B,C)
- Any pointer incl. primary A/B/C gets llvm.noalias today (probe_primary: arg0/1/2 all tagged);
  gate is non-constexpr only; sink = handlePointerContractArgs (Utility.cpp:2233).
- User-declared readonly is INERT (tt.readonly probe -> zero LLVM attrs); mapping deleted in #120.

### Soundness (S3)
- Contract = (readonly) never write through P + (noalias) P not overlapping any writable arg;
  violation = silent miscompile (stale-index CSE proven by GVN 2nd-load elimination + gfx1250 K$
  incoherence). Applied per family in S9/S11/S12/S13 — every no-op family would be sound-if-applied
  but the S3 liability is unjustified without a codegen gain.

---

## Hypotheses + refutation trail (synthesis-level)

- **H1 "the MoE prefill win generalizes across families."** REFUTED. Four families (F2/F3/F4/F5)
  show zero or metadata-only asm delta under the identical toggle; each fails a distinct one of
  the four mechanism conditions (raw-pointer load / uniform / scalar-SMEM width / in-loop aliasing
  store). The positive control (F1) confirms the toggle is live, so the null results are specific.
- **H2 "some family needs a plumbing extension to see the win."** REFUTED for annotation
  (S2 Fact A: primary/any arg already annotatable, zero backend change) — no family's no-op is
  caused by inability to annotate. The only missing capability (user-declared readonly, S2 B/C)
  is needed by NONE of the scanned families (readonly is inferred everywhere the read pointers
  need it: S3/S9/S12).
- **H3 "F6 survivors are confirmed wins."** NOT ASSERTED — they are triage-only (S14), explicitly
  UNMEASURED; the dossier flags them INVESTIGATE with the exact asm/FFM experiment each needs.
- **H4 "runtime numbers could change the ship calls."** N/A — no gfx1250 timing exists (FFM =
  correctness) and gfx950 host torch is broken; every verdict rests on asm delta + FFM-correct,
  as the PLAN scopes. A zero-instruction asm delta cannot be a runtime win regardless.

---

## CLAIM STATUS

**CLAIM (answered, roll-up of committed ledgers):** Deliverable B is assembled. The broadened
contract has exactly two real wins — F1 prefill (shipped) and F1 decode (LLVM patch, modest) —
four proven no-ops (F2/F3/F4/F5, each failing a distinct mechanism precondition, producing zero or
metadata-only asm delta and staying FFM-correct), and two unverified plausible F6 survivors
(causal_conv1d S5-s_load; gmm S8-LICM) that need dedicated cells. Plumbing already supports
annotating any pointer including primary operands with no backend change; the sole missing surface
(user-declared readonly) is needed by no scanned family.

**Counter-experiment that would refute it, and its result:**
- "Find a scanned family (other than F1) where the noalias toggle changes >0 machine
  instructions." -> None: F2 = 2 metadata lines/0 insns (S9), F3 = byte-identical epilogue (S11),
  F4 = histogram-identical / 1-3 metadata lines (S12), F5 = md5-identical (S13). NOT REFUTED.
- "Show a family's no-op is caused by a plumbing gap (can't annotate the pointer)." -> S2 Fact A +
  S11 Fact 5: every pointer incl. primary is annotatable today. NOT REFUTED.
- "Show the two F6 survivors are already measured wins (not just triage)." -> S14 is explicitly
  source/IR-structure triage; asm not dumped (harness friction). Flagged UNVERIFIED, not claimed.
  NOT REFUTED (the claim already states they are unverified).

The synthesis is bounded by its inputs: it is irrefutable *as a roll-up* (every row cites a
committed ledger fact); it is NOT a claim that the two F6 survivors are or are not wins — that is
explicitly handed off.

---

## Remaining unknowns (hand-offs, not blockers)

- **F6 causal_conv1d full cell** (highest priority): dump gfx1250 asm A/not-A for the i32 index
  loads; confirm baseline v_readfirstlane+global_load -> s_load under noalias; check the bool/i8
  sub-dword ceiling on has_initial_states_ptr; FFM at smallest continuous-batching size.
- **F6 gmm full cell** (after S8): confirm the atomic_add->group_sizes may-alias blocks the LICM
  hoist today and that noalias enables it; count per-iteration global_load of group_sizes.
- **F3 gemm_a16w16_atomic:** the one place the make.buffer.rsrc->GVN write-operand mechanism could
  fire (RMW-C epilogue); not in the F3 pair scanned.
- **F4 gluon primary:** unverifiable until the aiter<->triton API drift is fixed (out of scope).
- **No gfx1250 runtime anywhere:** all verdicts are asm-delta + FFM-correct; a maintainer wanting a
  perf number needs a cycle model or a gfx1250 execution environment.

## Deliverable location
`~/scripts/triton/investigations/noalias-contract/REPORT_B_dossier.md` (written this session).
