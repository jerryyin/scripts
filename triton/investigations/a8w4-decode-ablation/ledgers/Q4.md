STATUS: done
CONCLUSION: The gfx1250 one-workgroup fusion dispatched the owned Q4 child and passed 5/5 full FFM cases plus 10/10 public rejection cases against the actual executable Q1→Q3 composition; every one of the seven grouped-route fields and every reconstructed Q1 ID/weight was exact, while final IR proved that no global top-k route buffer exists.

# Q4 — fused softmax top-k plus grouped route materialization

## Frozen experiment

- Triton source: `/root/triton-mi450` at `80e223f93d59359161f5482fbb69bbfab29c0a0b`. Local `HEAD` and live `origin refs/pull/154/head` independently resolved to that SHA before this ledger was created.
- AITER source: `/root/aiter` at `4a1cc773f34cbfc74387259e51262556ee38edd0`.
- TokenSpeed provenance source: `/root/tokenspeed` at `3e725ac2b785b71f27ff9e9ac3796349c495d225`.
- Plan source: `/root/scripts` at `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf`.
- Requested and coordinator-confirmed launch profile: `gpt-5.6-sol`, reasoning effort `ultra`, `fork_turns=none`.
- Grouping parent: completed Q3 `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k20_precomputed_topk_group_route.py`, SHA-256 `33d1afba612af921df7ec8fe5dbab31c8a43423d1df4f665660aa9b0095ee791`.
- Router donor: completed Q1 `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k18_softmax_topk_router.py`, SHA-256 `e4c7153744ae02931724a6592da47618490458ae19a0b402df0660e0d1b74cbe`.
- Fusion provenance: TokenSpeed `_fused_topk`, `_fused_route_small_m`, `_route_small_m`, and `gluon_fused_route` in `/root/tokenspeed/tokenspeed-kernel-amd/python/tokenspeed_kernel_amd/ops/moe/fused_mxfp_gfx950.py:9175-9264,9385-9505,10635-10692,11253-11277`.
- Owned child: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k21_fused_softmax_group_route.py`.
- Owned ledger: this file.
- One allowed idea: remove only the global materialization boundary between Q1 and Q3 by keeping Q1's selected expert IDs and FP32 weights as in-program values and feeding them directly into Q3's histogram/prefix/schedule/stable-rank construction in the same workgroup. Q4 does not add biased/grouped selection, an M=1 shortcut, GEMM, a new output dtype boundary, or an unrelated layout experiment.
- Donor compatibility boundary: TokenSpeed's fused donor establishes the one-program ownership and direct SSA-value handoff, but its helper selects by packed logits, softmaxes only selected logits, casts gates to the input dtype, and has no Q1 normalization/scale arguments. Those are not Q1 semantics. Q4 therefore ports the fusion structure from TokenSpeed while retaining Q1's source-exact full-row FP32 softmax, smaller-expert tie break, optional selected-gate renormalization, and unconditional routing scale.
- Public input ABI: rank-2 CUDA `router_logits[M,E]` in `{float16,bfloat16,float32}`, integer `top_k`, boolean `normalize_topk_weights`, and float `routed_scaling_factor`. The supported intersection is `1 <= M <= 16`, `1 <= top_k <= E <= 1024`, and `G=M*top_k <= 64`. Non-contiguous logits are accepted and made contiguous before launch, matching Q1.
- Public output ABI is the Q3 `GroupedRoute` contract:
  - `slice_sizes[E]`, `int32`, selected-route histogram per expert.
  - `slice_offsets[E+1]`, `int32`, exclusive histogram prefix with terminal route count.
  - `block_offsets[5,E+1]`, `int32`, exclusive block-count prefixes for block sizes `(16,32,64,128,256)`.
  - `block_schedule[5,MAXBLK]`, `int32`, packed `(block_id << 16) | expert_id` entries followed by `-1`.
  - `gather_indices[G]` and `scatter_indices[G]`, `int32`, stable expert grouping by original token-major flat route index.
  - `gates[G]`, `float32`, matching Q1's selected FP32 weights after optional normalization and unconditional scale.
- Invalid-route policy: Q1 stable selection produces unique IDs in `[0,E)` for supported finite logits, so the accepted prefix normally has length `G`. The device body nevertheless retains Q3's validity mask and deterministic rejected tail (`gather=-1`, `scatter=-1`, `gate=0`) rather than weakening that ABI. Non-finite-logit behavior remains outside this frozen matrix.
- Numerical and ordering contract: load stored logits and convert to FP32; compute max-subtracted full-row softmax in FP32; select descending softmax scores with exact ties resolved to the smaller expert ID; emit the selected full-row softmax gates as FP32; optionally renormalize those selected gates in FP32 using Q1's zero-denominator guard; always multiply by `routed_scaling_factor`; group routes stably by `(expert_id, original_flat_route_index)` without gate arithmetic. Integer outputs must match exactly. Gates use Q1's frozen `atol=5e-3`, `rtol=5e-3` against the actual composition, with exact equality recorded separately when observed.
- Intrinsic launch restriction: the grid is exactly one workgroup. `NUM_WARPS` is derived exactly as Q1 and the TokenSpeed fused wrapper do: one warp for `M<=2`, four warps otherwise. Required cases exercise both. `MP`, `EP`, `GP`, `TKP`, and `MAXBLKP` are derived powers of two, not tuning axes. All five Q3 block-size rows are always emitted and tested.
- Required actual unfused comparator: for each CUDA input, call completed Q1 `invoke_softmax_topk_route_gluon` with the exact normalization/scale arguments, then pass its materialized FP32 weights and `int32` IDs to completed Q3 `gluon_precomputed_topk_group_route`. Compare every one of the seven public Q4 fields against that actual two-kernel result on FFM. Separately reconstruct flat selected IDs/weights from Q4's scatter permutation and expert slices and compare them to the actual Q1 intermediates.
- Required full FFM cases:
  - `tied-m1-k4-f16-normalized`: `M=1,E=8,top_k=4`, FP16 exact four-way tie, normalization enabled, scale 1.75; exercises stable smaller-ID order, one warp, and all five schedule rows.
  - `regular-m2-k2-bf16-unnormalized`: `M=2,E=5,top_k=2`, BF16 ordinary/tied logits, normalization disabled, scale 0.5; exercises non-power-of-two E and the one-warp boundary.
  - `extreme-m4-k1-bf16-unnormalized`: `M=4,E=8,top_k=1`, BF16 `-1000/1000`, normalization disabled, scale 0.5; exercises softmax saturation and four warps.
  - `mixed-m4-k3-f32-unnormalized`: `M=4,E=13,top_k=3`, FP32 ties/extremes, normalization disabled, scale 2.827; exercises `G=12 -> GP=16`, `E=13 -> EP=16`, stable ranks, and unconditional scale.
  - `max-routes-m16-k4-f32-normalized`: `M=16,E=13,top_k=4`, FP32 deterministic repeated expert choices, normalization enabled, scale 1.25; exercises `G=64`, multiple stable ranks per expert, and the public route-count boundary.
- The smoke suite is `tied-m1-k4-f16-normalized`, because it proves owned-child compilation/dispatch, Q1 stable tie/normalization/scale semantics, Q3 metadata construction, all five schedule rows, and the one-warp specialization against the actual Q1→Q3 composition.
- Public pre-launch rejection matrix: rank-1 logits, unsupported `float64`, `M=0`, `M=17`, `top_k=0`, `top_k>E`, `E=0`, `E=1025`, `M*top_k=80`, and CPU logits.
- Exact smoke command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/Q4 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k21_fused_softmax_group_route.py --suite smoke
```

- Exact full command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/Q4 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k21_fused_softmax_group_route.py --suite full
```

## Parent → child ownership change

- Before Q4, Q1 program 0 owns `M x E` logits and globally stores `M x top_k` IDs/weights; Q3 program 0 reloads those two tensors, groups them, and stores seven final fields. The explicit boundary is two global intermediate tensors and two launches.
- After Q4, one grid program owns logits, Q1 softmax/selection state, flat selected IDs/weights, and Q3 grouping state. Selected routes remain in device-local SSA values from selection through histogram and stable-rank placement; only the seven final fields are global outputs.
- Expert grouping, block schedule encoding, and stable rank stay Q3-identical. The math primitive adds Q1's FP32 max/exp/sum/divide and stable selection before Q3's integer histogram/scans/rank reduction. It introduces no bias, sigmoid, GEMM, MFMA, or WMMA.
- Observed global traffic is one logits load tile plus stores of the seven final fields. No top-k ID/weight pointer exists in the fused kernel signature. Every final TTGIR specialization contains exactly one `tt.load`, rooted at `Logits`, and 29 statically unrolled `tt.store` operations rooted in the seven final output pointers; selected IDs/weights pass through SSA `tt.gather` values rather than a global store/load boundary.
- Q3 grouping and Q1 layout collectives do lower through compiler-managed dynamic LDS and barriers. Final metadata reports 128 bytes of shared memory for four specializations and 256 bytes for `G=64`, with zero global scratch in all five. LLVM declares external address-space-3 `@global_smem`; ISA emits `ds_*` operations and barriers while reporting fixed group/private segments of zero because the launch supplies the metadata-sized dynamic LDS allocation.

## Source diff classification

- Essential fusion from Q1: Q4 retains Q1's logits-to-FP32 load, full-row max-subtracted softmax, repeated maximum selection, smaller-expert tie break, optional selected-gate normalization with the same zero-denominator guard, and unconditional scale. It removes Q1's top-k output pointers, output strides, and two global stores per selected slot.
- Essential fusion into Q3: Q4 removes Q3's `TopkIds`/`TopkWeights` pointers, input strides, and two global loads. It flattens Q1's `[MP,TKP]` selected tiles into token-major `[GP]` `idx`/`vals` tensors with layout conversion and SSA `tt.gather`, then feeds those values directly to Q3's validity, histogram, prefix, five schedule rows, stable-rank match, final scatter, and deterministic-tail code.
- Essential shared layout ownership: Q1's derived `NW_C` remains the fused program's warp policy, so Q3's one-warp `LE/LG/LB/LT` layouts become otherwise identical wave32 layouts parameterized by `NW_C`. This is necessary because one program now owns both phases; it is not an exposed tuning axis.
- Preserved Q3 behavior: from `valid = ...` through the rejected-tail stores, Q4 keeps Q3's ordering and metadata logic. The only payload-boundary specialization is FP32 `GateScal`, which is exactly the dtype materialized by Q1 and consumed by the actual Q1→Q3 composition.
- Mechanical launch support: allocate only the seven Q3 outputs, derive `MP/EP/GP/TKP/MAXBLKP`, launch grid `(1,)`, keep the shared bounds, make non-contiguous logits contiguous as Q1 does, and expose launch metadata for dispatch proof.
- Mechanical verification support: import completed Q1 and Q3 only in the host harness, execute them in sequence on the same CUDA logits, compare all seven outputs, invert Q4's stable permutation to recover the Q1 intermediates, assert the FFM/cache environment, run ten public rejection examples, and tear down the FFM process. Neither parent is called by the public Q4 implementation.
- Scope audit of the final device body found exactly one source `gl.load`, 11 syntactic `gl.store` sites that unroll to 29 TTGIR stores, and no bias, sigmoid, matmul/GEMM, dot, MFMA, WMMA, atomic, or M=1 branch. Python AST parsing, tab scan, and trailing-whitespace scan passed.

## FFM cases and evidence

The final-source smoke command exited 0 and reported:

```text
DISPATCH_PROOF {"ablation_id":"Q4","compiled_kernel":"_fused_softmax_group_route_small_m","forbidden_route_buffer_arguments":[],"grid":[1],"num_warps":1,"one_workgroup":true,"selected_child":".../k21_fused_softmax_group_route.py","template_file":".../k21_fused_softmax_group_route.py","result":"PASS"}
CASE_RESULT {"case":"tied-m1-k4-f16-normalized","field_equality":{"slice_sizes":"EXACT","slice_offsets":"EXACT","block_offsets":"EXACT","block_schedule":"EXACT","gather_indices":"EXACT","scatter_indices":"EXACT","gates":"EXACT"},"recovered_ids":[[0,1,2,3]],"recovered_weight_max_absolute_error":0.0,"max_absolute_error":0.0,"max_relative_error":0.0,"rms_relative_error":0.0,"assert_close":"PASS"}
SUITE_RESULT {"ablation_id":"Q4","cases_passed":1,"cases_total":1,"result":"PASS","suite":"smoke"}
```

The final-source full command then exited 0 and reported `SUITE_RESULT {"ablation_id":"Q4","cases_passed":5,"cases_total":5,"result":"PASS","suite":"full"}`.

| Case | Derived shape / launch | Exact distinctive outputs from the actual Q1→Q3 comparator | All seven fields | Gate max abs / max rel / RMS rel | Result |
|---|---|---|---|---|---|
| `tied-m1-k4-f16-normalized` | `G=4,GP=4,E=8,EP=8,MAXBLK=4`, one warp | IDs `[[0,1,2,3]]`; sizes `[1,1,1,1,0,0,0,0]`; offsets `[0,1,2,3,4,4,4,4,4]`; every schedule row `[0,1,2,3]`; gather `[0,0,0,0]`; scatter `[0,1,2,3]`; FP32 gates `[0.4375,0.4375,0.4375,0.4375]` after normalize and scale 1.75 | EXACT | `0 / 0 / 0` | PASS |
| `regular-m2-k2-bf16-unnormalized` | `G=4,GP=4,E=5,EP=8,MAXBLK=4`, one warp | IDs `[[0,1],[0,1]]`; sizes `[2,2,0,0,0]`; offsets `[0,2,4,4,4,4]`; every schedule row `[0,1,-1,-1]`; gather `[0,1,0,1]`; scatter `[0,2,1,3]`; unnormalized scale 0.5 gates exact | EXACT | `0 / 0 / 0` | PASS |
| `extreme-m4-k1-bf16-unnormalized` | `G=4,GP=4,E=8,EP=8,MAXBLK=4`, four warps | IDs `[[0],[1],[0],[7]]`; sizes `[2,1,0,0,0,0,0,1]`; every schedule row `[0,1,7,-1]`; gather/scatter `[0,2,1,3]`; grouped FP32 gates `[0.5,0.25,0.5,0.5]` | EXACT | `0 / 0 / 0` | PASS |
| `mixed-m4-k3-f32-unnormalized` | `G=12,GP=16,E=13,EP=16,MAXBLK=12`, four warps | IDs `[[1,2,4],[0,1,2],[0,1,2],[12,11,10]]`; sizes `[2,3,3,0,1,0,0,0,0,0,1,1,1]`; offsets `[0,2,5,8,8,9,9,9,9,9,9,10,11,12]`; each schedule row `[0,1,2,4,10,11,12,-1,-1,-1,-1,-1]`; scatter `[3,6,0,4,7,1,5,8,2,11,10,9]`; unnormalized scale 2.827 exact | EXACT | `0 / 0 / 0` | PASS |
| `max-routes-m16-k4-f32-normalized` | `G=64,GP=64,E=13,EP=16,MAXBLK=16`, four warps | sizes `[5,5,5,5,5,5,5,5,5,4,5,5,5]`; offsets terminate at 64; every schedule row is experts `[0..12]` followed by three `-1` pads; all 64 recovered IDs/weights and grouped outputs exact after normalization and scale 1.25 | EXACT | `0 / 0 / 0` | PASS |

Every case shape- and dtype-checked all seven fields before value comparison. Every integer output was exact `int32`; grouped gates were exact FP32, not merely within the frozen `5e-3/5e-3` tolerance. Reconstructing token-major selected routes from `slice_offsets` and `scatter_indices` reproduced the actual Q1 IDs exactly and the actual Q1 FP32 weights with zero maximum absolute error. All five block-size rows `(16,32,64,128,256)` were compared in every case.

Adversarial checks against the headline:

| Claim | Refutation test | Outcome |
|---|---|---|
| The comparator is the requested unfused implementation, not a rewritten host oracle | For every case, call completed `invoke_softmax_topk_route_gluon`, pass those exact tensors to completed `gluon_precomputed_topk_group_route`, and record both compiled parent kernel names | Survived |
| The owned child is the single fused dispatch | Match JIT template filename to `__file__`, require `_fused_softmax_group_route_small_m`, and require grid `(1,)` | Survived |
| IDs and weights do not cross a hidden global intermediate | Reject top-k pointers in the Python kernel signature, inspect all five TTGIR signatures, require exactly one `tt.load` from logits, and trace all 29 stores to seven final outputs | Survived |
| Exact ties retain Q1's smaller-expert ordering | Use a four-way FP16 maximum tie with `top_k=4`, then recover the selected order from Q4's grouped outputs | Survived with exact IDs `[0,1,2,3]` |
| Q1 normalization and scale were not replaced by the incompatible TokenSpeed fused helper | Exercise normalization both enabled and disabled and scales `1.75`, `0.5`, `2.827`, and `1.25`; compare against executable Q1 | Survived with zero gate error |
| Stable grouping and all five Q3 schedule rows survive fusion | Use deliberately repeated/interleaved experts through `G=64` and compare all seven fields to executable Q3 | Survived exactly |
| GP/EP padding cannot create a route or expert | Execute `G=12 -> GP=16` and `E=13 -> EP=16`, then require terminal count 12 and exact permutations/schedules | Survived |
| Both inherited Q1 warp layouts compile and execute | Run `M={1,2}` with one warp and `M={4,16}` with four warps across FP16/BF16/FP32 | Survived |
| Q5/Q6/GEMM ideas did not leak into Q4 | Scan the final device body for bias, sigmoid, matrix operations, atomics, and M=1-only branching | Survived |
| Unsupported work rejects before launch | Execute ten rank/dtype/shape/bound/device cases through the public wrapper | Survived, 10/10 |

Final-source cache and IR evidence:

- The Q4 cache contains exactly five fused specialization artifact sets: `OYQR6NE5XFEYCRT64HKTH2EXMF7JQLUVBBKR7KPP7YC2HUWETC3Q` (FP16 M=1, one warp, 128-byte shared), `MQI6ZFIPFMNN7UUMRDKCOTJ7Z2X6KBMC7Z2VQ7VWAC4PPPKLWAOA` (BF16 M=2, one warp, 128-byte shared), `2TMBBFIB4G23HYAM2X6ZXRNDKO2TSFNAJSW22BOJ25RLI2GP2K2Q` (BF16 M=4, four warps, 128-byte shared), `2LIPHB6HCRSUTF4UZDXSVORXB4MIL75YFREDS7YVJF43FVMRLM6A` (FP32 M=4, four warps, 128-byte shared), and `4RIEHDIKTQHG3BVPMBZXSWHU4YXEHGW2DQOAY3E7H4BJNQFHEZLA` (FP32 M=16/G=64, four warps, 256-byte shared).
- Every metadata record names `_fused_softmax_group_route_small_m`, targets HIP `gfx1250` with warp size 32, uses one CTA, and reports `global_scratch_size=0`; `num_warps` is exactly one or four according to `M`.
- Every TTGIR module identifies the owned Q4 source, declares `ttg.target="hip:gfx1250"` and one CTA, and has the runtime pointer signature `Logits` plus only `SliceSizes`, `SliceOffs`, `BlockOffs`, `BlockSched`, `GatherIndx`, `ScatterIndx`, and FP32 `GateScal`. There is no `TopkIds`/`TopkWeights` pointer. Each module has exactly one `tt.load`, from the logits tile, and 29 final stores after static unrolling; there are no TTGIR local load/store operations.
- The `G=64` TTGIR visibly contains FP32 `math.exp`, full-row reductions, smaller-ID `minsi` selection, selected-gate normalization and scale, SSA `tt.gather` of selected IDs/weights, `tt.histogram`, both Q3 scans, the `64x64` stable-rank match/reduce, final permutation/gate stores, and deterministic tail stores.
- LLVM defines `amdgpu_kernel void @_fused_softmax_group_route_small_m` with one read-only global pointer and exactly seven write-only global pointers. It also declares external address-space-3 `@global_smem` and emits LDS accesses/barriers for compiler-lowered reductions, layout conversion, histogram, and scans. This is real dynamic LDS, not a register-only claim.
- ISA declares `.amdgcn_target "amdgcn-amd-amdhsa-unknown-gfx1250"`, exports `_fused_softmax_group_route_small_m`, and emits `ds_*` plus barrier instructions. `.amdhsa_group_segment_fixed_size 0`, `.amdhsa_private_segment_fixed_size 0`, and `uses_flat_scratch=0` coexist with the 128/256-byte launch metadata because the shared allocation is dynamic; global scratch and private spill allocation remain zero.

## Accepted and rejected examples

- Accepted and executed tie/normalization boundary: `M=1,E=8,top_k=4,float16`, normalization enabled and scale 1.75. It chose experts `[0,1,2,3]`, produced four exact FP32 gates of `0.4375`, and emitted exact grouped metadata in the general fused child.
- Accepted and executed padding/scale boundary: `M=4,E=13,top_k=3,float32`, normalization disabled and scale 2.827. It exercised both padded axes, retained 12 valid routes, and matched all seven actual-composition fields exactly.
- Accepted and executed public route-count boundary: `M=16,E=13,top_k=4,float32`, normalization enabled, exactly 64 routes. Every final field and every reconstructed Q1 intermediate was exact.
- Rejected and executed through the public wrapper before any Q4 launch:
  - rank-1 logits: `router_logits must be rank 2`.
  - float64 logits: `router_logits dtype must be float16, bfloat16, or float32`.
  - `M=0` and `M=17`: `M must satisfy 1 <= M <= 16`.
  - `top_k=0` and `top_k>E`: `top_k must satisfy 1 <= top_k <= E`.
  - `E=0` and `E=1025`: `E must satisfy 1 <= E <= 1024`.
  - `M*top_k=80`: `M * top_k must be at most 64`.
  - CPU logits: `router_logits must be a CUDA tensor`.
- Deliberately outside Q4: non-finite-logit semantics, correction bias/group-limited selection, an M=1-only route shortcut, duplicate or externally supplied top-k routes, grouped GEMM, timing, and physical B0 validation.

## Observed

- The exact Q1 and Q3 sources/ledgers and TokenSpeed fused helper, device body, wrapper, bounds, and derived-warp policy were inspected at their pinned states.
- The TokenSpeed fused helper's selected-logit/input-dtype gate boundary differs from Q1's full-row-FP32/optional-normalization/unconditional-scale contract, so only its boundary-removal structure is a valid Q4 provenance idea.
- Local Triton is still detached at `HEAD=80e223f93d59359161f5482fbb69bbfab29c0a0b`, and a final independent live lookup of `origin refs/pull/154/head` returned the same SHA. AITER remains `4a1cc773f34cbfc74387259e51262556ee38edd0`, TokenSpeed remains `3e725ac2b785b71f27ff9e9ac3796349c495d225`, and the plan repository remains `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf`.
- Final parent hashes remain `e4c7153744ae02931724a6592da47618490458ae19a0b402df0660e0d1b74cbe` for Q1 and `33d1afba612af921df7ec8fe5dbab31c8a43423d1df4f665660aa9b0095ee791` for Q3. The final Q4 child SHA-256 is `d26b283876cc09cfe4dfd71366601f62d65419e2ab2ee87182f616ebc3a1db8d`.
- Final-source serialized smoke and full suites both ran under gfx1250 FFM with `TRITON_ALWAYS_COMPILE=1`, exact cache `/tmp/a8w4-decode-ablation/Q4`, and `flock /data/lock/amd-gpu.lock`.
- Smoke passed 1/1, full passed 5/5, and the public rejection matrix passed 10/10 in each invocation. Every grouped field and reconstructed parent intermediate was exact with zero recorded gate error.
- Exact dispatch, TTGIR, LLVM, ISA, dynamic-LDS, and zero-global-scratch evidence is recorded above. The artifact audit passed all five specialization sets.
- Original target hashes remain `33a065856b1b3cc67b4f6d0a7c9777a1c19ef67e745071ebc5caeb654698fcf4` for `moe_decode_gfx1250.py`, `391788b2c47cd118e566000b7e5da7e264d2dddb05e6054f1b84fdd4c05c313a` for `moe_common_gfx1250.py`, and `b825608f89a704fe979533a4badd6483e96cd79568e28b5c83cdf903ee922887` for `moe_gfx1250.py`.
- Q4 edited only the owned child and this ledger. It did not edit either parent, the registry, shared investigation ledger, runner, original target sources, or another cell. It performed no build, tuning, timing, Git stage, commit, amend, stash, reset, or push.

## Inferred

- Q1's unique valid top-k IDs satisfy Q3's single-block-per-expert invariant under `M<=16`; the terminal count `G` observed in every finite-logit case is consistent with that composition boundary.
- The owned-source signature, executable parent composition, exact output matrix, and final IR jointly support the headline that Q4 removes the global Q1→Q3 ID/weight boundary without changing the tested Q1 selection/numerics or Q3 ordering/metadata behavior.
- Dynamic LDS is compiler-managed implementation traffic for collectives and layout conversion, not a reintroduced global route tensor. The evidence does not imply the fused kernel is faster.

## Unknown

- Supported configurations inside the declared bounds beyond the five-case matrix, non-finite input logits, physical B0 behavior, and downstream integration remain untested.
- Whether eliminating two global route tensors and one launch offsets the fused program's larger live state and dynamic-LDS collective cost is unknown.
- Performance is intentionally unknown because FFM has no meaningful timing and Q4 performed no timing.

## Alternatives Considered

- Copying TokenSpeed `_fused_topk` verbatim was rejected because its selected-logit softmax, router-dtype cast, and absent normalization/scale controls would not equal Q1→Q3.
- Calling Q1 and Q3 from the Q4 public function was rejected as the implementation because it would preserve the materialized boundary and two launches; those calls are used only as the required executable comparator.
- Adding Q5 correction-bias/group selection or Q6's M=1 shortcut was rejected because each is a separately owned idea.

## Remaining blocker or next dependency

No Q4 correctness or IR blocker remains. Downstream cells may consume the proved fused grouped-route ABI under their own contracts; Q5 still owns correction-bias/group-limited selection and Q6 still owns any M=1 shortcut. No build, tuning, timing, Git write, or shared-file edit was performed.
