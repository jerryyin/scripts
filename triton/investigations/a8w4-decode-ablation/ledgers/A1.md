STATUS: done
CONCLUSION: All A1 gates pass on the final current-filesystem snapshot: the selected launch corpus is completely dispositioned, the registry/source/ledger graph is bijective, all 25 cells have grounded correctness evidence and one-change boundaries, protected sources are unchanged, and no FFM performance claim is made.

# A1 — independent round-1 corpus and evidence audit

## Frozen experiment

- This is an audit-only cell. Its only owned file is this ledger. A1 did not
  edit a kernel, shared plan/coverage/registry state, another ledger, or Git
  state.
- Required implementation cells:
  `H0,G2,G3,G4,G5,G6,G7,G8,Q1,Q2,Q3,Q4,Q5,Q6,R1,R2,R3,R4,R5,R6,R7,R8,R9,I1,I2`.
- Required gates were repository pins and the live PR ref; selected-corpus
  launch closure and dispositions; registry/source/ledger bijection; complete
  ledger schema and evidence; one-idea scope, negative space, and tuning
  boundaries; protected/parent/current-child hashes; absence of FFM-derived
  performance claims; and exact worktree state.
- Requested and coordinator-confirmed execution profile: `gpt-5.6-sol`,
  reasoning effort `ultra`.
- Final source state, independently re-read before this conclusion:
  - Triton local detached `HEAD`:
    `80e223f93d59359161f5482fbb69bbfab29c0a0b`.
  - Live `origin refs/pull/154/head`:
    `80e223f93d59359161f5482fbb69bbfab29c0a0b`.
  - AITER `main`:
    `4a1cc773f34cbfc74387259e51262556ee38edd0`.
  - TokenSpeed `main`:
    `3e725ac2b785b71f27ff9e9ac3796349c495d225`.
  - Scripts branch `users/jerryyin/a8w4-decode-ablation-plan`:
    `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf`.
- Final shared-document SHA-256 values used by this audit:
  - `PLAN.md`:
    `2c511ea3a8836ba602454b18aeec3ff81407a8c2536091e9edcdf5ba9895adf7`.
  - `KERNEL_COVERAGE.md`:
    `940395c0e429023f77d374370ccb7426596f1bbf6385419e072b367d70f5710b`.
  - `REGISTRY.md`:
    `126c7ecadd6563d42e5631aca927c255f534dfafc486de448a9f6a1608b86530`.
  - `investigation-ledger.md`:
    `04849468cb57cbb1f6f9d23ec8c5515971cec7388bd6139a0544ac8c49129e72`.
- FFM is correctness-only. A1 performed no timing, ranking, compiler build, or
  new FFM run.

## Parent → child ownership change

- A1 has no source parent or child. Before A1, 25 implementation rows and their
  evidence existed, while the final independent coverage/evidence conclusion
  was intentionally unresolved.
- The audit initially rejected broad or inaccurate wrapper dispositions and a
  nonexistent R9 donor path. The coordinator corrected those shared or owned
  files outside A1's ownership. A1 then discarded its earlier scan and repeated
  every gate against the exact final hashes above.
- After A1, the round has an independently checked correctness corpus. This
  changes no kernel, numerical boundary, dispatch policy, or tuning choice and
  does not select a winner.

## Source diff classification

### Corpus and launch closure

- The structural corpus in `PLAN.md` contains 20 absolute Python files. All
  20 exist and all 20 are explicitly linked and dispositioned in
  `KERNEL_COVERAGE.md`.
- `opt_flags.py` is separately cited as configuration-policy evidence; it has
  no launchable decode entry and therefore is not one of the 20 structural
  kernel/wrapper files.
- The only launch-body file outside those 20 needed to close a selected wrapper
  is AITER
  `_triton_kernels/moe/moe_routing/routing.py`. The selected public routing
  wrapper imports and launches its `_combined_routing` and
  `_combined_routing_fused` bodies, and coverage names both.
- A read-only AST scan found 33 static `kernel[grid](...)` launch sites and
  31 unique lexical targets in the selected files. Resolving AITER's four
  imported launch aliases and Triton's dynamic `_matmul` versus
  `_matmul_decode` selector left zero uncovered targets.
- The final manifest has 47 inventory rows: seven Triton/AITER compute,
  13 TokenSpeed compute, 14 routing/combine, and 13 support rows. Its 57
  absolute Python anchors all exist and are in bounds. Every disposition uses
  a valid active, performance-only, rejected, or support row ID.
- All five registered TokenSpeed `mxfp4.py` wrappers have path-accurate final
  dispositions. In particular:
  - A16W4 dispatch composes `R1,R2,R3,R5` and leaves only its family crossover
    in `P4`.
  - The gfx1250 precomputed wrapper maps to grouped compute, precomputed
    grouping, and its static-FP8 boundary.
  - The gfx950 dynamic, precomputed, and kernel-routing wrappers distinguish
    their reachable direct/grouped families rather than claiming every row.
- Q6 is recorded honestly: its donor kernel/wrapper is launchable, but its sole
  caller branch is unreachable at the pinned revision because the containing
  helper requires `M>=4` while that branch requires `M==1`. Q6 tests the
  standalone structural idea; neither the plan nor coverage calls it
  live-selected.

The canonical selected-corpus manifest is the SHA-256 of sorted lines
`<absolute-path> <file-sha256>\n` for those 20 files:
`abc3726fefd80d38342c0a082909e7bd6e1fe17de031bc74590106b74aacc569`.
The AITER routing-body closure hashes to
`696c520e13fb025c0bca8c73815210930785b229de4ecac7a66cbd6ac0abf55a`.

### Registry/source/ledger bijection

- Registry rows: 26 total = 25 implementation cells + A1.
- Owned Python files: 27 total = 25 distinct `k*.py` implementation modules +
  H0's `run_ffm.py` and `__init__.py`.
- Every implementation module parses, contains
  `ABLATION_ID/PARENT/IDEA/PROVENANCE`, and has the unique ID assigned by its
  registry row. Missing, extra, duplicate, or cross-owned modules: zero.
- Ledgers: 26 total, exactly one per registry row. At the audited registry
  snapshot, every implementation row is `done` and A1's coordinator-owned row
  is `in-progress`. This ledger changes its own first line only after the
  completed body is read back.
- Every registry parent edge resolves to a current source. All 26 concrete
  accepted-parent references found in 21 child rows have their current digest
  in the child ledger.
- All 25 implementation ledgers have `STATUS: done`, a line-2 conclusion, and
  all ten required sections, including `Alternatives Considered`. No
  implementation ledger contains a stale status, TODO/TBD/FIXME, or awaiting
  marker.
- All 51 absolute `.py:<line-range>` provenance references in the
  implementation ledgers exist and are in bounds. Relative donor references
  were also reconciled to the pinned repositories. R9 now names the real
  `ops/moe/fused_mxfp_gfx950.py:10121-10245` donor.

### One-change and negative-space audit

- Every parent→child edge has one named mechanism and separates essential
  device changes from standalone-harness, validation, and diagnostic support.
  Every exposed tuning axis has multiple tested values or a cited and tested
  intrinsic restriction.
- G4 is correctly retained only as a direct packed-schedule source formulation:
  parent and child TTGIR already have the same load/mask/shift/load/multiply
  anchor. No generated-code novelty or speed claim survives.
- R6 and R7 do not relabel a CDNA4 donor primitive as gfx1250 support. They
  mechanically adapt it to the target's legal
  `gl.amd.gfx1250.wmma_scaled`, test both legal N/warp layouts, and reject
  nonnative instruction-K values.
- R8 proves source-order correctness and bitwise R7 equivalence. Its ledger
  prominently records that K=512 TTGIR hoists all 12 route operand loads before
  the first WMMA, so pairwise native overlap is unproved.
- R9 is a deliberately narrow one-wave fusion. `BLOCK_N=32/NUM_WARPS=2`,
  `E>32`, unnormalized gates, and oversized route domains reject rather than
  importing a second cross-wave mechanism.
- I1 and I2 are honest two-launch integration rows. I1 owns a BF16-input W2
  adaptation after accepted R3; I2 invokes the exact accepted R4 and R5 public
  paths through a repeated scalar scale. Neither is described as kernel fusion.

## FFM cases and evidence

- The full matrices contain 233 passing accepted cases:

  | Cells | Full accepted cases |
  |---|---:|
  | H0 and G2–G6 | 150 |
  | G7–G8 | 10 |
  | Q1–Q6 | 29 |
  | R1–R9 | 36 |
  | I1–I2 | 8 |
  | **Total** | **233** |

- Every cell has a smoke, full matrix, and independent repeat, for a minimum of
  283 successful accepted invocations before comparator/control and rejection
  runs are counted. Nineteen cells record the repeat in their own ledger;
  Q1–Q6 record the coordinator's independent repeat in the shared investigation
  ledger.
- Every cell also has at least one concrete adversarial or rejected boundary.
  Those checks cover dispatch identity, invalid routing/domain values, intrinsic
  tile/warp/K restrictions, tails, dtype/shape/device contracts, source-order
  or intermediate-boundary comparisons, and configuration-dependent metadata.
- All executed evidence uses gfx1250 FFM, `TRITON_ALWAYS_COMPILE=1`, a
  cell-unique cache, and `flock /data/lock/amd-gpu.lock`. Each cell proves the
  owned child dispatches and records output shape/dtype, finite behavior,
  numerical or bitwise metrics, and PASS.
- R9's provenance-string correction changed its source hash. Its corrected
  final source
  `c8c9185cb16e2432998069e249a467dc54123d51a98c87f6e42f2a5ad657cd85`
  was therefore rerun: smoke `1/1`, full `4/4`, repeated smoke `1/1`, all PASS.
- No other current implementation source differs from the hash underlying its
  recorded final evidence. A1 therefore did not spend a new FFM run merely to
  duplicate already-grounded, hash-matched results.
- A search for speedup, faster/fastest, win, latency, throughput, ranking, and
  performance-advantage language found no positive FFM-derived claim. Hits are
  explicit negations/unknowns, routing tie-winner terminology, or donor policy
  described as provenance. FFM contributes no timing or ranking conclusion.

## Accepted and rejected examples

- Accepted: H0 plus its descendants preserve all declared grouped configuration
  axes, while illegal gather + `BLOCK_K=128` remains an explicit boundary
  rather than a silently changed baseline.
- Accepted: every selected launch target, registered wrapper, routing body, and
  support source now has an exact disposition. Broad family labels that named
  unreachable mechanisms were rejected and corrected before the final scan.
- Accepted with a narrow claim: G4 is correct source structure, R8 is correct
  source scheduling, and Q6 is a correct standalone specialization.
- Rejected claims: G4 generated-code improvement; R8 surviving pairwise native
  overlap; Q6 live dispatch at the pinned revision; R6/R7 native CDNA4 MFMA;
  R9 two-wave support; and fusion for either I1 or I2.
- Accepted: all three protected target files match both their frozen hashes and
  the corresponding `HEAD` blobs. Any source change there would have blocked
  completion.
- Rejected: treating FFM case duration, opcode counts, missing buffers, or
  fewer launches as performance evidence.

## Observed

### Exact implementation snapshot

| Cell | Owned implementation source SHA-256 |
|---|---|
| H0 | `0238de7f6a8d0ccc3dcee20a6de52edccfc60f74ca9413d44a9c93e5106ac4d9` |
| G2 | `fe27527c6848a7aacb1c0930689eee900ef05ee4f8d29750b949df910c7b4ec2` |
| G3 | `b75524e6f774b9de57712309af97080f4c557a114cab19a3ba333104e85d8663` |
| G4 | `62360b03ea2a693d7d10d8eeb6c1814e28bbfe13653c4a618298591cb9cef2bd` |
| G5 | `6ab3a4f05e87a0b228dbf0a4fb42327d0fcba56cd22ea8acbe4182e1e90cf010` |
| G6 | `4f80c32e406c9bae44fb3f528cf8db2fc64d24c81d23069a22c9aab6c7c962e5` |
| G7 | `0752b2886311423eb0efa2ac4f91cf9587a2514f12f5a0293a4c03a3d43ddb48` |
| G8 | `c569bdd28dc625861abc13933d7a36258e38ca5f5308384ed7a50f5ed47accd9` |
| Q1 | `e4c7153744ae02931724a6592da47618490458ae19a0b402df0660e0d1b74cbe` |
| Q2 | `70631a9a346e35931879a3c3781be6a47c113a9e5f57be952602951968d74d9f` |
| Q3 | `33d1afba612af921df7ec8fe5dbab31c8a43423d1df4f665660aa9b0095ee791` |
| Q4 | `d26b283876cc09cfe4dfd71366601f62d65419e2ab2ee87182f616ebc3a1db8d` |
| Q5 | `14682fd6de3c8213fca67879f2d66b010743a973ff95474937fb7f7582084eab` |
| Q6 | `e047c933801349abfc1cdccd5d2e72aaa0dc7e9f665961c5368f8f568386e4df` |
| R1 | `86504a9a93c8d83d16b9005ce869cca654993a2b5dbbc9fc041962c11bb7c500` |
| R2 | `805f11c75b13f6162d846a6690c06e785d8d9ea195a9ba8688d713672b97a733` |
| R3 | `9b7abeb53c8ce2efbc76e00ff9178ab439829c2083c50ebc33cb6541851919a2` |
| R4 | `57911e070704d7ac69e91434f0a38782b031a76ab8063b78ff07b4a3824ed7b9` |
| R5 | `cea3270c70a7f5b020fb1b8f4b9f18264a00ba2832d2f979616088d7204fcc93` |
| R6 | `ef5cab65373c415fc7a413c38d59e8e2c42c8d35aa52e3059dad03f9b0e4594d` |
| R7 | `db83de042478ed1f815f70ec99357e259755c1d2f7a164ca9f74f46971bf9260` |
| R8 | `3d0d0b26917b02b5ce984e47e1908dece9feb10448fffddfc4a2ecfd61498f65` |
| R9 | `c8c9185cb16e2432998069e249a467dc54123d51a98c87f6e42f2a5ad657cd85` |
| I1 | `a9e1ee9f8f93819f5c83756b428f55f96f43a1be1e8762a0c211c7b1d82b1f3d` |
| I2 | `164c25c849d9eccb9d90aeacd1f7d733faa8cba660a316f59280498d9bedd807` |

Using registry order and canonical lines
`<cell> <filename> <sha256>\n`, the 25-source manifest hashes to
`d047dc981fc91a24475eddca3b91e15e57ef5b53a73002f95536df9e94bb5cd1`;
the corresponding 25-ledger manifest hashes to
`32810243b7cd30865edc54d1984f69f94a0791c6dc87ff869f124104bfba9d26`.
Using sorted filename order for all 27 owned Python files, including H0
support, yields
`8c07026fe8204a4f8c6a1325256b65316a195af0c1e7e375bb9fdc99a5028da1`.

H0 support hashes are:

- `run_ffm.py`:
  `97a85046baa1e7d0f4566a8d2eeeed5046680e3003876162b9a5021e20e31245`.
- `__init__.py`:
  `6c7ce7d0ebd3a8b9ca7b48c016c3cf65c447edc035a8286c9ee8cf47ae06463a`.

H0's individual ledger records its AST closure digest rather than its whole
owned-source digest; A1 records the whole-file digest above, and every concrete
H0 child ledger pins that same value. Q1–Q6 keep coordinator-repeat evidence
in the shared investigation ledger rather than duplicating it in each cell.
Neither documentary placement is a correctness or provenance gap.

### Protected originals

- `moe_decode_gfx1250.py`:
  `33a065856b1b3cc67b4f6d0a7c9777a1c19ef67e745071ebc5caeb654698fcf4`.
- `moe_common_gfx1250.py`:
  `391788b2c47cd118e566000b7e5da7e264d2dddb05e6054f1b84fdd4c05c313a`.
- `moe_gfx1250.py`:
  `b825608f89a704fe979533a4badd6483e96cd79568e28b5c83cdf903ee922887`.

Live bytes, `HEAD` blobs, frozen ledger values, and `run_ffm.py` constants all
agree. `git diff --quiet HEAD -- <protected-file>` succeeds for all three.

### Worktree state

- AITER and TokenSpeed are clean.
- Triton has no tracked or staged diff. Its only untracked state is the declared
  27-file ablation directory plus pre-existing `hsakmt_counters.csv`.
- Scripts has tracked modifications to `PLAN.md`, `KERNEL_COVERAGE.md`, and
  `REGISTRY.md`; untracked `investigation-ledger.md` and 26 ledger files; and
  zero staged files.
- `git diff --check` passes for the scripts tracked diff.
- A1 performed no stage, commit, amend, push, stash, reset, build, timing run,
  or source edit.

## Inferred

- Within the plan's explicitly bounded corpus, round 1 is complete for
  functional correctness and independent source attribution.
- The evidence supports taking these exact hashed variants into a later,
  separately authorized hardware/performance phase. It does not rank them or
  imply that every retained mechanism is worthwhile.
- The closed launch graph and exact wrapper dispositions make an omitted
  selected decode mechanism unlikely at the pinned revisions; that inference
  does not extend beyond the declared architecture/precision/source boundary.
- The registry/source/ledger graph is reproducible from current files without
  relying on a prior conversational recap.

## Unknown

- Physical B0 behavior, timing, occupancy, register pressure, cache traffic,
  and all crossovers remain unknown.
- Exhaustive Cartesian tuning combinations, shapes outside each frozen matrix,
  future compiler/source revisions, and MoE kernels outside the declared
  selected corpus remain unaudited by design.
- R8 native issue overlap remains unknown despite correct source order.
- Q6 has no live caller at the pinned TokenSpeed revision.
- A1 does not establish end-to-end production integration beyond the explicit
  two-launch I1/I2 correctness paths.

## Alternatives Considered

- Rerunning all 25 FFM matrices was rejected because the final source hashes
  match the evidence-bearing hashes and the only post-evidence source change,
  R9's provenance correction, already received a fresh smoke/full/repeat run.
- Accepting broad wrapper family labels was rejected. Each registered wrapper
  was followed through its reachable branches, and unreachable mechanisms were
  removed before the final scan.
- Treating Q6's dead caller branch as live dispatch was rejected; the
  standalone launchable source remains useful provenance without that claim.
- Treating G4 source simplification or R8 source lookahead as a performance
  result was rejected by generated-IR evidence.
- Editing shared registry/coverage/claim state from A1 was rejected because the
  audit owns only this ledger. The coordinator may merge A1's result afterward.
- Timing or choosing a combined “best” kernel was rejected because it belongs
  to a separately authorized performance round.

## Remaining blocker or next dependency

No A1 corpus, correctness-evidence, provenance, one-change, tuning, hash, or
performance-claim blocker remains. The coordinator may merge this audit result
into shared status. Any timing, ranking, production integration, or combined
kernel work is a separate next phase and is not authorized by A1.
