STATUS: done
CONCLUSION: The gfx1250 one-workgroup sigmoid+bias router dispatched the owned child and passed 4/4 required FFM cases with exact stable IDs and zero recorded gate error across the donor's bias-only selection, router-dtype rounding, normalization, and scale boundary.

# Q2 — one-workgroup sigmoid + correction-bias top-k router

## Frozen experiment

- Triton source: `/root/triton-mi450` at `80e223f93d59359161f5482fbb69bbfab29c0a0b`. Before this ledger was created, local `HEAD` and live `origin refs/pull/154/head` both independently resolved to that SHA.
- AITER source: `/root/aiter` at `4a1cc773f34cbfc74387259e51262556ee38edd0`.
- TokenSpeed provenance source: `/root/tokenspeed` at `3e725ac2b785b71f27ff9e9ac3796349c495d225`.
- Plan source: `/root/scripts` at `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf` on `users/jerryyin/a8w4-decode-ablation-plan`.
- Requested and coordinator-confirmed launch profile: `gpt-5.6-sol`, reasoning effort `ultra`, `fork_turns=none`.
- Semantic parent: TokenSpeed `_sigmoid_bias_topk_route_gluon_kernel` and `invoke_sigmoid_bias_topk_route_gluon` in `/root/tokenspeed/tokenspeed-kernel-amd/python/tokenspeed_kernel_amd/ops/moe/gluon_a4w4_gfx950/decode_kernels.py:219-300,303-307,368-418`.
- Executable donor reference and tolerance: `/root/tokenspeed/tokenspeed-kernel-amd/test/ops/test_mxfp4_clean_warp_decode_gfx950.py:340-359,502-578`.
- Owned child: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k19_sigmoid_bias_topk_router.py`.
- Owned ledger: this file.
- One allowed idea: compute sigmoid scores, correction-biased stable top-k selection, and un-biased routing gates under the donor's explicit router-dtype boundary in exactly one bounded decode workgroup. Provenance: TokenSpeed `decode_kernels.py:219-300,303-307,368-418` at the pinned SHA.
- Explicit Q2 scope boundary: Q2 does not compute rowwise softmax, does not make correction bias optional, and does not produce expert histograms, prefix offsets, schedules, gather/scatter indices, or grouped gates. Q1 owns softmax routing; Q3 owns grouped metadata materialization.
- Numerical and ordering contract: load rank-2 `float16`, `bfloat16`, or `float32` logits at their stored dtype and cast to FP32; compute sigmoid in FP32; round every sigmoid score to the input router dtype before bias addition, selection, and gate extraction; add correction bias in FP32 only for selection; select biased choices in descending order, resolving exact ties to the smaller expert ID; emit the selected un-biased, router-dtype-rounded sigmoid gates as FP32. When normalization is enabled, re-round the selected gates to the router dtype, sum and normalize with the donor's zero-denominator guard, and multiply by `routed_scaling_factor`. When normalization is disabled, preserve the donor's exact asymmetric behavior: emit the unnormalized un-biased rounded gates and do not apply `routed_scaling_factor`. IDs must match exactly. Weights use the donor test's `atol=5e-3`, `rtol=5e-3` boundary.
- Accepted shape/dtype bounds inherited from the donor: rank 2; router dtype in `{float16,bfloat16,float32}`; `0 < top_k <= E <= 1024`; `M * top_k <= 128`; correction bias is rank 1 with exactly `E` elements.
- Configuration and intrinsic restrictions: the grid is intrinsically fixed to one workgroup. `MP`, `EP`, and `TKP` are derived next powers of two, not tuning axes. `NUM_WARPS` is derived as 1 for `M<=2` and 4 otherwise; required cases exercise both values through `M={1,4}`. The gfx950 donor distributes an expert row over 64 lanes; the only architecture adaptation is a 32-lane expert dimension for gfx1250.
- Correctness values: `M={1,4}`, `top_k={1,4}`, `E={8,13}`, router dtypes `{float16,bfloat16,float32}`, normalization `{False,True}`, and routing scales `{0.5,1.75,2.827}`. These are semantic/input axes, not performance tuning.
- Required full FFM cases:
  - `bias-flip-m1-k1-f32`: `M=1,E=8,top_k=1`, ordinary FP32 logits, correction bias changes the winner from expert 0 to expert 1, normalization disabled, scale 2.827; expected gate is expert 1's un-biased sigmoid score and the scale is intentionally ignored by the source contract.
  - `tied-m1-k4-f16`: `M=1,E=8,top_k=4`, exact tied FP16 logits and zero bias, normalization enabled, scale 1.75; expected IDs begin `[0,1,2,3]`.
  - `extreme-m4-k1-f32`: `M=4,E=8,top_k=1`, FP32 logits containing `-1000` and `1000`, normalization enabled, scale 0.5; this exercises finite sigmoid saturation and the four-warp specialization.
  - `rounding-m4-k4-bf16`: `M=4,E=13,top_k=4`, BF16 logits with score pairs that differ in FP32 but tie after BF16 sigmoid rounding, non-power-of-two expert padding, normalization disabled, scale 2.827; selection and gates must follow the rounded values and the scale is intentionally ignored.
- The smoke suite is `bias-flip-m1-k1-f32`, because it simultaneously proves child compilation/dispatch, correction-bias-only selection, un-biased gate emission, unnormalized scaling semantics, and the one-wave specialization.
- The owned module uses a source-local test entry because the unchanged H0 runner intentionally requires the grouped `_matmul_decode` ABI and its 25 grouped-GEMM cases. PLAN permits contract-specific reference code inside the owned variant; invoking H0's grouped ABI would not dispatch or test Q2.
- Exact smoke command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/Q2 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k19_sigmoid_bias_topk_router.py --suite smoke
```

- Exact full command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/Q2 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k19_sigmoid_bias_topk_router.py --suite full
```

## Parent → child ownership change

- Before Q2, H0 contains only the grouped A8W4 GEMM and consumes routing metadata produced outside its owned source. It owns no biased router program.
- After Q2, grid program 0 owns the complete bounded `M x E` logits matrix and correction-bias vector, performs every row sigmoid, dtype round, selection, and optional normalization in registers, and stores the complete `M x top_k` ID/gate outputs. No second program or launch participates.
- Expert grouping remains absent: Q2 emits direct-route expert IDs and gates, not histograms, padded rows, block schedules, or gather/scatter metadata.
- The math primitive changes from no owned router to FP32 sigmoid plus iterative max/min reductions; it introduces no softmax, GEMM, MFMA, or WMMA.
- Global traffic is one masked logits load, one masked correction-bias load, and one ID plus one gate store per selected route. Selection state remains in registers. The source contains no LDS/global-intermediate operation, and all four compiled metadata records report `shared=0` and `global_scratch_size=0`.
- The numerical boundary is input dtype to FP32 sigmoid to router-dtype score rounding, FP32 correction-bias addition for choice only, router-dtype gate rounding before optional normalization, and FP32 output stores.

## Source diff classification

The complete device-body diff against the exact TokenSpeed donor is:

```diff
--- TokenSpeed decode_kernels.py:218-300
+++ Q2 k19_sigmoid_bias_topk_router.py:73-155
@@
-    lt: gl.constexpr = gl.BlockedLayout([1, 1], [1, 64], [NUM_WARPS, 1], [1, 0])
+    lt: gl.constexpr = gl.BlockedLayout([1, 1], [1, 32], [NUM_WARPS, 1], [1, 0])
```

- Essential architecture port: replace the gfx950 donor's 64-lane expert distribution with the gfx1250 32-lane expert distribution. Every other device-body line is source-identical, including sigmoid rounding, FP32 bias addition, stable tie selection, un-biased gate extraction, the normalization-only scale branch, and output stores.
- Mechanical self-contained launch support: replace TokenSpeed package imports with the pinned target's `triton.experimental.gluon` imports; add the four `ABLATION_*` constants; preserve the donor dtype map, bounds, one-workgroup grid, next-power-of-two padding, and derived-warp policy; return compiled-name/grid metadata for dispatch proof; turn the donor's boolean support predicate into fail-fast `ValueError` diagnostics; keep the public two-tensor return contract.
- Mechanical verification support inside the owned module: add the stable host reference, four frozen routing cases, eight pre-launch rejection cases, explicit bias-flip and unrounded-score adversaries, exact dtype/shape/finite/error checks, structured dispatch/environment/case evidence, CLI suite selection, and `os._exit` FFM teardown. None participates in the device kernel or changes routing semantics.
- Scope scan found no softmax, Q1 import, histogram, prefix, schedule, Q3 import, or grouped-metadata construction. The only `gather`/`scatter` occurrences are host-reference tensor indexing.

## FFM cases and evidence

The final-source smoke command exited 0 and reported:

```text
DISPATCH_PROOF {"ablation_id":"Q2","compiled_kernel":"_sigmoid_bias_topk_route_gluon_kernel","grid":[1],"one_workgroup":true,"selected_child":".../k19_sigmoid_bias_topk_router.py","template_file":".../k19_sigmoid_bias_topk_router.py","result":"PASS"}
CASE_RESULT {"case":"bias-flip-m1-k1-f32","ids":[[1]],"unbiased_ids":[[0]],"weights":[[0.5]],"normalize_topk_weights":false,"routed_scaling_factor":2.827,"finite_fraction":1.0,"max_absolute_error":0.0,"max_relative_error":0.0,"rms_relative_error":0.0,"num_warps":1,"assert_close":"PASS"}
SUITE_RESULT {"ablation_id":"Q2","cases_passed":1,"cases_total":1,"result":"PASS","suite":"smoke"}
```

The final-source full command then exited 0 and reported `SUITE_RESULT {"ablation_id":"Q2","cases_passed":4,"cases_total":4,"result":"PASS","suite":"full"}`. IDs were checked with exact equality, and weights used the frozen donor `atol=5e-3,rtol=5e-3` without relaxation.

| Case | Input contract | IDs | Output shapes and dtypes | Finite | Max abs / max rel / RMS rel | Result |
|---|---|---|---|---:|---|---|
| `bias-flip-m1-k1-f32` | `M=1,E=8,top_k=1`, FP32, unnormalized, scale 2.827, 1 warp | `[[1]]`; unbiased top-1 was `[[0]]` | IDs `[1,1]` int32; gates `[1,1]` float32 | 1.0 | `0 / 0 / 0` | PASS |
| `tied-m1-k4-f16` | `M=1,E=8,top_k=4`, FP16 exact ties, normalize, scale 1.75, 1 warp | `[[0,1,2,3]]` | IDs `[1,4]` int32; gates `[1,4]` float32 | 1.0 | `0 / 0 / 0` | PASS |
| `extreme-m4-k1-f32` | `M=4,E=8,top_k=1`, FP32 `-1000/1000`, normalize, scale 0.5, 4 warps | `[[0],[1],[0],[7]]` | IDs `[4,1]` int32; gates `[4,1]` float32 | 1.0 | `0 / 0 / 0` | PASS |
| `rounding-m4-k4-bf16` | `M=4,E=13,top_k=4`, BF16 rounding, unnormalized, scale 2.827, 4 warps | row 0 `[3,4,0,1]`; unrounded FP32 was `[3,4,1,0]` | IDs `[4,4]` int32; gates `[4,4]` float32 | 1.0 | `0 / 0 / 0` | PASS |

The exact source boundary is directly visible in results:

- Bias-only selection: the bias-flip case changed top-1 from expert 0 to expert 1, but emitted expert 1's un-biased sigmoid gate `0.5`, not its biased choice score `1.0`.
- Stable ties: the FP16 tied case returned `[0,1,2,3]`, proving smaller expert ID wins each exact tie.
- Normalization and scale: the tied case returned four `0.4375` gates, whose sum is 1.75; the extreme top-k1 case returned `0.5`, matching its normalized scale.
- Unnormalized source asymmetry: the bias-flip gate remained `0.5` and the BF16 case retained raw rounded sigmoid gates even though both passed scale 2.827, proving the donor ignores routing scale when normalization is disabled.
- Router-dtype boundary: BF16 rounded `sigmoid(0.0)` and `sigmoid(0.001)` to the same score, so row 0 selected smaller ID 0 before ID 1; an otherwise identical unrounded-FP32 host selection ordered ID 1 before ID 0.
- Padding: the BF16 `E=13` case compiled with `EP=16` and returned no ID outside `[0,12]`.

The unique cache contains four `_sigmoid_bias_topk_route_gluon_kernel` HSACOs. Each metadata record identifies gfx1250, warp size 32, one CTA, no shared memory, and no global scratch; the records cover `num_warps={1,4}`. This corroborates the source-level register-resident/no-intermediate mechanism and is not performance evidence.

Adversarial checks against the headline:

| Claim | Refutation test | Outcome |
|---|---|---|
| The owned child is dispatched exactly once | Compare JIT template filename with `__file__`, require the compiled child symbol, and record hardcoded grid `(1,)` | Survived |
| Bias affects choice but not emitted gate | Compare biased top-1 against an un-biased top-1 control and require gate `0.5` rather than biased choice `1.0` | Survived |
| Exact ties choose smaller expert IDs | Four-way FP16 choice tie and BF16 post-rounding tie | Survived |
| Router-dtype rounding precedes choice | Compare BF16 route IDs against an otherwise identical unrounded-FP32 choice | Survived; `[3,4,0,1]` versus `[3,4,1,0]` |
| Scale follows the donor's normalization branch | Normalized scales 1.75/0.5 and unnormalized scale 2.827 | Survived |
| EP padding cannot select masked experts | Compile/run `E=13` with `EP=16` and require every ID `<13` | Survived |
| Both derived warp layouts are correct | Execute `M=1` with one warp and `M=4` with four warps across all three router dtypes | Survived |
| The bounded wrapper rejects unsupported work rather than masking it | Eight invalid rank/dtype/top-k/E/route-slot/bias-shape examples through the public wrapper | Survived |

## Accepted and rejected examples

- Accepted and executed: `M=1,E=8,top_k=1,float32`, unnormalized, scale 2.827. Bias selected expert 1 although unbiased sigmoid selected expert 0, and the emitted gate was expert 1's un-biased score `0.5`; the case passed with zero error.
- Accepted and executed boundary: `M=4,E=13,top_k=4,bfloat16`, unnormalized, scale 2.827. It exercised `EP=16` padding and a post-rounding tie, returned only valid IDs, and passed with zero error.
- Rejected and executed through the public wrapper before any kernel launch:
  - rank-1 logits: `router_logits must be rank 2`.
  - float64 logits: `router_logits dtype must be float16, bfloat16, or float32`.
  - `top_k=0`: `top_k must satisfy 0 < top_k <= E`.
  - `top_k=9,E=8`: `top_k must satisfy 0 < top_k <= E`.
  - `E=1025`: `E must be at most 1024`.
  - `M=33,top_k=4`: `M * top_k must be at most 128`.
  - rank-2 correction bias and rank-1 length-7 bias for `E=8`: `correction_bias must be a rank-1 tensor with E elements`.
- Deliberately unsupported by the Q2 API: bias-free routing, softmax routing, expert grouping, and grouped metadata materialization. The correction-bias argument is required, and the output contract ends at direct-route IDs and weights.

## Observed

- Local and live Triton PR tips and the AITER, TokenSpeed, and plan repository tips match the frozen SHAs.
- The donor kernel and wrapper implement the frozen selection/weight boundary above at `decode_kernels.py:219-300,303-307,368-418`; the donor test reference and `5e-3/5e-3` tolerance are at `test_mxfp4_clean_warp_decode_gfx950.py:340-359,502-578`.
- The final child SHA-256 is `70631a9a346e35931879a3c3781be6a47c113a9e5f57be952602951968d74d9f`.
- The final device-body diff is exactly the gfx950 64-lane to gfx1250 32-lane layout change shown above.
- Final smoke and full suites both ran under gfx1250 FFM with `TRITON_ALWAYS_COMPILE=1`, exact cache `/tmp/a8w4-decode-ablation/Q2`, and `flock /data/lock/amd-gpu.lock`.
- The compiled name and JIT template path identify the owned child in both suites; the launch grid is exactly one workgroup.
- All four required cases passed exact ID comparison and the frozen donor FP32 gate tolerance with zero recorded error. Both `NUM_WARPS` values and all three declared router dtypes compiled and executed.
- All eight invalid examples rejected through the owned public wrapper before launch.
- Compiled metadata reports `num_ctas=1`, `shared=0`, and `global_scratch_size=0` for every specialization.
- Final hashes of `moe_decode_gfx1250.py`, `moe_common_gfx1250.py`, and `moe_gfx1250.py` remain `33a065856b1b3cc67b4f6d0a7c9777a1c19ef67e745071ebc5caeb654698fcf4`, `391788b2c47cd118e566000b7e5da7e264d2dddb05e6054f1b84fdd4c05c313a`, and `b825608f89a704fe979533a4badd6483e96cd79568e28b5c83cdf903ee922887`.
- Q2 edited only the owned child and this ledger. It did not edit `run_ffm.py`, the registry, the shared investigation ledger, original target sources, or another cell. It performed no build, tuning, performance measurement, Git stage, commit, stash, reset, or push.

## Inferred

- The one-line device diff plus exact-ID/zero-error FFM results support that the gfx1250 32-lane adaptation preserves the donor's biased-router semantics for the tested Q2 contract.
- The no-LDS/no-intermediate source structure and `shared=0/global_scratch_size=0` metadata support the intended register-resident mechanism. This does not establish a performance advantage.

## Unknown

- Shapes within the declared bounds beyond the required matrix, non-finite input logits, correction-bias dtypes other than the tested FP32, physical B0 behavior, and integration with downstream Q5 remain untested by Q2.
- Performance is intentionally unknown because FFM has no meaningful timing.

## Alternatives Considered

- Reusing Q1 and adding a mode flag was rejected because Q2 owns a distinct sigmoid/dtype/bias numerical boundary and the plan forbids hiding it in a polymorphic softmax cell.
- Adding Q3 histogram/schedule materialization was rejected because Q2's contract ends at direct-route IDs and weights.

## Remaining blocker or next dependency

No Q2 correctness blocker remains. Q5 may consume the proved sigmoid/correction-bias top-k primitive under its separately declared grouped-materialization fusion contract. No build, tuning, commit, stage, stash, reset, or push was performed.
