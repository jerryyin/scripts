STATUS: done
CONCLUSION: Q6 implements and proves the unique-valid-`M=1` shortcut: all five serialized gfx1250 FFM cases match executable Q3 exactly on every output field, all 19 unsupported boundaries reject before child launch, and the five emitted child specializations contain direct expert-by-slot reductions rather than Q3's histogram, scans, or route-pair stable-rank construction.

# Q6 — flat M=1 precomputed grouped-route materialization

## Frozen experiment

- Triton source: `/root/triton-mi450` detached at `80e223f93d59359161f5482fbb69bbfab29c0a0b`. Before this ledger was created, local `HEAD` and an independent live lookup of `origin refs/pull/154/head` both resolved to that SHA.
- AITER source: `/root/aiter` at `4a1cc773f34cbfc74387259e51262556ee38edd0`.
- TokenSpeed provenance source: `/root/tokenspeed` at `3e725ac2b785b71f27ff9e9ac3796349c495d225`.
- Plan source: `/root/scripts` at `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf`.
- Requested and coordinator-confirmed launch profile: `gpt-5.6-sol`, reasoning effort `ultra`, `fork_turns=none`.
- Parent: completed Q3 `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k20_precomputed_topk_group_route.py`, SHA-256 `33d1afba612af921df7ec8fe5dbab31c8a43423d1df4f665660aa9b0095ee791`.
- Structural provenance: TokenSpeed `_precomputed_topk_route_m1_flat` and `gluon_precomputed_topk_flat_m1_route` in `/root/tokenspeed/tokenspeed-kernel-amd/python/tokenspeed_kernel_amd/ops/moe/fused_mxfp_gfx950.py:9722-9785,10716-10789`. The standalone kernel/wrapper are launchable, but their only caller branch at `:8037-8057` is unreachable at the pinned revision: the containing helper requires `n_tokens >= _PRECOMPUTED_MFMA_MIN_M == 4`, while the branch requires `n_tokens == 1`. Q6 therefore tests the structural idea explicitly and does not claim it is live-selected.
- Owned child: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k25_flat_m1_group_route.py`.
- Owned ledger: this file.
- One allowed idea: under the explicitly validated invariant `M=1` with unique IDs all in `[0,E)`, remove Q3's general histogram, associative prefix scans, and prior-equal-route stable-rank matrix. Keep input lanes in top-k slot order, directly derive each expert's `0/1` membership and grouped offset from comparisons against the selected slots, and use that offset to place schedules, slot indices, and gates in Q3's exact expert-grouped output order.
- Donor compatibility boundary: TokenSpeed directly writes `slice_offsets[expert]=topk_slot` and keeps output rows in top-k slot order. That is correct for its downstream ragged consumer but not bit-equivalent to Q3, whose offsets/schedules/gates are expert-grouped. Q6 transfers only the `M=1` direct-metadata ownership and absence of general grouping collectives; it reconciles row order through Q3's `scatter_indices`/gate permutation rather than weakening any field comparison.
- Public input ABI is Q3's precomputed-route ABI restricted to the shortcut invariant: contiguous same-device CUDA `topk_weights[1,TOPK]` in `{float16,bfloat16,float32}`, contiguous `topk_ids[1,TOPK]` in `int32`, and integer `num_experts=E`; `1 <= TOPK <= min(E,64)`, `1 <= E <= 1024`; every ID must satisfy `0 <= id < E` and IDs must be unique.
- Public output ABI remains all seven Q3 fields and dtypes exactly: `slice_sizes[E]`, `slice_offsets[E+1]`, `block_offsets[5,E+1]`, `block_schedule[5,TOPK]`, `gather_indices[TOPK]`, `scatter_indices[TOPK]`, and `gates[TOPK]`. Integer fields are `int32`; gates preserve the input-weight dtype bit-exactly.
- For unique valid M=1 routes, Q3's exact result has `slice_sizes[e]=1` iff expert `e` is selected; `slice_offsets[e]=count(selected_id<e)` and terminal `TOPK`; every block-offset row equals `slice_offsets`; every block-schedule row lists selected experts in ascending expert ID; gather is zero; scatter records each grouped row's original top-k slot; gates follow the same grouped permutation. `MAXBLK=TOPK` for every accepted Q6 input.
- Concrete row-order adversary: IDs `[3,1,6,0]` with `E=7` must produce grouped experts `[0,1,3,6]`, scatter `[3,1,0,2]`, and gates reordered from those original slots. The child may not compare against the donor's slot-ordered ABI.
- Numerical boundary: routing is discrete and gates are copied without arithmetic or dtype conversion. Every field, shape, dtype, and gate bit pattern must equal the actual Q3 result.
- Intrinsic launch restriction: exactly one workgroup and one gfx1250 wave32 warp. There is no exposed tuning axis. `EP` and `TKP` are derived next powers of two; all five fixed block-size rows `(16,32,64,128,256)` are always emitted and compared.
- Required actual comparator: for every accepted CUDA input, call completed Q3 `gluon_precomputed_topk_group_route` on the same tensors and compare all seven outputs exactly. This is intentionally stronger than comparing against a rewritten host oracle.
- Required full FFM cases:
  - `m1-k1-f32-e4`: IDs `[2]`, FP32 gate, `E=4`; minimal top-k and no padding.
  - `slot-order-k4-f16-e7`: IDs `[3,1,6,0]`, FP16 gates, `E=7 -> EP=8`; the smoke case and direct row-order adversary.
  - `padding-k3-bf16-e5`: IDs `[4,1,3]`, BF16 gates, `E=5 -> EP=8`, `TOPK=3 -> TKP=4`.
  - `wide-k8-f32-e13`: unsorted unique IDs, FP32 gates, `E=13 -> EP=16`; exercises a wider direct membership/offset tile.
  - `max-k64-f16-e64`: a deterministic permutation of all 64 experts with FP16 gates; exercises the Q3 route-count boundary and every schedule position.
- Public pre-launch rejection matrix: rank mismatch, shape mismatch, ID dtype other than `int32`, unsupported weight dtype, non-integer/bool `num_experts`, `E=0`, `E=1025`, `M=0`, `M=2`, `TOPK=0`, `TOPK>E`, `TOPK=65`, duplicate IDs, negative ID, ID equal to `E`, non-contiguous inputs, device mismatch, and CPU inputs. The required `M!=1`, duplicate, and invalid-ID boundaries must reject before child launch.
- Exact smoke command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/Q6 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k25_flat_m1_group_route.py --suite smoke
```

- Exact full command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/Q6 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k25_flat_m1_group_route.py --suite full
```

## Parent → child ownership change

- Before Q6, Q3 program 0 owns general `M<=16,G<=64` routes, filters invalid IDs, builds an expert histogram, scans it for slice offsets, scans active experts for schedules, constructs a `GP x GP` prior-equal-route matrix for stable ranks, and then places grouped outputs.
- After Q6, program 0 is valid only for `M=1` unique in-range routes. It owns top-k slots directly, computes expert membership and the count of selected IDs below each expert, then uses those direct offsets to place the same seven output fields. Invalid, duplicate, or multi-token inputs never launch the child.
- Emitted source IR has exactly two global loads, for IDs and gates, and 21 statically unrolled final-output stores: three slice stores, three stores for each of five block rows, and three gather/scatter/gate stores. No global intermediate is introduced.
- Compiler metadata reports shape-dependent dynamic LDS of 16, 32, 64, or 256 bytes for layout conversion/reduction/gather lowering. All five specializations report zero global scratch and zero profile scratch; ISA reports zero private segment and `uses_flat_scratch=0`.

## Source diff classification

- The one semantic device change is Q6's checked specialization. Q3's histogram plus exclusive histogram scan becomes `selected[e,slot] = (id[slot] == e)` and `slice_sizes[e] = sum(selected[e,:])`; its prefix scan becomes `slice_offsets[e] = sum(id[slot] < e)`; and its route-pair same-expert/prior-index stable rank becomes the direct grouped position `slice_offsets[id[slot]]`. Offset-indexed stores then retain Q3's expert-grouped schedules, scatter permutation, and gate order.
- Q6 does not copy TokenSpeed's incompatible slot-index offsets. It keeps loads in original top-k slot lanes, computes the Q3 grouped position, stores `scatter_indices[grouped_pos]=slot`, and stores each gate at the same grouped position. The unsorted smoke case is a direct adversary against accidentally retaining TokenSpeed's output order.
- Unique valid `M=1` means every selected expert owns exactly one row. `MAXBLK` is therefore `TOPK`, every block-offset row equals `slice_offsets`, and every block-schedule row is the same ascending-expert sequence. The child still emits all five inherited block rows explicitly.
- The public wrapper adds only invariant validation, exact Q3-shaped allocation, and one-workgroup/one-wave launch. Completed Q3 is imported only by the test comparator; the public Q6 path calls only `_flat_m1_group_route`.
- Mechanical verification code freezes five accepted cases, 19 rejected cases, exact seven-field comparison, row-order reconstruction, all-five-row checks, environment/cache assertions, dispatch provenance, structured JSON evidence, and FFM-safe process teardown.
- AST and emitted-artifact audits found no device-body call or operation for `histogram`, `associative_scan`/`tt.scan`, same-expert route matching, prior-route comparison, or stable rank. The kernel arguments are exactly the nine data pointers, two input strides, and `E/TOPK/EP/TKP/NB_C` plus two output strides; there is no `M`, `GP`, `MAXBLK`, or `MAXBLKP` argument.
- The final child has 961 lines, no tabs, no trailing whitespace, no line over 88 columns, parses successfully, and has SHA-256 `e047c933801349abfc1cdccd5d2e72aaa0dc7e9f665961c5368f8f568386e4df`.

## FFM cases and evidence

The exact frozen smoke command exited 0. It reported gfx1250, the exact `/tmp/a8w4-decode-ablation/Q6` cache, `TRITON_ALWAYS_COMPILE=1`, 19/19 rejection passes, exact owned-child dispatch, and:

```text
CASE_RESULT {"case":"slot-order-k4-f16-e7","field_equality":{"slice_sizes":"EXACT","slice_offsets":"EXACT","block_offsets":"EXACT","block_schedule":"EXACT","gather_indices":"EXACT","scatter_indices":"EXACT","gates":"EXACT"},"block_schedule":[[0,1,3,6],[0,1,3,6],[0,1,3,6],[0,1,3,6],[0,1,3,6]],"scatter_indices":[3,1,0,2],"nonidentity_scatter":true,"max_absolute_error":0.0,"max_relative_error":0.0,"rms_relative_error":0.0,"assert_close":"PASS"}
SUITE_RESULT {"ablation_id":"Q6","cases_passed":1,"cases_total":1,"result":"PASS","suite":"smoke"}
```

The exact frozen full command then exited 0 and reported `SUITE_RESULT {"ablation_id":"Q6","cases_passed":5,"cases_total":5,"result":"PASS","suite":"full"}`.

| Case | Derived shape | Distinctive exact evidence | Seven Q3 fields | Gate max abs / max rel / RMS rel | Result |
|---|---|---|---|---|---|
| `m1-k1-f32-e4` | `TOPK=TKP=1,E=EP=4,MAXBLK=1` | sizes `[0,0,1,0]`; offsets `[0,0,0,1,1]`; every schedule row `[2]`; scatter `[0]`; FP32 gate `[0.75]` | EXACT | `0 / 0 / 0` | PASS |
| `slot-order-k4-f16-e7` | `TOPK=TKP=4,E=7,EP=8,MAXBLK=4` | schedule `[0,1,3,6]`; scatter `[3,1,0,2]`; gates follow original slots `[3,1,0,2]` | EXACT | `0 / 0 / 0` | PASS |
| `padding-k3-bf16-e5` | `TOPK=3,TKP=4,E=5,EP=8,MAXBLK=3` | sizes `[0,1,0,1,1]`; schedule `[1,3,4]`; scatter `[1,2,0]`; BF16 gates exact | EXACT | `0 / 0 / 0` | PASS |
| `wide-k8-f32-e13` | `TOPK=TKP=8,E=13,EP=16,MAXBLK=8` | schedule `[0,1,3,5,7,9,11,12]`; scatter `[1,5,3,7,2,4,6,0]` | EXACT | `0 / 0 / 0` | PASS |
| `max-k64-f16-e64` | `TOPK=TKP=E=EP=MAXBLK=64` | every expert selected; offsets `[0..64]`; all 64 scatter/gate positions and every block row exact | EXACT | `0 / 0 / 0` | PASS |

For each accepted case, the harness called completed Q3 on the same CUDA ID/weight tensors, checked shape and dtype before value equality, and required exact equality for `slice_sizes`, `slice_offsets`, `block_offsets`, `block_schedule`, `gather_indices`, `scatter_indices`, and `gates`. It separately reconstructed grouped expert IDs through Q6 scatter, required zero M=1 gather, required nonidentity scatter for every unsorted adversary, and compared all five block-size rows `(16,32,64,128,256)`.

Final-source cache and IR evidence:

- The Q6 cache contains exactly five child specialization artifact sets and five Q3 comparator artifact sets. The child sets are `NLPAVFGSMSTT2WWSLO5HQLKAXLCGZPCM7XKGLRFWPMOZKHMPW57A` (FP32 `TKP=1,EP=4`, 16-byte dynamic LDS), `GE46FYXALZP4AIHVKBBSYCSJ6JS7F4SAQZH3BXKALRHW2NYQR4MQ` (FP16 `TKP=4,EP=8`, 32 bytes), `7SE7FOCVJZ2K5UJJT3ALXYKXYXDXS66TVNRLTP6D7OGRAO76BJJA` (BF16 `TKP=4,EP=8`, 32 bytes), `LUXGAIXDFNOW7QNAB2QB4HTUVXGGTXLZAMRRCCIWYMRVL4CMPQWA` (FP32 `TKP=8,EP=16`, 64 bytes), and `O4YVRGLGH45IIW53JRTONAGVSNPMNNWH2QDEDIX5UIRIVHM7D34Q` (FP16 `TKP=64,EP=64`, 256 bytes).
- Every child metadata record names `_flat_m1_group_route`, targets HIP `gfx1250` with wave size 32, uses one warp and one CTA, and reports `global_scratch_size=0` and `profile_scratch_size=0`.
- Every source-IR and TTGIR module identifies the owned Q6 file and has exactly two `tt.load` operations, 21 statically unrolled `tt.store` operations, two direct reductions, and one `tt.gather`. There is no histogram, scan, or `ttg.local_alloc/load/store`. The only two-dimensional integer comparisons are `expert_id == expert_number` and `expert_id < expert_number` over `EP x TKP`; even when both padded dimensions are 64, there is no route-versus-route equality, prior-index predicate, or stable-rank value.
- LLVM defines `amdgpu_kernel void @_flat_m1_group_route` with two read-only global inputs, exactly seven write-only global outputs, and the two runtime-injected scratch/profile pointers marked `readnone`. It declares address-space-3 `@global_smem`, has no `alloca`, and lowers direct reductions/layout conversion/gather through dynamic LDS.
- ISA declares `.amdgcn_target "amdgcn-amd-amdhsa-unknown-gfx1250"`, exports `_flat_m1_group_route`, and attributes source locations to the owned child. Across the five cases it emits 7, 10, 10, 16, and 55 `ds_*` operations respectively, no hardware `s_barrier*` instruction for the one-wave launch, `.amdhsa_group_segment_fixed_size 0`, `.amdhsa_private_segment_fixed_size 0`, and `uses_flat_scratch=0`. The fixed group segment is zero because the 16–256-byte allocation is launch-time dynamic LDS.

## Accepted and rejected examples

- Accepted and executed: all five cases above. Together they cover `TOPK={1,3,4,8,64}`, `E={4,5,7,13,64}`, all three gate dtypes, both `E` and `TOPK` power-of-two padding, deliberately unsorted routes, the maximum route count, and every inherited block row.
- Rejected through the public wrapper before Q6 child launch, 19/19 in both smoke and full invocations:
  - rank-1 inputs: `topk_weights and topk_ids must both be rank 2`.
  - unequal input shapes: `topk_weights and topk_ids must have the same shape`.
  - `int64` IDs and `float64` weights: the exact supported dtype messages.
  - boolean and floating `num_experts`: `num_experts must be an integer`.
  - `E=0` and `E=1025`: `num_experts must satisfy 1 <= E <= 1024`.
  - `M=0` and `M=2`: `flat route requires M to equal 1`.
  - `TOPK=0`, `TOPK>E`, and `TOPK=65`: the exact top-k bound messages.
  - duplicate IDs: `flat route expert IDs must be unique for M=1`.
  - ID `-1` and ID equal to `E`: `flat route expert IDs must satisfy 0 <= id < E`.
  - non-contiguous inputs, CPU/CUDA device mismatch, and CPU-only inputs: the exact layout/device messages.
- Deliberately unsupported by Q6: general `M!=1` grouping, duplicate or invalid routes, `TOPK>64`, top-k selection/recomputation, Q4/Q5 router fusion, grouped GEMM, output dtype conversion, and a slot-ordered output ABI.

## Observed

- The completed Q3 source/ledger and the exact TokenSpeed M=1 donor kernel, wrapper, and unreachable caller branch were inspected at their pinned states.
- TokenSpeed's output offsets and row order are not Q3-equivalent for unsorted top-k IDs; Q6 must add only the mechanical Q3-ABI reconciliation described above while retaining the donor's structural removal of general grouping machinery.
- Local Triton remains detached at `HEAD=80e223f93d59359161f5482fbb69bbfab29c0a0b`, and a final independent live lookup of `origin refs/pull/154/head` returned the same SHA. The scripts, AITER, and TokenSpeed repositories remain at their frozen SHAs.
- Completed Q3 still hashes to `33d1afba612af921df7ec8fe5dbab31c8a43423d1df4f665660aa9b0095ee791`; final Q6 hashes to `e047c933801349abfc1cdccd5d2e72aaa0dc7e9f665961c5368f8f568386e4df`.
- Final-source smoke and full suites both ran serialized under gfx1250 FFM with `TRITON_ALWAYS_COMPILE=1`, exact cache `/tmp/a8w4-decode-ablation/Q6`, and `flock /data/lock/amd-gpu.lock`. Smoke passed 1/1, full passed 5/5, and the 19-case public rejection matrix passed in each invocation.
- The artifact audit passed all five child specializations and proved exact child dispatch, source provenance, runtime pointer roles, dynamic LDS, and zero scratch/private allocation.
- Original target hashes remain `33a065856b1b3cc67b4f6d0a7c9777a1c19ef67e745071ebc5caeb654698fcf4` for `moe_decode_gfx1250.py`, `391788b2c47cd118e566000b7e5da7e264d2dddb05e6054f1b84fdd4c05c313a` for `moe_common_gfx1250.py`, and `b825608f89a704fe979533a4badd6483e96cd79568e28b5c83cdf903ee922887` for `moe_gfx1250.py`.
- Q6 edited only its owned child and this ledger. It did not edit Q3, the registry, shared investigation ledger, runner, original target sources, or another cell. The untracked `hsakmt_counters.csv` already existed before Q6 and was not manually edited, deleted, or claimed as owned.
- Q6 performed no build, tuning, timing, Git stage, commit, amend, stash, reset, or push.

## Inferred

- Under the checked unique-valid-`M=1` invariant, `count(id==e)` and `count(id<e)` are exactly Q3's histogram and exclusive histogram prefix without constructing either general collective. The executed field equality supports that identity for the tested matrix.
- Each selected expert has one row and therefore one block for every fixed block size. The exact five-row comparisons support the direct block-offset/schedule collapse.
- Offset-indexed scatter/gate stores reconcile input slot order with Q3 grouped order without sorting inputs or constructing a per-route stable rank.
- Dynamic LDS is compiler-managed implementation traffic for reductions/layout conversion/gather, not a global intermediate or the removed histogram/scan/rank algorithm. This structural proof does not imply a speedup.

## Unknown

- Accepted configurations inside the declared bounds beyond the five-case matrix, especially very large padded `E` with small `TOPK`, remain untested.
- Non-finite gate payloads, physical B0 behavior, integration into the target decode call path, and downstream grouped-GEMM behavior remain untested.
- Host-side ID validation copies IDs to CPU; its runtime cost and the compiler-managed LDS/reduction cost are intentionally unmeasured.
- Performance is intentionally unknown because Q6 performs no timing.

## Alternatives Considered

- Copying TokenSpeed's M=1 body verbatim was rejected because its slot-ordered `slice_offsets`, schedules, and gates do not equal Q3 for unsorted IDs.
- Sorting input tensors on the host was rejected because it changes the public input boundary and hides route-order reconciliation outside the owned child.
- Calling Q3 from Q6's public implementation was rejected because it retains the exact histogram/scan/rank machinery Q6 owns; Q3 is used only as the executable comparator.
- Accepting invalid or duplicate IDs and synthesizing a fallback was rejected because Q6's sole structural proof depends on one valid row per expert. Those values must reject explicitly; general Q3 remains the semantic fallback outside this standalone cell.
- Importing Q4/Q5 routing fusion, adding top-k computation, or adding grouped GEMM was rejected because each is outside the single M=1 grouping specialization.

## Remaining blocker or next dependency

No Q6 correctness, dispatch, or IR blocker remains for the frozen standalone contract. Integration and performance work, if desired, require a separately authorized cell; no build, timing, Git write, or shared-source edit was performed here.
