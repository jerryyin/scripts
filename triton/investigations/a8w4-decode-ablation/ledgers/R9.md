STATUS: done
CONCLUSION: The one-wave R9 specialization is correctness-valid under gfx1250 FFM: final smoke, 4/4 full cases, and repeated smoke pass; all replicated programs make bitwise-identical route decisions, routing agrees with Q1/source references, and W1 agrees exactly with R6 fed the selected IDs.

# R9 — route-direct scaled matrix with replicated in-program top-k

## Frozen experiment

- Triton source: `/root/triton-mi450` at detached `HEAD` `80e223f93d59359161f5482fbb69bbfab29c0a0b`; live `origin refs/pull/154/head` independently resolved to the same SHA before editing.
- AITER source: `/root/aiter` at `4a1cc773f34cbfc74387259e51262556ee38edd0`.
- TokenSpeed source: `/root/tokenspeed` at `3e725ac2b785b71f27ff9e9ac3796349c495d225`.
- Plan source: `/root/scripts` at `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf` on `users/jerryyin/a8w4-decode-ablation-plan`.
- Requested and coordinator-confirmed profile: `gpt-5.6-sol`, reasoning effort `ultra`.
- Compute parent: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k13_route_direct_scaled_mfma.py`, accepted R6 SHA-256 `ef5cab65373c415fc7a413c38d59e8e2c42c8d35aa52e3059dad03f9b0e4594d`.
- Routing parent: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k18_softmax_topk_router.py`, accepted Q1 SHA-256 `e4c7153744ae02931724a6592da47618490458ae19a0b402df0660e0d1b74cbe`.
- Owned child: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k17_route_direct_scaled_mfma_fused_topk.py`.
- Owned ledger: this file.
- One allowed idea: remove R6's precomputed route-ID input; every `(token, N tile, top-k slot)` program recomputes Q1's stable full-row FP32 softmax/top-k from logits, immediately uses its slot's selected expert in the inherited direct scaled-WMMA projection, and only the unique `(pid_n=0, slot=0)` owner materializes the token's complete IDs/weights.
- Exact TokenSpeed fusion donor: `/root/tokenspeed/tokenspeed-kernel-amd/python/tokenspeed_kernel_amd/ops/moe/fused_mxfp_gfx950.py:10121-10245`, SHA-256 `775d3f0578684e43de63a3139ff7135e882d8c0e584defc160728ee4cb1f7850`. Lines 10173-10177 map `(token,N tile,slot)`; lines 10179-10214 recompute stable top-k in each program; lines 10215-10225 assign one route-output writer; lines 10227-10244 immediately feed the selected expert into W1 compute.
- Q1 numerical provenance: TokenSpeed `/root/tokenspeed/tokenspeed-kernel-amd/python/tokenspeed_kernel_amd/ops/moe/gluon_a4w4_gfx950/decode_kernels.py:129-215`, SHA-256 `d65b5cfff9630a23ebc92cb7f8062eef56985f1bbc3717f7db3b97a71620e2e0`, and accepted Q1's 32-lane port. R9 uses Q1's full-row FP32 softmax, smaller-ID stable ties, optional selected-weight normalization, and routing scale.
- Donor mismatch stated explicitly: TokenSpeed's fused cooperative kernel is gfx950, spreads routing across four waves, already uses LDS for matrix transport, and emits top-k-only softmax weights. R9 retains R6's gfx1250 direct-global-load scaled WMMA and zero-LDS contract. Under `normalize_topk_weights=True`, Q1's normalized selected full-softmax gates are algebraically the donor's top-k-only softmax gates; R9 applies Q1's routing scale afterward.
- Frozen narrow specialization: R9 supports only R6's `BLOCK_N=16, NUM_WARPS=1`. Literal routing over R6's `BLOCK_N=32, NUM_WARPS=2` would either require a cross-wave reduction/synchronization (adding LDS/cooperation absent from R6) or a new per-wave replicated-routing/operand-remapping scheme. `BLOCK_N=32/NUM_WARPS=2` rejects explicitly rather than smuggling either second idea into R9.
- `BLOCK_K=128` remains fixed by the gfx1250 A8-by-W4 scaled-WMMA instruction. Workload K values 128 and 512 exercise one and four runtime fragments; K=320 rejects cleanly.
- R9 accepts router logits in Q1's `{float16,bfloat16,float32}` set with `E` equal to the local packed-weight expert count, `0<TOP_K<=E`, and `M*TOP_K<=128`. It restricts `normalize_topk_weights=True`, the exact common subset where Q1 and the donor's top-k-only softmax weight contract coincide; unnormalized routing rejects explicitly.
- R6's data/numerical ABI remains: E4M3 `x[M,K]`, FP32 `x_scale[M]`, packed-E2M1 `weight[E,K/2,N]`, compact UE8M0 `weight_scale[E,K/32,N]`, optional FP32 bias `[E,N]`, and FP32 route projections `[M*TOP_K,N]`. Router IDs are local zero-based IDs because logits and weights cover the same local expert set.
- R9 additionally returns Q1-format int32 IDs `[M,TOP_K]` and FP32 weights `[M,TOP_K]`. Routing weights are emitted for downstream use but do not multiply the W1 route projection.
- Correctness instrumentation uses a separate capture entry to expose every program's complete ID/weight decision plus all 16 duplicated matrix rows. Production exposes no capture pointers.
- Frozen full cases:
  - `R9-regular-m1-k1-f32-k128`: `M=1,E=8,TOP_K=1,N=16,K=128`, exact one N tile, scale 1.0.
  - `R9-tied-m1-k4-f16-repeat`: `M=1,E=8,TOP_K=4,N=34,K=512`, exact ties, three N tiles, scale 1.75.
  - `R9-extreme-m4-k1-bf16-tail`: `M=4,E=8,TOP_K=1,N=18,K=512`, extreme logits, two N tiles, scale 0.5.
  - `R9-mixed-m4-k4-f32-repeat`: `M=4,E=13,TOP_K=4,N=34,K=512`, non-power-of-two E, ties/extremes, three N tiles, scale 2.827.
- Smoke is `M=1,E=8,TOP_K=4,N=34,K=128`, tied FP16 logits, three N tiles, and scale 1.75.
- Every case compares fused IDs/weights with the independently compiled Q1 child, weights with an independent stable top-k-only softmax reference, projection output with independently compiled R6 fed Q1's exact IDs, all program captures with the token decision, and repeated N-tile/slot decisions with each other.
- Original target hashes before editing: `moe_decode_gfx1250.py` `33a065856b1b3cc67b4f6d0a7c9777a1c19ef67e745071ebc5caeb654698fcf4`, `moe_common_gfx1250.py` `391788b2c47cd118e566000b7e5da7e264d2dddb05e6054f1b84fdd4c05c313a`, and `moe_gfx1250.py` `b825608f89a704fe979533a4badd6483e96cd79568e28b5c83cdf903ee922887`.

Exact smoke command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONDONTWRITEBYTECODE=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/R9 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k17_route_direct_scaled_mfma_fused_topk.py --suite smoke
```

Exact full command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONDONTWRITEBYTECODE=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/R9 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k17_route_direct_scaled_mfma_fused_topk.py --suite full
```

## Parent → child ownership change

- Before: R6 maps one program to `(route,N tile)`, loads one precomputed route ID, directly consumes that expert, and stores one route projection.
- After: R9 maps the equivalent program set as `(token,N tile,slot)`, recomputes the token's complete Q1 route in every program, selects its slot's expert immediately, and computes the inherited projection. Only `(pid_n=0,slot=0)` writes the complete ID/weight vectors.
- Matrix ownership, duplicated-row scaled-WMMA, direct selected-expert weight/scale loads, row-zero projection store, and output order remain R6's.
- Expert grouping, routed-output ownership, W1 gate/up fusion, LDS/TDM transport, atomics, lookahead, and routing-weight application remain absent.

## Source diff classification

- Final child SHA-256: `c8c9185cb16e2432998069e249a467dc54123d51a98c87f6e42f2a5ad657cd85` (1,890 lines).
- `git diff --no-index --stat` against R6 reports one child with 974 insertions and 581 deletions. The size comes from retaining a standalone executable harness while changing the routing ABI; it is not a production-target edit.
- Header/import/ABI hunks add the R9 identity, Q1 control, router logits, ID/weight outputs, routing scale, and capture-only per-program decision buffers. The precomputed `route_ids_ptr`, `EXPERT_START`, and local-ID load/return path are removed.
- The only kernel dataflow change before compute is the donor's `(slot,N tile,token)` mapping plus replicated Q1 softmax/stable-top-k and a unique `(pid_n=0,slot=0)` route-output writer. The selected slot ID becomes the same scalar `expert` consumed by R6.
- A focused `diff -u` of the R6 and R9 matrix bodies shows the A8W4 scaled-WMMA compute unchanged: layouts, duplicated X rows, direct packed-weight/UE8M0 loads, `wmma_scaled`, activation scale, bias, and row-zero store are identical. Differences in that tail are limited to deriving `route` after fused routing and capture stride/guard names.
- Launch/validation hunks replace route-ID validation with router-domain validation, freeze `BLOCK_N=16/NUM_WARPS=1`, require normalized weights, allocate Q1-format outputs, and keep capture pointers out of production.
- Harness hunks replace R6's R1 comparison with the R9 gates: independently compiled Q1, independent top-k-only softmax, R6 fed the exact Q1 IDs, every-program decision capture, and production/capture equivalence.
- No grouping, output-owned W2, routing-weight product, gate/up fusion, LDS/TDM, atomics, lookahead, scaled-upcast, or MFMA was added.

## FFM cases and evidence

- Final source parsed/imported successfully through every FFM invocation with `PYTHONDONTWRITEBYTECODE=1`; source-mechanism and all nine host rejection gates ran before each numerical suite.
- Final smoke command: PASS, `1/1`. Tied FP16 `M=1,E=8,TOP_K=4,N=34,K=128` launched 12 fused programs over three N tiles. Every captured full decision was bitwise identical with IDs `[0,1,2,3]`; Q1 weights, top-k-only weights, R6 projection, host projection, capture, and production all had zero observed error.
- Final full command: PASS, `4/4`.
  - `R9-regular-m1-k1-f32-k128`: ID `[7]`; one program; Q1/source weights and R6/host projections had zero observed error.
  - `R9-tied-m1-k4-f16-repeat`: IDs `[0,1,2,3]`; 12 programs over three N tiles; all route decisions bitwise identical and every comparison had zero observed error.
  - `R9-extreme-m4-k1-bf16-tail`: IDs `[[0],[1],[0],[7]]`; eight programs over two N tiles; all comparisons had zero observed error.
  - `R9-mixed-m4-k4-f32-repeat`: IDs `[[1,2,4,6],[0,1,2,3],[0,1,2,3],[12,11,10,9]]`; 48 programs over three N tiles. Q1 weights matched exactly; the independent top-k-only reference differed by at most `2.384185791015625e-7` absolute / `1.7881393432617188e-7` relative, far below `5e-3`; R6 and host projection error was zero.
- The exact final smoke command was then rerun independently and passed `1/1` with the same IDs, zero comparison errors, and bitwise-identical replicated decisions.
- During A1 provenance review, one `ABLATION_PROVENANCE` string was found to include a nonexistent extra `gluon_a4w4_gfx950/` path component. The string was corrected to the actual TokenSpeed donor path. Because this changed the accepted source hash, the final smoke, full `4/4`, and repeated smoke suites were all rerun on the corrected source and passed; device math, launch ABI, and tolerances were unchanged.
- Production/capture routing IDs and weights and projection outputs were bitwise equal in every case. All 16 captured WMMA rows were bitwise equal to each route's semantic output.
- Selected-expert adversaries changed the host projection in every full case (maximum absolute effects from `3.9915409088134766` to `15.040534973144531`), proving the output is sensitive to the route choice.
- Generated production TTIR has exactly three stores (IDs, weights, projection); capture TTIR has six. The production kernel has no capture symbol. The unique-writer source guard and three-store production IR jointly establish that IDs/weights are materialized once per token while each route/N-tile output has one owner.
- Representative final production audits:
  - TOP_K=1/K=128: one `tt.dot_scaled`, one `v_wmma_scale_f32_16x16x128_f8f6f4`, 14 expected routing reductions, three TTIR stores, four native global stores, zero LDS, zero scratch.
  - TOP_K=4/K=128 smoke: one `tt.dot_scaled`, one native scaled-WMMA instruction, 32 expected routing reductions, three TTIR stores, six native global stores, zero LDS, zero scratch.
- Every compiled launch asserts at least one scaled-WMMA IR operation and native instruction, expected routing reductions, exact TTIR store count, zero LDS/scratch, no `v_mfma_*`, and no global atomic.
- The first development smoke classified one mechanical production-wrapper error after all capture-side numerical checks had already passed: one dummy output stride pair was absent, so Gluon reported missing `BLOCK_K`. Adding that ABI pair fixed production compilation; no routing/math/layout change was used as the fix. All evidence above is from the final source after that correction.

## Accepted and rejected examples

- Accepted boundary: `M=4,E=13,TOP_K=4,N=34,K=512`, three repeated routing tiles and 16 route projections; selected IDs span 0–12 across the full matrix.
- Nine rejected boundaries pass with exact message checks:
  - unnormalized routing;
  - `BLOCK_N=32/NUM_WARPS=2`;
  - unsupported K=320;
  - non-native `BLOCK_K=256`;
  - a would-be non-local domain where logits select expert 7 but packed weights contain only experts 0–6;
  - float64 router logits;
  - one-wave `E=33`;
  - `M*TOP_K=132`;
  - BF16/A16 activation input.
- There is no run-time rejected route ID inside the kernel: unlike R6, R9 owns selection from a router domain required to equal the packed-weight domain, so a non-local selected ID is structurally prevented and the mismatched domain rejects before launch.

## Observed

- Source inspection establishes the donor's replicated `(token,N tile,slot)` routing and unique route-output writer.
- Source inspection also establishes the architecture/transport mismatch: donor routing spans multiple waves inside a kernel whose compute already owns LDS, while accepted R6 is direct-load and zero-LDS.
- FFM establishes that Q1's normalized selected full-softmax values and the donor's top-k-only softmax values agree within FP32 rounding for the frozen cases.
- FFM establishes exact selected IDs, bitwise equality across repeated N tiles/slots, exact R6/host W1 projections, exact duplicated rows, and zero LDS/scratch.
- R6 and Q1 hashes remain the accepted values. Original target hashes remain exactly `33a065...`, `391788...`, and `b82560...`; only the owned child and this ledger were edited for R9.

## Inferred

- A one-wave specialization is the narrowest literal fusion that preserves R6's zero-LDS matrix ownership and a single route-output writer without inventing a new cross-wave routing scheme.
- The `2.384185791015625e-7` worst source-reference delta is consistent with algebraically equivalent normalization performed through different FP32 softmax paths; it is not an ID or projection discrepancy.

## Unknown

- Performance and any crossover against separate Q1+R6 launches are intentionally unknown because FFM timing is meaningless.
- Hardware behavior remains unmeasured; FFM establishes functional/generated-code correctness only.
- `E>32`, unnormalized gates, and R6's two-wave `BLOCK_N=32` configuration are unsupported rather than extrapolated.

## Alternatives Considered

- Importing the donor's cooperative LDS matrix pipeline was rejected because it combines routing fusion with a transport rewrite.
- Splitting Q1 routing across R6's two waves was rejected because the resulting cross-wave reduction/synchronization violates the no-LDS/no-cooperation boundary.
- Replicating the complete router independently per wave and remapping its expert tensors into WMMA N ownership was rejected as a second routing/layout idea rather than a mechanical port.
- A separate Q1 launch followed by R6 was rejected because it preserves the precomputed-route boundary R9 must remove.
- Output-owned top-k, W1 gate/up, routing-weight application, and lookahead were rejected because they belong to other cells.

## Remaining blocker or next dependency

No R9 blocker remains. A1 may consume this child and ledger for the round-wide corpus/scope audit; performance work must remain separate.
