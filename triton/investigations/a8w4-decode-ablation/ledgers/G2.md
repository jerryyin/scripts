STATUS: done
CONCLUSION: The BM16/NW4 child emitted AITER's all-N WMMA warp layout, passed 25/25 legal grouped-decode cases plus repeated smoke under gfx1250 FFM, and explicitly rejected incompatible BLOCK_M and warp-count values.

# G2 — AITER decode-specific WMMA warp layout

## Frozen experiment

- Triton source: `/root/triton-mi450` at detached `HEAD` `80e223f93d59359161f5482fbb69bbfab29c0a0b`; live `origin refs/pull/154/head` independently resolved to the same SHA before editing.
- AITER provenance source: `/root/aiter` at `4a1cc773f34cbfc74387259e51262556ee38edd0`.
- TokenSpeed source-state control: `/root/tokenspeed` at `3e725ac2b785b71f27ff9e9ac3796349c495d225`.
- Plan source: `/root/scripts` at `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf`.
- Requested and coordinator-confirmed launch profile: `gpt-5.6-sol`, reasoning effort `ultra`, `fork_turns=none`.
- Parent: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k00_pr154_baseline.py`.
- Owned child: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k02_aiter_decode_wmma_layout.py`.
- Final SHA-256: parent `0238de7f6a8d0ccc3dcee20a6de52edccfc60f74ca9413d44a9c93e5106ac4d9`; child `fe27527c6848a7aacb1c0930689eee900ef05ee4f8d29750b949df910c7b4ec2`.
- One allowed implementation idea: at donor-compatible `BLOCK_M=16`, replace the parent WMMA layout's mixed M/N warp ownership with AITER decode's four-warp all-N ownership. Exact donor: `/root/aiter/aiter/ops/triton/_gluon_kernels/gfx1250/moe/moe_op_gemm_a8w4.py:1007-1021`, where unpacked and packed layouts use `warp_bases=[[0,1],[0,2]]`; the persistent decode sibling repeats it at `:214-229`. AITER selects the dedicated decode body only for `block_m == 16` and its configuration fixes `num_warps=4` at `/root/aiter/aiter/ops/triton/moe/moe_op_gemm_a8w4.py:260-277,526-576`.
- Numerical contract: reuse H0's dequantized torch reference and unchanged A8W4 source tolerance (`maxtol=3e-2`, effective `rmstol=4e-3`) with the same one-byte output rounding boundary.

### Exposed configuration and tested correctness values

| Axis | Tested values | Compatibility status |
|---|---|---|
| `BLOCK_M` | `16` | Intrinsically fixed by the donor decode layout; a compile attempt with `32` rejected explicitly. |
| `BLOCK_N` | `64,128,256` | Retained parent axis. |
| `BLOCK_K` | `128,256` | Retained parent axis; gather is used only with `256` because H0 proved gather+`128` illegal independently of G2. |
| `NUM_WARPS` | `4` | Intrinsically fixed by the donor's two warp bases and AITER dispatch; a compile attempt with `8` rejected explicitly. |
| `NUM_BUFFERS` | `2,3,4` | Retained parent axis. |
| Weight-scale layout | strided, preshuffled | Retained parent axis; preshuffling may scale the N-coordinate bases by the unchanged parent register-tile footprint, while the only ownership change remains moving the second warp basis from M to N. |

- Common semantic coverage: `M={1,2,4,8,16}`, gather and scatter, E4M3 and E5M2 activation, packed E2M1 weight plus UE8M0 scales, bias enabled.
- Sparse joint coverage: G2-C1 `M/N/K=1/256/512`, scatter, E4M3, `BM/BN/BK=16/64/128`, four warps, two buffers, strided; G2-C2 `4/256/512`, scatter, E5M2, `16/128/256`, four warps, three buffers, strided; G2-C3 `8/400/416`, scatter, E5M2, `16/128/128`, four warps, three buffers, strided; G2-C4 `16/256/512`, scatter, E4M3, `16/256/256`, four warps, two buffers, preshuffled; G2-C5 `2/256/1024`, scatter, E4M3, `16/128/256`, four warps, four buffers, strided.
- Frozen invariants: `SCHEDULE='baseline'`, `PINGPONG=False`, `L2_PREFETCH_DISTANCE=-1`, `PARTIAL_TDM=False`, `RESOLVE_PARTITION_CONFLICTS=False`, `TDM_SPLIT=False`, `SPLIT_K=1`, `RAGGED_DIMENSION='M'`, and `NUM_SUBTILES=(1,1,1)`.
- Required command environment: `PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels`, `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`, `TRITON_ALWAYS_COMPILE=1`, `TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/G2`, serialized by `flock /data/lock/amd-gpu.lock` through `/root/scripts/tools/run_on_model.sh --backend ffm`. Smoke uses the shared `run_ffm.py --variant k02_aiter_decode_wmma_layout --suite smoke`; the full and rejected-boundary matrix imports the unchanged shared runner and supplies the G2-specific rebuilt `Case` list because the H0 full suite hardcodes incompatible `BLOCK_M` values.

## Parent → child ownership change

- Before: the parent four-warp strided layout uses bases `[[0,1],[1,0]]`, so warp-id bits distribute a tile across N and M.
- After: at exactly `BLOCK_M=16` and four warps, bases become `[[0,1],[0,2]]`, so both warp-id bits distribute across N. For the retained preshuffled register-tile footprint, the same ownership transformation scales the N basis coordinates by the unchanged `tiles_per_warp`.
- Unchanged: program ID still owns one expert-grouped ragged-M block and one N tile; expert grouping, routing metadata, global descriptors, TDM/LDS traffic, scaled WMMA primitive, K pipeline, FP32 accumulation and bias, output conversion, and store/scatter boundary remain identical.

## Source diff classification

The complete parent-to-child diff is:

```diff
--- k00_pr154_baseline.py
+++ k02_aiter_decode_wmma_layout.py
@@ -5,10 +5,10 @@

 from triton_kernels.matmul_details._common import compute_pids, compute_offsets

-ABLATION_ID = "H0"
-ABLATION_PARENT = "Triton PR #154 moe_decode_gfx1250.py + decode-reachable moe_common_gfx1250.py helpers"
-ABLATION_IDEA = "Freeze one execution-equivalent parameterized PR #154 decode baseline"
-ABLATION_PROVENANCE = "moe_decode_gfx1250.py:17-189; moe_common_gfx1250.py:1-682 at 80e223f93d59359161f5482fbb69bbfab29c0a0b"
+ABLATION_ID = "G2"
+ABLATION_PARENT = "k00_pr154_baseline.py"
+ABLATION_IDEA = "Use AITER decode's four-warp all-N WMMA layout at BLOCK_M=16"
+ABLATION_PROVENANCE = "AITER moe_op_gemm_a8w4.py:1007-1021 at 4a1cc773f34cbfc74387259e51262556ee38edd0"


 # Handle imports for both pytest (module context) and direct execution
@@ -67,7 +67,7 @@

 @gluon.constexpr_function
 def get_wmma_layout(num_warps, packed, use_wmma_scaled, scale_preshuffle):
-    assert (num_warps in (4, 8))
+    assert num_warps == 4, "G2 AITER decode WMMA layout requires NUM_WARPS == 4"
     if scale_preshuffle:
         reg_bases = [[0, 1], [1, 0]]
         tiles_per_warp = 2
@@ -75,11 +75,7 @@
         reg_bases = []
         tiles_per_warp = 1

-    # [NUM_WARPS // 2, 2]
-    if num_warps == 4:
-        warp_bases = [[0, tiles_per_warp], [tiles_per_warp, 0]]
-    else:
-        warp_bases = [[0, tiles_per_warp], [0, tiles_per_warp * 2], [tiles_per_warp, 0]]
+    warp_bases = [[0, tiles_per_warp], [0, tiles_per_warp * 2]]

     if use_wmma_scaled:
         WMMA_INSTR_SHAPE: gl.constexpr = [16, 16, 64] if packed else [16, 16, 128]
@@ -154,6 +150,7 @@
                  WITH_X_MX_SCALE, WITH_W_MX_SCALE, SCALE_PRESHUFFLE, index_type, NUM_SUBTILES=(1, 1, 1), EVEN_K=True,
                  USE_GATHER=False, NUM_WARPS=4, TDM_WARP_USED_HINT=None, L2_PREFETCH_DISTANCE=-1,
                  RESOLVE_PARTITION_CONFLICTS=False, TDM_SPLIT=False):
+        assert BLOCK_M == 16, "G2 AITER decode WMMA layout requires BLOCK_M == 16"
         self.BLOCK_M = gl.constexpr(BLOCK_M)
         self.BLOCK_N = gl.constexpr(BLOCK_N)
         self.BLOCK_K = gl.constexpr(BLOCK_K)
```

- Essential idea: change the second four-warp basis from the parent M coordinate to the donor N coordinate. With strided scales this is exactly parent `[[0,1],[1,0]]` → donor `[[0,1],[0,2]]`; with the parent's unchanged preshuffled register bases and `tiles_per_warp=2`, it is mechanically scaled to `[[0,2],[0,4]]`.
- Mechanical support: update the four experiment identity constants and fail fast on the donor's intrinsic `BLOCK_M=16` and `NUM_WARPS=4` compatibility boundaries.
- An AST digest audit found no added or removed top-level definitions. Only `get_wmma_layout` and `MoEConfig` changed; `MoEPipelinedProgram`, `MoEProgramBase`, `_matmul_decode`, `create_descriptor`, and all other helpers are AST-identical to the parent.
- The original target hashes remain exactly H0's frozen values: `moe_decode_gfx1250.py` `33a065856b1b3cc67b4f6d0a7c9777a1c19ef67e745071ebc5caeb654698fcf4`, `moe_common_gfx1250.py` `391788b2c47cd118e566000b7e5da7e264d2dddb05e6054f1b84fdd4c05c313a`, and `moe_gfx1250.py` `b825608f89a704fe979533a4badd6483e96cd79568e28b5c83cdf903ee922887`.

## FFM cases and evidence

The exact initial and adversarial-smoke command was:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/G2 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/run_ffm.py --variant k02_aiter_decode_wmma_layout --suite smoke
```

The exact full-cell command was:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/G2 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 - <<'PY'
import dataclasses
import json
import sys
from pathlib import Path

sys.path.insert(0, str((Path.cwd() / "third_party/amd/python/examples/gluon").resolve()))
from moe_a8w4_decode_ablation import run_ffm as runner

runtime = runner.load_variant("k02_aiter_decode_wmma_layout")
runner.assert_runtime_environment(runtime.module.ABLATION_ID)
runner.assert_parent_state()
runner.prove_rejected_boundary(runtime)

original_make_case_inputs = runner._make_case_inputs

def make_case_inputs(case):
    if case.scale_preshuffling and case.block_m == 16:
        return original_make_case_inputs(dataclasses.replace(case, block_m=128))
    return original_make_case_inputs(case)

runner._make_case_inputs = make_case_inputs
cases = [
    dataclasses.replace(case, case_id=f"G2-{case.case_id}", block_m=16)
    for case in runner._default_cases()
]
cases += [
    runner.Case("G2-C1", 1, 256, 512, "scatter", "float8_e4m3fn", 16, 64, 128, 4, 2, False),
    runner.Case("G2-C2", 4, 256, 512, "scatter", "float8_e5m2", 16, 128, 256, 4, 3, False),
    runner.Case("G2-C3", 8, 400, 416, "scatter", "float8_e5m2", 16, 128, 128, 4, 3, False),
    runner.Case("G2-C4", 16, 256, 512, "scatter", "float8_e4m3fn", 16, 256, 256, 4, 2, True),
    runner.Case("G2-C5", 2, 256, 1024, "scatter", "float8_e4m3fn", 16, 128, 256, 4, 4, False),
]
if len(cases) != 25:
    raise AssertionError(f"constructed {len(cases)} cases, expected 25")
evidence = [runner.run_case(runtime, case, index) for index, case in enumerate(cases, start=1)]
if any(item["assert_close"] != "PASS" for item in evidence):
    raise AssertionError("one or more G2 cases failed")
print("G2_SUITE_RESULT " + json.dumps({"ablation_id": "G2", "cases_passed": len(evidence), "cases_total": 25, "result": "PASS", "suite": "full-cell"}, sort_keys=True), flush=True)
PY
```

The exact intrinsic-boundary command was:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/G2 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 - <<'PY'
import json
import sys
from pathlib import Path

sys.path.insert(0, str((Path.cwd() / "third_party/amd/python/examples/gluon").resolve()))
from moe_a8w4_decode_ablation import run_ffm as runner

runtime = runner.load_variant("k02_aiter_decode_wmma_layout")
runner.assert_runtime_environment(runtime.module.ABLATION_ID)
runner.assert_parent_state()

def expect_rejection(case, expected):
    try:
        runner.run_case(runtime, case, 1)
    except BaseException as error:
        messages = []
        cursor = error
        while cursor is not None:
            messages.append(str(cursor))
            cursor = cursor.__cause__
        message = " | ".join(messages)
        if expected not in message:
            raise AssertionError(f"unexpected rejection for {case.case_id}: {message}") from error
        print("CONFIG_REJECTION " + json.dumps({"case": case.case_id, "expected": expected, "message": message, "result": "PASS"}, sort_keys=True), flush=True)
        return
    raise AssertionError(f"{case.case_id} unexpectedly compiled")

expect_rejection(runner.Case("REJECT-BM32", 1, 256, 512, "scatter", "float8_e4m3fn", 32, 64, 128, 4, 2, False), "G2 AITER decode WMMA layout requires BLOCK_M == 16")
expect_rejection(runner.Case("REJECT-NW8", 1, 256, 512, "scatter", "float8_e4m3fn", 16, 128, 256, 8, 2, False), "G2 AITER decode WMMA layout requires NUM_WARPS == 4")
print("G2_REJECTION_SUITE PASS", flush=True)
PY
```

The full-cell command changes no shared file. Its sole host-side compatibility shim bypasses `run_ffm.py:516-517`, whose H0-specific preshuffle guard includes `BLOCK_M`; plain FP8 X has no MX scale and the remainder of `_make_case_inputs` does not consume `block_m`. The original G2-C4 case still drives BM16 routing metadata and launch arguments at `run_ffm.py:701-719`, while `BLOCK_N=256` and `BLOCK_K=256` satisfy the weight-scale preshuffle shape. Every invocation rebuilds inputs, reference, routing metadata, layout, and grid.

The required sequence passed: initial smoke, an isolated preshuffled probe, both intrinsic rejection probes, the 25-case full matrix, and adversarial smoke. Every process exited 0. Each numerical case compiled `_matmul_decode`, reported finite fraction 1.0, and had maximum absolute, maximum relative, and RMS relative errors exactly 0.0 after the unchanged one-byte output boundary.

Every smoke/full invocation emitted:

```text
DISPATCH_PROOF ... "ablation_id": "G2", "selected_child": ".../k02_aiter_decode_wmma_layout.py", "specialized_file": ".../k02_aiter_decode_wmma_layout.py", "template_file": ".../k02_aiter_decode_wmma_layout.py", "template_identity": true, "result": "PASS"
FFM_ENV ... "cache": "/tmp/a8w4-decode-ablation/G2", "triton_always_compile": "1", "result": "PASS"
PARENT_STATE ... "sha": "80e223f93d59359161f5482fbb69bbfab29c0a0b", "result": "PASS"
```

The 20 common cases used BM16/BN128/BK256, four warps, two buffers, and strided scales:

| Cases represented | Output shape | Output dtypes | Grid M×N | Actual metadata blocks | Finite | Abs / max-rel / RMS-rel | Assertion |
|---|---|---|---:|---:|---:|---|---|
| `G2-D-m1-{gather,scatter}-{e5m2,e4m3}` | `[1,256]` | E5M2 / E4M3 | 1×2 | 1 | 1.0 | 0 / 0 / 0 | PASS |
| `G2-D-m2-{gather,scatter}-{e5m2,e4m3}` | `[2,256]` | E5M2 / E4M3 | 2×2 | 2 | 1.0 | 0 / 0 / 0 | PASS |
| `G2-D-m4-{gather,scatter}-{e5m2,e4m3}` | `[4,256]` | E5M2 / E4M3 | 4×2 | 4 | 1.0 | 0 / 0 / 0 | PASS |
| `G2-D-m8-{gather,scatter}-{e5m2,e4m3}` | `[8,256]` | E5M2 / E4M3 | 8×2 | 7 | 1.0 | 0 / 0 / 0 | PASS |
| `G2-D-m16-{gather,scatter}-{e5m2,e4m3}` | `[16,256]` | E5M2 / E4M3 | 10×2 | 8 | 1.0 | 0 / 0 / 0 | PASS |

The five sparse cases retained multiple legal values for every non-intrinsically-restricted parent axis:

| Case | M/N/K; movement; activation | BM/BN/BK; warps/buffers; layout | Grid M×N; blocks | Output shape/dtype | Finite | Abs / max-rel / RMS-rel | Assertion |
|---|---|---|---|---|---:|---|---|
| G2-C1 | `1/256/512`; scatter; E4M3 | `16/64/128`; `4/2`; strided | 1×4; 1 | `[1,256]` E4M3 | 1.0 | 0 / 0 / 0 | PASS |
| G2-C2 | `4/256/512`; scatter; E5M2 | `16/128/256`; `4/3`; strided | 4×2; 4 | `[4,256]` E5M2 | 1.0 | 0 / 0 / 0 | PASS |
| G2-C3 | `8/400/416`; scatter; E5M2 | `16/128/128`; `4/3`; strided | 8×4; 7 | `[8,400]` E5M2 | 1.0 | 0 / 0 / 0 | PASS |
| G2-C4 | `16/256/512`; scatter; E4M3 | `16/256/256`; `4/2`; preshuffled | 10×1; 8 | `[16,256]` E4M3 | 1.0 | 0 / 0 / 0 | PASS |
| G2-C5 | `2/256/1024`; scatter; E4M3 | `16/128/256`; `4/4`; strided | 2×2; 2 | `[2,256]` E4M3 | 1.0 | 0 / 0 / 0 | PASS |

Raw summaries:

```text
SUITE_RESULT {"ablation_id": "G2", "cases_passed": 1, "cases_total": 1, "result": "PASS", "suite": "smoke"}
G2_SUITE_RESULT {"ablation_id": "G2", "cases_passed": 25, "cases_total": 25, "result": "PASS", "suite": "full-cell"}
CONFIG_REJECTION {"case": "REJECT-BM32", "expected": "G2 AITER decode WMMA layout requires BLOCK_M == 16", "result": "PASS"}
CONFIG_REJECTION {"case": "REJECT-NW8", "expected": "G2 AITER decode WMMA layout requires NUM_WARPS == 4", "result": "PASS"}
G2_REJECTION_SUITE PASS
SUITE_RESULT {"ablation_id": "G2", "cases_passed": 1, "cases_total": 1, "result": "PASS", "suite": "smoke"}
```

Compiled TTGIR proves that the source substitution reached the generated child:

- Strided BM16/BN64/BK128 artifact `/tmp/a8w4-decode-ablation/G2/GXTRKZKXMWBUXKQ2XJCAZQRRVMZW4AFBIRGPWNUWLMLQ624RFZJA/_matmul_decode.ttgir:6-7` contains unpacked and packed `ctaLayout = {warp = [[0, 1], [0, 2]]}` with instruction shapes `[16,16,128]` and `[16,16,64]`.
- Preshuffled BM16/BN256/BK256 artifact `/tmp/a8w4-decode-ablation/G2/6MOZ6OLIMYMCX7GYZSJQWGZB2LR22GVDSDLJCRJLAYKNXZBCT5GQ/_matmul_decode.ttgir:6-7` contains the unchanged `register = [[0,1],[1,0]]` plus `warp = [[0,2],[0,4]]` for both instruction shapes.

## Accepted and rejected examples

- Accepted: G2-C4 at `BLOCK_M=16`, `BLOCK_N=256`, `BLOCK_K=256`, four warps, two buffers, and preshuffled weight scales returned `[16,256]` E4M3, finite fraction 1.0, zero recorded errors, and PASS.
- Rejected donor boundary: the otherwise valid REJECT-BM32 configuration stopped during child specialization with exactly `G2 AITER decode WMMA layout requires BLOCK_M == 16`.
- Rejected donor boundary: the otherwise valid REJECT-NW8 configuration stopped during child specialization with exactly `G2 AITER decode WMMA layout requires NUM_WARPS == 4`; G2 does not invent an unproven eight-warp generalization.
- Rejected inherited public boundary: decode without M-ragged metadata stopped with exactly `decode kernel only supports M-ragged (MoE) gemms`.

## Observed

- Local Triton `HEAD` and live PR #154 head match the pinned SHA.
- AITER's non-persistent and persistent decode kernels both use the all-N four-warp bases; AITER's configuration and dispatch tie that decode path to `BLOCK_M=16` and four warps.
- Exact template, specialization, compiled-kernel name, and TTGIR all identify the owned G2 child and its intended layout.
- All 20 BM16 common semantic cases, five sparse legal configurations, two intrinsic rejection probes, the public rejection, and repeated smoke passed under gfx1250 FFM with the source numerical contract.
- The only generated IR difference inspected at the mechanism anchor is the WMMA CTA warp layout; strided code contains AITER's literal bases, while preshuffled code preserves the parent register bases and scales the same all-N ownership.
- The original target sources and shared harness remain unchanged. G2 performed no build, tuning, timing, commit, stage, stash, reset, or push.

## Inferred

- The source diff, emitted TTGIR, and full numerical matrix support the conclusion that moving the second warp basis from M to N preserves the tested grouped-decode contract at BM16/NW4.
- Preshuffled scaling is mechanical compatibility support: the parent already makes each warp own two register tiles, and the emitted layout preserves those register bases while keeping both warp-id bits on N.

## Unknown

- Performance, ranking, and crossover behavior are unknown; FFM has no meaningful timing.
- Physical B0 correctness and configurations outside the tested legal matrix are untested.
- Whether another independently designed eight-warp all-N layout would be correct or useful is deliberately unknown and outside G2.

## Alternatives Considered

- Generalizing the donor to eight warps was rejected because AITER supplies only two warp bases and selects four warps; inventing a third basis would be another unproven design.
- Falling back to the parent layout for `BLOCK_M != 16` was rejected because it would make G2 a mixed-mechanism child rather than exposing the donor compatibility boundary.
- Editing `run_ffm.py` was rejected because the shared harness is outside G2 ownership. The imported cell-specific case list reused its dispatch, environment, reference, metric, and launch helpers without changing the file.

## Remaining blocker or next dependency

No G2 correctness blocker remains. The coordinator can review the one-change diff and ledger, then update shared registry/claim state. G2 makes no performance claim.
