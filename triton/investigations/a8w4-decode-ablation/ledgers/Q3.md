STATUS: done
CONCLUSION: The gfx1250 one-workgroup precomputed-top-k materializer dispatched the owned child and passed 5/5 full FFM cases plus 14/14 public rejection cases; every histogram, prefix, block schedule, gather/scatter index, and gate matched an independent stable-sort oracle exactly, including the frozen invalid-route tail.

# Q3 — precomputed-top-k grouped route materializer

## The idea in one breath

Given the top-k experts already chosen for each token, Q3 does the bookkeeping that a grouped GEMM needs: it counts how many tokens each expert got, lays out one contiguous slot-range per expert, and builds the permutation (gather/scatter) plus per-slot gates that move tokens into and out of that expert-grouped order — all in a single tiny gfx1250 workgroup.

## Why this exists

A grouped (batched-by-expert) GEMM wants every token destined for expert E sitting **contiguously** in memory, so it can do one dense matmul per expert with a clean pointer + length. But routing produces tokens in *token* order, sprinkled across experts arbitrarily. Something has to bridge the two orderings. That "something" is a permutation: `gather` says, for each grouped output slot, which original token to pull in; `scatter` says where each grouped result goes back in the token/route layout. Q3 is exactly that bridge, precomputed as plain integer metadata so the expensive GEMM kernel never has to think about routing.

Q1/Q2 *pick* experts (softmax/top-k). Q3 assumes that's done — it takes `topk_ids[M,TOPK]` + `topk_weights[M,TOPK]` and emits the layout. No softmax, no top-k, no matmul here. Downstream, Q4/Q5 consume this proved grouped-route ABI, and Q6 separately owns any M=1-only decode shortcut.

Two subtleties the record stresses:

- **Stability.** When two routes land on the same expert, their relative order must be deterministic — sorted by `(expert_id, original_flat_route_index)`. This is a *stable sort* by expert. It matters because the scatter step has to be the exact inverse of the gather step; any tie-break ambiguity would desync the two, and it makes the output bit-reproducible for oracle comparison.
- **Invalid-route tails.** A top-k slot can hold a garbage/out-of-range id (`<0` or `>=E`) — a "no route" marker. Those are dropped: they contribute to no histogram bin and no valid slot. Q3 packs all valid routes into the front of each output array and freezes the leftover tail to sentinels (`gather=-1`, `scatter=-1`, `gate=0`) so the ABI is fully deterministic (the gfx950 donor kernel left this tail uninitialized).

## Worked example

Take **M=4 tokens, TOPK=2, E=4**. Flatten routes row-major: `flat = token*TOPK + slot`. One id is deliberately out of range.

| token | slot0 (expert) | slot1 (expert) |
|---|---|---|
| t0 | 2 | 0 |
| t1 | 0 | 3 |
| t2 | 2 | **5** (invalid, ≥E) |
| t3 | 1 | 0 |

Flattened routes (flat index → expert):

```
flat:    0    1    2    3    4    5      6    7
expert:  2    0    0    3    2    5(X)   1    0
token:   0    0    1    1    2    2      3    3
```

flat 5 is rejected and vanishes from everything below. 7 valid routes remain.

**1. Histogram — tokens per expert** (`slice_sizes[E]`):

```
expert:  e0  e1  e2  e3
count:    3   1   2   1     (e0 = flats 1,2,7;  e1 = flat 6;  e2 = flats 0,4;  e3 = flat 3)
```

**2. Exclusive prefix-sum — where each expert's block starts** (`slice_offsets[E+1]`, last entry = total valid):

```
        e0  e1  e2  e3  (end)
offset:  0   3   4   6    7
```

So the grouped array reserves slots `[0,3)` for e0, `[3,4)` for e1, `[4,6)` for e2, `[6,7)` for e3, and slot `7` is the invalid tail.

**3. Stable rank + the permutation.** Within each expert, keep routes in ascending `flat` order (that's the stable tie-break). Assign each valid route to `offset[expert] + rank_within_expert`:

| out slot | expert | flat | token | rank | gather=token | scatter=flat | gate=weight(flat) |
|---|---|---|---|---|---|---|---|
| 0 | e0 | 1 | t0 | 0 | 0 | 1 | w(0,1) |
| 1 | e0 | 2 | t1 | 1 | 1 | 2 | w(1,0) |
| 2 | e0 | 7 | t3 | 2 | 3 | 7 | w(3,1) |
| 3 | e1 | 6 | t3 | 0 | 3 | 6 | w(3,0) |
| 4 | e2 | 0 | t0 | 0 | 0 | 0 | w(0,0) |
| 5 | e2 | 4 | t2 | 1 | 2 | 4 | w(2,0) |
| 6 | e3 | 3 | t1 | 0 | 1 | 3 | w(1,1) |
| 7 | — (invalid tail) | | | | **-1** | **-1** | **0** |

Read off the final arrays:

```
gather_indices  = [0, 1, 3, 3, 0, 2, 1, -1]   # for each grouped slot, which token to pull in
scatter_indices = [1, 2, 7, 6, 0, 4, 3, -1]   # for each grouped slot, which original route it came from
gates           = [w(0,1), w(1,0), w(3,1), w(3,0), w(0,0), w(2,0), w(1,1), 0]
```

`gather` is the token→grouped permutation the GEMM reads with; `scatter` is its inverse, telling the epilogue where to write each expert result back so the top-k weighted sum lands on the right token. `gates` rides alongside so the result of slot *i* gets multiplied by its routing weight — copied verbatim, never arithmetic, so it matches bit-for-bit.

**4. Block schedule.** The GEMM tiles each expert's rows into fixed blocks. Q3 emits, per block size in `(16,32,64,128,256)`, the exclusive block-count prefix (`block_offsets`) and a packed work list (`block_schedule`) of `(block_id<<16)|expert_id` entries, `-1`-padded. In this tiny case every non-empty expert has ≤16 rows, so each block row is just one block per active expert: experts `[0,1,2,3]` then `-1` padding. (This one-block-per-expert collapse is only safe because valid expert ids are unique per token row and M≤16 — hence duplicate valid ids are rejected before launch.)

Picture the whole transform as the permutation arrow:

```
token order  (flats, invalid 5 dropped):   0  1  2  3  4 (5X) 6  7
                                             \ e2 e0 e0 e3 e2   e1 e0
grouped order (by expert, stable by flat):  [e0:1,2,7][e1:6][e2:0,4][e3:3] | tail
out slot:                                     0  1  2   3     4  5    6      7(-1)
```

---

# Rigorous record

## Frozen experiment

- Triton source: `/root/triton-mi450` at `80e223f93d59359161f5482fbb69bbfab29c0a0b`. Local `HEAD` and live `origin refs/pull/154/head` independently resolved to that SHA before this ledger was created.
- TokenSpeed provenance source: `/root/tokenspeed` at `3e725ac2b785b71f27ff9e9ac3796349c495d225`.
- Plan source: `/root/scripts` at `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf`.
- Requested launch profile: `gpt-5.6-sol`, reasoning effort `ultra`, `fork_turns=none`.
- Semantic parent: TokenSpeed `_fused_precomputed_topk_route_small_m` and `gluon_precomputed_topk_fused_route` in `/root/tokenspeed/tokenspeed-kernel-amd/python/tokenspeed_kernel_amd/ops/moe/fused_mxfp_gfx950.py:10915-11090`.
- Owned child: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k20_precomputed_topk_group_route.py`.
- Owned ledger: this file.
- One allowed idea: consume already-computed top-k IDs and weights and materialize expert histograms, exclusive prefix offsets, packed block schedules, stable per-expert ranks, gather/scatter indices, and gates in exactly one bounded gfx1250 workgroup. Q3 does not compute top-k, softmax, sigmoid, correction bias, or grouped GEMM.

### ABI — the seven metadata/gate fields

Public **input** ABI: rank-2 contiguous CUDA tensors `topk_weights[M,TOPK]` and `topk_ids[M,TOPK]`, plus integer `num_experts=E`; IDs are `int32`; weights are `float16`, `bfloat16`, or `float32`; `1 <= M <= 16`, `1 <= TOPK <= E`, `1 <= E <= 1024`, and `G=M*TOPK <= 64`.

Public **output** ABI:

| Field | Shape / dtype | Contents |
|---|---|---|
| `slice_sizes` | `[E]`, `int32` | exact accepted-route histogram per expert |
| `slice_offsets` | `[E+1]`, `int32` | exclusive histogram prefix with terminal accepted-route count |
| `block_offsets` | `[5,E+1]`, `int32` | exclusive block-count prefixes for fixed block sizes `(16,32,64,128,256)` |
| `block_schedule` | `[5,MAXBLK]`, `int32` | packed `(block_id << 16) | expert_id` entries followed by `-1` |
| `gather_indices` | `[G]`, `int32` | valid prefix = token indices in stable expert order; rejected tail = `-1` |
| `scatter_indices` | `[G]`, `int32` | valid prefix = original flattened route indices in stable expert order; rejected tail = `-1` |
| `gates` | `[G]`, input-weight dtype | valid prefix = matching route weights; rejected tail = exact zero |

- Invalid-expert policy: each ID `<0` or `>=E` is rejected per route and makes no contribution to any histogram, prefix, schedule, or valid output prefix. Rejected routes occupy only the deterministic tail sentinels above. Valid routes are ordered by stable key `(expert_id, original_flat_route_index)`. Each row must have unique valid expert IDs; duplicate valid IDs are rejected before launch because the inherited one-block-per-expert collapse depends on the precomputed-top-k uniqueness invariant and `M<=16`.
- Numerical boundary: routing is discrete. All integer metadata must match exactly. Gates are copied without arithmetic and must match bit-exactly in their original dtype, including deterministic zero tails.
- Intrinsic launch restriction: the grid is exactly one workgroup and `num_warps=1`; this is not an exposed tuning axis. The gfx950 parent uses one wave64 because `GP<=64`; Q3 adapts its blocked layouts to gfx1250 wave32 while retaining one workgroup and one warp. `GP` and `EP` are derived next powers of two, not tuning axes.
- Block-size axis: the ABI always emits and verifies all five inherited block sizes `(16,32,64,128,256)`. Because valid expert IDs are unique within each token row and `M<=16`, every expert histogram is at most 16 and therefore has at most one block for every emitted block size. The generic packed reference still computes `ceil(hist/BLOCK_M)` and `(block_id << 16) | expert_id` independently.

## Configuration & cases

Required full FFM cases:

| Case | Shape | Exercises |
|---|---|---|
| `m1-k1-f32` | `M=1,TOPK=1,E=4`, FP32, valid route | retains but does not specialize the M=1 boundary |
| `interleaved-m4-k4-f16` | `M=4,TOPK=4,E=7`, FP16, repeated experts in deliberately interleaved flat order | stable per-expert ranking |
| `padding-rejected-m3-k3-bf16` | `M=3,TOPK=3,E=5`, BF16, `G=9 -> GP=16`, `E=5 -> EP=8`, negative and positive out-of-range IDs | non-power-of-two padding and deterministic rejected tails |
| `max-routes-m16-k4-f32` | `M=16,TOPK=4,E=13`, FP32, `G=64` | public route-count boundary, repeated per-expert ranks, all five block-size rows |
| `all-rejected-m2-k2-f32` | `M=2,TOPK=2,E=3`, FP32, all IDs invalid | zero histograms, zero terminal offset, all-`-1` schedules/indices, zero gates |

The smoke suite is `padding-rejected-m3-k3-bf16`, because it jointly exercises owned-child dispatch, stable grouping, both derived padding dimensions, invalid-ID rejection, all output fields, and deterministic tails.

Independent oracle: source-local host code enumerates flattened routes, filters by the frozen invalid-ID policy, sorts valid routes by `(expert_id, flat_route_index)`, computes histograms and exclusive prefixes without calling the child, constructs generic per-block schedules for all five block sizes, and fills the gather/scatter/gate valid prefix plus deterministic tail. Every public field, shape, dtype, and gate bit pattern is compared.

Public pre-launch rejection matrix (14): rank mismatch, shape mismatch, ID dtype other than `int32`, unsupported weight dtype, `M=0`, `M=17`, `TOPK=0`, `TOPK>E`, `E=0`, `E=1025`, `G>64`, duplicate valid expert within a row, non-contiguous inputs, and non-CUDA inputs.

### Exact FFM commands

Smoke:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/Q3 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k20_precomputed_topk_group_route.py --suite smoke
```

Full:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/Q3 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k20_precomputed_topk_group_route.py --suite full
```

## Parent → child ownership change

- Before Q3, the target grouped A8W4 decode kernel consumes externally materialized grouped-route metadata. It does not own construction of expert histograms, offsets, schedules, or stable route permutations.
- After Q3, grid program 0 owns all `G<=64` precomputed routes, filters invalid IDs, creates every metadata field, and copies ordered gates. No second launch, top-k computation, or M=1 specialization participates.
- The math primitive changes from no owned route materializer to integer histogram, associative prefix scans, equality-based stable ranks, and indexed stores. It introduces no floating-point reduction and no gate arithmetic.
- Global traffic is one ID and one weight load per actual route, metadata stores for all experts/block rows, and one gather/scatter/gate store per output slot. There is no global intermediate and final metadata reports zero global scratch. TTGIR represents the histogram, scans, match matrix, and ranks as local SSA tensors, but LLVM lowers collective/layout operations through compiler-managed LDS and barriers; final metadata reports 128 bytes of shared memory for four specializations and 256 bytes for `G=64`.

The complete final device-body diff against the exact TokenSpeed parent:

```diff
--- TokenSpeed fused_mxfp_gfx950.py:10914-11001
+++ Q3 k20_precomputed_topk_group_route.py:91-186
@@
-    LE: gl.constexpr = gl.BlockedLayout([1], [64], [1], [0])
-    LG: gl.constexpr = gl.BlockedLayout([1], [64], [1], [0])
-    LB: gl.constexpr = gl.BlockedLayout([1], [64], [1], [0])
-    LT: gl.constexpr = gl.BlockedLayout([1, 1], [1, 64], [1, 1], [1, 0])
+    LE: gl.constexpr = gl.BlockedLayout([1], [32], [1], [0])
+    LG: gl.constexpr = gl.BlockedLayout([1], [32], [1], [0])
+    LB: gl.constexpr = gl.BlockedLayout([1], [32], [1], [0])
+    LT: gl.constexpr = gl.BlockedLayout([1, 1], [1, 32], [1, 1], [1, 0])
@@
     gl.store(GatherIndx + pos, tok, mask=valid)
     gl.store(ScatterIndx + pos, g.to(gl.int32), mask=valid)
     gl.store(GateScal + pos, vals, mask=valid)
+
+    # The parent leaves invalid-route slots uninitialized. Q3 freezes a
+    # deterministic ABI without changing the stable valid prefix.
+    valid_total = gl.sum(valid.to(gl.int32), 0)
+    rejected_tail = gmask & (g >= valid_total)
+    gl.store(GatherIndx + g, -1, mask=rejected_tail)
+    gl.store(ScatterIndx + g, -1, mask=rejected_tail)
+    gl.store(GateScal + g, 0.0, mask=rejected_tail)
```

- Essential architecture port: replace all four gfx950 wave64 layouts with gfx1250 wave32 layouts while retaining the parent's single workgroup and single warp.
- Essential ABI completion: count accepted routes and initialize only the rejected output tail to `gather=-1`, `scatter=-1`, and `gate=0`. The valid stable-sort prefix and all parent metadata calculations remain source-identical.
- Mechanical self-contained launch support: replace TokenSpeed package imports with the pinned target's Gluon imports; add the Q3 constants/dataclasses; reproduce HIP block sizes and `RaggedTensorMetadata.max_n_blocks`; preserve bounds and one-wave launch; freeze contiguous/CUDA/same-device and per-row uniqueness checks; expose launch metadata for dispatch proof.
- Mechanical verification support inside the owned module: add an independent stable-sort oracle, five frozen cases, 14 pre-launch rejection cases, exact field/shape/dtype checks, structured evidence, environment/cache assertions, CLI suite selection, and FFM teardown. None participates in device routing.
- Scope scan and the exact device diff prove the child consumes `topk_ids`/`topk_weights` directly. It contains no top-k selection, logits, softmax, sigmoid, correction bias, grouped GEMM, H0 import, or M=1 branch.

## FFM results

The final-source smoke command exited 0 and reported:

```text
DISPATCH_PROOF {"ablation_id":"Q3","compiled_kernel":"_fused_precomputed_topk_route_small_m","grid":[1],"num_warps":1,"one_workgroup":true,"selected_child":".../k20_precomputed_topk_group_route.py","template_file":".../k20_precomputed_topk_group_route.py","result":"PASS"}
CASE_RESULT {"case":"padding-rejected-m3-k3-bf16","field_equality":{"slice_sizes":"EXACT","slice_offsets":"EXACT","block_offsets":"EXACT","block_schedule":"EXACT","gather_indices":"EXACT","scatter_indices":"EXACT","gates":"EXACT"},"valid_routes":6,"max_absolute_error":0.0,"max_relative_error":0.0,"rms_relative_error":0.0,"assert_close":"PASS"}
SUITE_RESULT {"ablation_id":"Q3","cases_passed":1,"cases_total":1,"result":"PASS","suite":"smoke"}
```

The final-source full command then exited 0 and reported `SUITE_RESULT {"ablation_id":"Q3","cases_passed":5,"cases_total":5,"result":"PASS","suite":"full"}`.

| Case | Derived shape | Exact distinctive outputs | All seven fields | Gate max abs / max rel / RMS rel | Result |
|---|---|---|---|---|---|
| `m1-k1-f32` | `G=1,GP=1,E=4,EP=4,MAXBLK=1` | sizes `[0,0,1,0]`; gather/scatter `[0]/[0]`; every schedule row `[2]`; gate `[0.75]` FP32 | EXACT | `0 / 0 / 0` | PASS |
| `interleaved-m4-k4-f16` | `G=16,GP=16,E=7,EP=8,MAXBLK=7` | sizes `[2,3,2,4,1,2,2]`; scatter `[3,9,1,4,14,7,12,0,6,11,13,5,10,15,2,8]`; each schedule row `[0,1,2,3,4,5,6]` | EXACT | `0 / 0 / 0` | PASS |
| `padding-rejected-m3-k3-bf16` | `G=9,GP=16,E=5,EP=8,MAXBLK=5` | sizes `[0,2,1,1,2]`; offsets `[0,0,2,3,4,6]`; gather `[0,1,2,1,0,2,-1,-1,-1]`; scatter `[2,3,7,5,0,6,-1,-1,-1]`; schedule row `[1,2,3,4,-1]` | EXACT | `0 / 0 / 0` | PASS |
| `max-routes-m16-k4-f32` | `G=64,GP=64,E=13,EP=16,MAXBLK=16` | sizes `[5,5,5,5,5,5,5,5,5,4,5,5,5]`; offsets terminate at 64; each schedule row is experts `[0..12]` then three `-1` pads | EXACT | `0 / 0 / 0` | PASS |
| `all-rejected-m2-k2-f32` | `G=4,GP=4,E=3,EP=4,MAXBLK=3` | zero sizes/offsets; every schedule entry and gather/scatter entry `-1`; every gate zero | EXACT | `0 / 0 / 0` | PASS |

For every case, `slice_sizes`, `slice_offsets`, `block_offsets`, `block_schedule`, `gather_indices`, `scatter_indices`, and `gates` were independently shape- and dtype-checked before exact tensor equality. Every integer field was `int32`; gate dtype remained the input dtype. All five block-size rows `(16,32,64,128,256)` were compared in every case.

Adversarial checks against the headline:

| Claim | Refutation test | Outcome |
|---|---|---|
| The owned child is dispatched exactly once | Match JIT template filename to `__file__`, require compiled child symbol, grid `(1,)`, and `num_warps=1` | Survived |
| Q3 is not an H0 child or router recomputation | Exact parent/child device diff and scope scan for H0, logits, softmax, sigmoid, and selection operations | Survived |
| Stable per-expert order uses original flat-route order | Deliberately interleave repeated experts and compare the complete scatter permutation against an independent `(expert,flat_index)` stable sort | Survived |
| Negative and positive out-of-range IDs are dropped deterministically | Mixed `-1`, `E`, and `99`, then an all-invalid case; compare every metadata field and all tail sentinels | Survived |
| Padding cannot create a route or expert | Execute `G=9 -> GP=16` and `E=5 -> EP=8`, require terminal accepted count 6 and no padded expert in any schedule | Survived |
| The M=1 boundary stays on the general kernel | Execute M=1 through the same compiled child and verify there is no M=1 source branch | Survived |
| The maximum route tile remains stable | Execute `G=64` with repeated experts and compare all 64 scatter/gate entries | Survived |
| All inherited BLOCK_M choices are represented | Compare all five block offset/schedule rows in all five cases against generic `ceil(hist/BLOCK_M)` construction | Survived |
| Single-block collapse cannot silently accept duplicate top-k IDs | Send a duplicate valid expert in one row through the public wrapper | Survived; rejected before launch |

Final-source IR evidence is exact:

- Each of the five final specialization metadata records names `_fused_precomputed_topk_route_small_m`, targets `gfx1250`, and reports `warp_size=32`, `num_warps=1`, `num_ctas=1`, and `global_scratch_size=0`. Shared memory is 128 bytes for four cases and 256 bytes for the `G=64` case.
- The final `G=64` TTGIR module identifies the owned source at `k20_precomputed_topk_group_route.py:92`, declares `ttg.target="hip:gfx1250"`, `ttg.num-ctas=1`, `ttg.num-warps=1`, and `ttg.threads-per-warp=32`, and contains `tt.histogram`, both prefix scans, the `64x64` stable-rank match/reduce, `tt.gather`, and the three rejected-tail stores.
- LLVM defines `amdgpu_kernel void @_fused_precomputed_topk_route_small_m`; its LDS accesses and `llvm.amdgcn.s.barrier` calls explain the nonzero shared-memory metadata. ISA declares `.amdgcn_target ... gfx1250`, exports the same child symbol, and attributes source locations to the owned file.
- The Q3 cache contains ten artifact sets because the required smoke/full pair was run before and after a formatting-only alignment to make the parent/child device diff exact. The five post-alignment artifact directories are `EQ2KVS7CGKPZXUN2YEDLE6VXZB723CAB7UOXNURHHDLKATLEDXGA`, `K2WTLXKYOON5T5PVPNTXW7AUJLJJ5YSVF7Q2X3EGCLNE63HGU67A`, `3IXRZDHCVLUBD3DFRZ7QGYETGCXKQFYECKPVTE3LGUXSJL4YMDMA`, `GSDFHJOZMYIRKIIR4NLMTTA33WUEPTKY2RMJR34CIAK3ODVIHCTA`, and `GGTLIIXAVUHG3W7HHMQTW3ASHJBE7SIAMBHYSD4ZA7R6IPY7NJRA`.

### Accepted and rejected examples

- Accepted and executed: `M=1,TOPK=1,E=4,float32`; it used the general one-workgroup child, emitted expert 2's complete metadata, and copied gate `0.75` exactly.
- Accepted and executed invalid-ID contract: `M=3,TOPK=3,E=5,bfloat16` with IDs `[[4,-1,1],[1,5,3],[4,2,99]]`; six routes were accepted, three were dropped, and all seven fields matched the oracle exactly.
- Accepted and executed public boundary: `M=16,TOPK=4,E=13,float32`, exactly 64 routes; every metadata field and all 64 gathered/scattered gates matched exactly.
- Rejected and executed through the public wrapper before kernel launch:
  - rank-1 inputs: `topk_weights and topk_ids must both be rank 2`.
  - unequal shapes: `topk_weights and topk_ids must have the same shape`.
  - `int64` IDs: `topk_ids dtype must be int32`.
  - `float64` weights: `topk_weights dtype must be float16, bfloat16, or float32`.
  - `M=0` and `M=17`: `M must satisfy 1 <= M <= 16`.
  - `TOPK=0` and `TOPK>E`: `top_k must satisfy 1 <= top_k <= E`.
  - `E=0` and `E=1025`: `num_experts must satisfy 1 <= E <= 1024`.
  - `M*TOPK=80`: `M * top_k must be at most 64`.
  - duplicate valid expert in one token row: `valid expert IDs must be unique within each token row`.
  - non-contiguous inputs: `topk_weights and topk_ids must be contiguous`.
  - CPU inputs: `topk_weights and topk_ids must be CUDA tensors`.
- Deliberately unsupported by the Q3 API: top-k selection/recomputation, output dtype conversion, duplicate valid experts per token, `M>16`, `G>64`, an M=1-only shortcut, and grouped GEMM.

## Observed

- The exact parent source, its wrapper bounds, the one-wave launch, and the five block-size rows were inspected at the pinned TokenSpeed SHA.
- The frozen Q3 contract intentionally consumes precomputed top-k outputs and preserves a general `M<=16` route materializer; it does not take the later Q6 M=1 shortcut.
- Local Triton `HEAD` and live `origin refs/pull/154/head` still both resolve to `80e223f93d59359161f5482fbb69bbfab29c0a0b`; the plan repository remains at `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf`.
- The final child SHA-256 is `33d1afba612af921df7ec8fe5dbab31c8a43423d1df4f665660aa9b0095ee791`.
- The exact final device-body diff is the four wave64-to-wave32 layout changes plus deterministic rejected-tail stores shown above.
- Final-source serialized smoke and full suites both ran under gfx1250 FFM with `TRITON_ALWAYS_COMPILE=1`, exact cache `/tmp/a8w4-decode-ablation/Q3`, and `flock /data/lock/amd-gpu.lock`.
- All five full cases passed all seven exact field comparisons with zero recorded gate error. The matrix covers `M={1,2,3,4,16}`, `TOPK={1,2,3,4}`, `E={3,4,5,7,13}`, `G={1,4,9,16,64}`, every input weight dtype, non-power-of-two padding, mixed invalid IDs, and all-invalid IDs.
- All 14 invalid examples rejected through the public wrapper before launch.
- Exact dispatch, TTGIR, LLVM, ISA, shared-memory, and scratch evidence is recorded above.
- Original target hashes remain `33a065856b1b3cc67b4f6d0a7c9777a1c19ef67e745071ebc5caeb654698fcf4` for `moe_decode_gfx1250.py`, `391788b2c47cd118e566000b7e5da7e264d2dddb05e6054f1b84fdd4c05c313a` for `moe_common_gfx1250.py`, and `b825608f89a704fe979533a4badd6483e96cd79568e28b5c83cdf903ee922887` for `moe_gfx1250.py`.
- Q3 edited only the owned child and this ledger. It did not edit the registry, shared investigation ledger, runner, original target sources, or another cell. It performed no build, tuning, performance measurement, Git stage, commit, amend, stash, reset, or push.

## Inferred

- The inherited one-block-per-expert schedule collapse is valid under the explicitly checked unique-valid-ID-per-row invariant because each expert can occur at most once per token and therefore at most `M<=16` times.
- The exact device diff, owned-child dispatch proof, and all-field FFM comparisons support that the gfx1250 wave32 port preserves the parent's valid-route grouping semantics for the tested contract.
- Deterministic tail initialization resolves the parent's invalid-route uninitialized-output ambiguity without changing any accepted route or metadata value.

## Unknown

- **FFM = correctness only, no timing.** Accepted configurations inside the declared bounds beyond the matrix, very large `E`, non-finite gate payloads, physical B0 behavior, and integration with downstream Q4/Q5 remain untested by Q3.
- The runtime cost of host-side uniqueness validation and compiler-lowered LDS collectives is intentionally unmeasured.
- Performance is intentionally unknown because Q3 performs no timing.

## Alternatives Considered

- Recomputing top-k inside Q3 was rejected because Q3's semantic boundary begins at existing top-k IDs/weights and Q1/Q2 own router selection.
- Specializing immediately to M=1 was rejected because Q3 is the general semantic port; the plan assigns the decode-only shortcut to Q6.
- Leaving rejected-route output slots uninitialized, as the donor does, was rejected because it makes the public ABI nondeterministic and prevents field-exact oracle comparison. Deterministic sentinels preserve all valid-route semantics without adding a launch.

## Remaining blocker or next dependency

No Q3 correctness blocker remains. Q4 and Q5 may consume the proved grouped-route ABI; Q6 separately owns any M=1-only shortcut. No build, tuning, performance measurement, Git write, or shared-file edit was performed.
