STATUS: done
CONCLUSION: The owned gfx1250 R3 child preserved R2's FP32 gate/up/SwiGLU computation, rounded only the activated W1 intermediate to BF16 at its unique production store, emitted no FP8 requantization, and passed smoke/full/repeated-smoke FFM with bitwise-exact BF16 boundary results across every legal tile value.

# R3 — BF16 activated-intermediate boundary without FP8 requantization

## Frozen experiment

- Triton source: `/root/triton-mi450` at detached `HEAD` `80e223f93d59359161f5482fbb69bbfab29c0a0b`; live `origin refs/pull/154/head` independently resolved to the same SHA before editing.
- AITER source: `/root/aiter` at `4a1cc773f34cbfc74387259e51262556ee38edd0`.
- TokenSpeed source: `/root/tokenspeed` at `3e725ac2b785b71f27ff9e9ac3796349c495d225`.
- Plan source: `/root/scripts` at `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf` on `users/jerryyin/a8w4-decode-ablation-plan`.
- Requested and coordinator-confirmed launch profile: `gpt-5.6-sol`, reasoning effort `ultra`, `fork_turns=none`.
- Reviewed parent: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k07_route_direct_gate_up.py`, SHA-256 `805f11c75b13f6162d846a6690c06e785d8d9ea195a9ba8688d713672b97a733`.
- Blocking ledger: `/root/scripts/triton/investigations/a8w4-decode-ablation/ledgers/R2.md`, `STATUS: done`.
- Owned child: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k08_route_direct_gate_up_bf16.py`.
- Owned ledger: this file.
- One allowed idea: preserve R2's complete FP32 gate/up/SwiGLU computation, then round the activated route intermediate once to BF16 at its unique W1 global store. Emit no FP8 value, FP8 scale, quantization buffer, or secondary launch.
- Concrete TokenSpeed final-boundary provenance: `/root/tokenspeed/tokenspeed-kernel-amd/python/tokenspeed_kernel_amd/ops/moe/gluon_a16w4_situ_decode.py:223-237`, SHA-256 `9b56caf36c0205718f915d37d60df0ed38567c834fba1e30621f17b5a98a7144`. Lines 233-237 cast the activated product to the BF16 intermediate pointer element type and store it.
- Independent same-boundary reinforcement: `/root/tokenspeed/tokenspeed-kernel-amd/python/tokenspeed_kernel_amd/ops/moe/gluon_bf16_moe/warp_decode_gluon_kernel.py:89-107`, SHA-256 `481d5ab43ff5de44f5509624c2e61c525b6ba94cf8c18b007f9e8b3c0d6e49d1`, accumulates gate/up and evaluates SwiGLU in FP32 before casting the final intermediate to the BF16 output type.
- Cursor provenance: [Better MoE model inference with warp decode](https://cursor.com/blog/warp-decode), sections “How the two kernels work” and “Improved accuracy” (published 2026-04-06), describes the gate/up kernel writing the activated intermediate and retaining BF16 rather than converting the W1/W2 boundary through MXFP8.
- Provenance boundary: the A16W4 donor additionally rounds gate and up separately to BF16 at lines 223-225 before applying its model-specific SiTU. R3 does not import those pre-activation casts or SiTU. The plan explicitly requires rounding/storing the activated R2 intermediate at one boundary; changing the activation inputs would be a second numerical idea.
- Original target hashes before editing: `moe_decode_gfx1250.py` `33a065856b1b3cc67b4f6d0a7c9777a1c19ef67e745071ebc5caeb654698fcf4`, `moe_common_gfx1250.py` `391788b2c47cd118e566000b7e5da7e264d2dddb05e6054f1b84fdd4c05c313a`, and `moe_gfx1250.py` `b825608f89a704fe979533a4badd6483e96cd79568e28b5c83cdf903ee922887`.

### Frozen ABI and numerical contract

- Child ABI: `invoke_route_direct_a8w4_gate_up_bf16(x, x_scale, packed_gate_up_weight, gate_up_weight_scale, route_ids, gate_up_bias, *, expert_start, block_n, block_k, num_warps, capture_components=False)`.
- Inputs and ownership remain R2's: contiguous E4M3 X `[M,K]`, FP32 X scale `[M]`, concatenated packed-E2M1 gate/up W `[E,K/2,2N]`, compact UE8M0 scales `[E,K/32,2N]`, global int32 route IDs `[M,top_k]`, optional FP32 gate/up bias `[E,2N]`, and direct `(route,N tile)` programs.
- Gate/up accumulation, bias addition, `SiLU(gate)`, and multiplication by up remain FP32. Exactly the resulting activated value is converted to BF16 and stored as `[M*top_k,N]`. No gate/up pre-rounding and no FP8 intermediate/scale exist.
- `capture_components=True` remains R2's constexpr correctness instrumentation: FP32 gate, up, and `SiLU(gate)` are exposed for unchanged-boundary comparisons. The semantic final output is BF16 in both capture and default specializations.
- The exact boundary reference is `R2_reference.output.to(torch.bfloat16)`. Correctness requires bitwise equality to that tensor, plus comparison of its FP32 interpretation against the R2 FP32 reference under the unchanged `maxtol=3e-2`, effective `rmstol=4e-3` contract.
- Every deterministic smoke/full case was checked before editing: BF16 conversion changes every R2 final element. Maximum host rounding effects range from `0.0026886463165283203` to `0.054729461669921875`, so the required boundary is adversarially observable rather than numerically dormant.
- A negative FP8 control is also observable: the BF16 reference differs from an E4M3 round-trip in 15/16 to 248/256 elements across the full cases, with maximum differences `0.15625` to `1.75`.

### Configuration and required cases

- Inherited exposed `BLOCK_N`: `{4,8}`.
- Inherited intrinsic `NUM_WARPS == BLOCK_N`: legal `{4,8}`; mismatch rejects.
- Inherited exposed `BLOCK_K`: `{256,512}` logical activation elements; both values pass through native packed-FP4 scaled upcast. `128` remains rejected by the proven four-packed-byte lane-group restriction; non-divisible K rejects.
- `SCALE_BLOCK=32`, the BF16 output dtype, and `capture_components` are numerical/test semantics, not tuning axes.
- Full sparse matrix is unchanged from R2:
  - `GU-m1-k1-bn4-bk256`: `M=1`, `top_k=1`, `N=16`, `K=256`, `BLOCK_N/NUM_WARPS=4/4`, `BLOCK_K=256`.
  - `GU-m1-k4-bn8-bk512`: `M=1`, `top_k=4`, `N=16`, `K=512`, `BLOCK_N/NUM_WARPS=8/8`, `BLOCK_K=512`.
  - `GU-m4-k1-bn8-bk256`: `M=4`, `top_k=1`, `N=18`, `K=512`, `BLOCK_N/NUM_WARPS=8/8`, `BLOCK_K=256`, including the masked N tail and two K iterations.
  - `GU-m4-k4-bn4-bk512`: `M=4`, `top_k=4`, `N=16`, `K=512`, `BLOCK_N/NUM_WARPS=4/4`, `BLOCK_K=512`.
- Smoke remains `M=1`, `top_k=4`, `N=16`, `K=256`, `BLOCK_N/NUM_WARPS=4/4`, `BLOCK_K=256`.
- Every case reconstructs inputs/reference/configuration and separately validates unchanged FP32 gate/up/SiLU, exact BF16 final bits, nonzero BF16 rounding, and separation from the FP8 negative control.
- Accepted example: the full `M=4`, `top_k=4`, `BLOCK_N/NUM_WARPS=4/4`, `BLOCK_K=512` case emits BF16 `[16,16]`.
- Rejected/unsupported examples remain R2's eight boundaries: non-local expert, non-exact K, wave/output mismatch, `BLOCK_K=128`, BF16 X input, odd physical gate/up width, scale-shape mismatch, and bias-shape mismatch.

Exact smoke command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/R3 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k08_route_direct_gate_up_bf16.py --suite smoke
```

Exact full command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/R3 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k08_route_direct_gate_up_bf16.py --suite full
```

## Parent → child ownership change

- Before: R2 computes FP32 gate/up/SwiGLU and stores one FP32 route intermediate.
- After: the same program computes the same FP32 values, converts only the final activated intermediate to BF16, and stores one BF16 route intermediate for the later A16W4 W2 family.
- Unchanged: direct `(route,N tile)` ownership, route ordering/localization, one X stream, dual selected-expert W/scale streams, two FP32 accumulators, FP32 bias and SwiGLU, wave `gl.sum`, all tuning axes/restrictions, zero grouping/padding/TDM/LDS/scratch, no MFMA/WMMA, and no route combine.
- Global traffic changes only in the width/type of the unique semantic W1 output store. Capture-only FP32 component stores remain constexpr validation instrumentation and compile away from the default path.

## Source diff classification

The final parent→child diff is reproduced by:

```bash
diff -u --label k07_route_direct_gate_up.py --label k08_route_direct_gate_up_bf16.py /root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k07_route_direct_gate_up.py /root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k08_route_direct_gate_up_bf16.py
```

The retained 1,242-line parent SHA-256 is `805f11c75b13f6162d846a6690c06e785d8d9ea195a9ba8688d713672b97a733`; the final 1,436-line child SHA-256 is `9b7abeb53c8ce2efbc76e00ff9178ab439829c2083c50ebc33cb6541851919a2`; and the complete unified-diff stream SHA-256 is `7d7af6a5c857e18c4d48f828fef99e15f96c1fa19dc120bf3c2a1a00a71809b0`. Both inputs remain at the absolute paths above, so the diff is independently reproducible without another artifact.

The 24 unified hunks classify as follows:

- `@@ -19...`: experiment identity, parent, isolated cache, and precise TokenSpeed/Cursor provenance.
- `@@ -58...`: mechanical generated-code evidence fields added to the immutable launch result.
- `@@ -81...`, `@@ -405...`, `@@ -419...`, and `@@ -486...`: mechanical child kernel/launcher/compiled-name renames and BF16 return documentation.
- `@@ -291...`: the only essential device-compute change: convert `activated = SiLU(gate) * up` from FP32 to `out_ptr.dtype.element_ty` at the unique semantic store.
- `@@ -434...`: essential ABI boundary plus validation support. Allocate the semantic output as BF16 while keeping the three capture-only gate/up/SiLU buffers FP32; the latter compile away when `capture_components=False`.
- `@@ -486...`, `@@ -518...`, and `@@ -534...`: mechanical IR/assembly audit. Count the one FP32→BF16 IR truncation, BF16 rounding/store, capture-only FP32 stores, and forbidden FP8 output requantization while retaining R2's upcast, no-matrix, zero-LDS, and zero-scratch assertions.
- `@@ -786...`, `@@ -799...`, `@@ -812...`, and `@@ -827...`: source-mechanism assertions require one final BF16 cast and BF16 allocation, reject any source FP8 downcast path, and state exactly that the stored value is the FP32 activated product.
- `@@ -929...`, `@@ -964...`, and `@@ -984...`: mechanical rejected-boundary and exact-child dispatch updates, plus production generated-code evidence.
- `@@ -1025...`, `@@ -1051...`, `@@ -1070...`, `@@ -1094...`, `@@ -1102...`, `@@ -1142...`, and `@@ -1162...`: plan-mandated correctness evidence. Compare FP32 gate/up/SiLU unchanged, require the final tensor to be bitwise equal to `R2_reference.output.to(torch.bfloat16)`, prove nonzero BF16 rounding and separation from an E4M3 negative control, and require default production output to be bitwise identical to capture output.

No hunk changes route ownership/order, expert lookup, activation loading, selected gate/up W or scale streams, FP32 accumulation/bias/SwiGLU semantics, K reduction, legal tuning values, or matrix/shared/scratch behavior. The larger child is validation scaffolding for the single final-boundary change, not additional kernel ideas.

## FFM cases and evidence

- The exact smoke command exited zero with `SUITE_RESULT {"ablation_id":"R3","cases_passed":1,"cases_total":1,"result":"PASS","suite":"smoke"}`.
- The exact full command on the final child exited zero with four of four cases passing. The exact smoke command was then rerun and again passed one of one with identical reported component/boundary metrics.
- Every case emitted a BF16 final output that was bitwise equal to the explicit `reference.output.to(torch.bfloat16)` boundary. Thus final error against the declared BF16 boundary was exactly zero in every element.
- The unchanged FP32 components remained separately observable: gate and up matched exactly in every full case; SiLU maximum absolute error was at most `4.76837158203125e-7`. All component and R2-reference comparisons passed unchanged `maxtol=3e-2`, effective `rmstol=4e-3`.
- BF16 rounding was live in every case, changing every output element. Relative to R2's FP32 final reference, the full-matrix maximum absolute effect was `0.054729461669921875`, maximum relative error was `0.0036931501235812902`, and RMS relative error was `0.0008020185050554574`.
- The negative control was also live: BF16 differed from an E4M3 round-trip in 15/16 through 248/256 elements, with maximum differences from `0.15625` through `1.75`. The generated output path contained zero FP8 requantization instructions.

| Case | Output / grid | `BLOCK_N/warps` | `BLOCK_K` | Exact BF16 bits | BF16 rounding max abs | FP8-control changed | R2 max rel / RMS rel | Static native upcasts | Result |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| `GU-m1-k1-bn4-bk256` | `[1,16]` / `[4]` | `4/4` | 256 | 16/16 | `0.0026886463165283203` | 15/16 | `0.0017045526765286922 / 0.0006433853995986283` | 2 | PASS |
| `GU-m1-k4-bn8-bk512` | `[4,16]` / `[8]` | `8/8` | 512 | 64/64 | `0.012422561645507812` | 61/64 | `0.002432882785797119 / 0.0006334553472697735` | 4 | PASS |
| `GU-m4-k1-bn8-bk256` | `[4,18]` / `[12]` | `8/8` | 256 | 72/72 | `0.052440643310546875` | 69/72 | `0.002666338114067912 / 0.0006433454691432416` | 2 | PASS |
| `GU-m4-k4-bn4-bk512` | `[16,16]` / `[64]` | `4/4` | 512 | 256/256 | `0.054729461669921875` | 248/256 | `0.0036931501235812902 / 0.0008020185050554574` | 4 | PASS |

- Repeated smoke again produced BF16 `[4,16]`, grid `[16]`, 64/64 exact declared-boundary values, 64/64 values observably rounded from FP32, and 60/64 values different from the FP8 control. Default-versus-capture maximum absolute difference remained `0.0`.
- Input adversaries remained live rather than degenerate. Across the full cases, activation-scale effect was `14.464590072631836` to `24.075794219970703`; gate and up differed by at least `2.69049072265625`; SiLU changed gate by at least `0.8458733558654785`; and multiplying by up changed the activated gate by at least `2.9055492877960205`.
- Exact production dispatch resolved both `selected_child` and `template_file` to `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k08_route_direct_gate_up_bf16.py`, compiled `_route_direct_a8w4_gate_up_bf16`, matched the expected `(route,N tile)` grid, and used `capture_components=false`.
- Source evidence in the child is locatable at `k08_route_direct_gate_up_bf16.py:145-160` (direct route/expert selection), `:191-207` (one activation-scale and one X load), `:218-262` (unchanged dual selected-expert streams/reductions), and `:264-303` (FP32 bias/SwiGLU, capture guard, and the one final typed store).
- Compared at the same production boundary, R2 TTGIR `/tmp/a8w4-decode-ablation/R2/7AIKH4GLWT7CU2XKJ4R7CYGYNLMF6BW2YJJFHTBX3DEZ34CSHATA/_route_direct_a8w4_gate_up.ttgir:178` directly stores `%activated` through an FP32 pointer. R3 TTGIR `/tmp/a8w4-decode-ablation/R3/WABGBSNS4WYUYFM7IZAQW5DYQMAWFZJEOQI4W7OTN4WQ34FNRXPA/_route_direct_a8w4_gate_up_bf16.ttgir:186` truncates `%activated` from FP32 to BF16, then line 187 stores that BF16 value through a BF16 pointer.
- The corresponding R3 LLVM IR contains `fptrunc float ... to bfloat` at line 241 and a single aligned BF16 global store at line 248. Its production assembly folds the activated multiply and BF16 result formation into `v_fma_mixlo_bf16` at line 424 and issues its sole `global_store_b16` at line 427. The R2 production counterpart instead has its sole `global_store_b32` at assembly line 406.
- Nine successful R3 kernel specializations were audited by canonical opcode counts rather than artifact hashes. Every specialization has exactly one IR FP32→BF16 truncation, one `v_fma_mixlo_bf16`, one `global_store_b16`, and zero FP8-output `v_cvt_scalef32_pk8_{fp8,bf8}_f32` instructions. The three default specializations have one IR/global store total and zero `global_store_b32`; the six capture specializations have the semantic BF16 store plus exactly three debug `global_store_b32`.
- Across those nine specializations, seven have two static `v_cvt_scale_pk8_bf16_fp4` instructions and two have four, matching one versus two K fragments. All nine metadata records identify gfx1250, requested warps `{4,8}`, `shared=0`, and `global_scratch_size=0`; all nine assemblies contain zero `ds_*`, scratch load/store, `v_wmma_*`, and `v_mfma_*`.
- AST parse, module import, source-mechanism checks, and host rejected-boundary checks passed. `ruff` is not installed locally and was not treated as a correctness substitute or blocker.

## Accepted and rejected examples

- Accepted: all four full cases plus smoke used global route IDs from `{2,3,4}` with `expert_start=2`, exercised both values of every tuning axis, and produced bitwise-exact BF16 boundary tensors.
- Accepted: the default non-capture specialization emitted only one BF16 semantic store and matched the capture specialization exactly in both dispatch-proof runs.
- Rejected: global route ID `1` raised `route_ids must all be local` before dispatch.
- Rejected: `K=384`, `BLOCK_K=256` raised `K must be divisible by BLOCK_K for exact unmasked tiles`.
- Rejected: `BLOCK_N=8`, `NUM_WARPS=4` raised `NUM_WARPS must equal BLOCK_N for one wave per output`.
- Rejected: `BLOCK_K=128` raised `BLOCK_K must be one of (256, 512)`.
- Rejected: BF16 X raised `x dtype must be torch.float8_e4m3fn`, proving R3 remains A8W4 despite its BF16 output.
- Rejected: physical projection width `15` raised `packed_gate_up_weight projection width must be even for gate/up halves`.
- Rejected: scale width `2N-1` raised `gate_up_weight_scale shape must be [E, K/32, 2N]`.
- Rejected: bias width `2N-1` raised `gate_up_bias shape must be [E, 2N]`.

## Observed

- All repository SHAs and the live Triton PR head match the frozen plan.
- R2 is reviewed/done and its parent hash matches the completed ledger.
- The plan requires only the activated-intermediate BF16 boundary. TokenSpeed sources implement a final cast-to-output-type store; Cursor identifies BF16 rather than MXFP8 as the stage boundary.
- The deterministic test inputs contain nonzero BF16 rounding and differ from an FP8 round-trip.
- The owned child preserves R2's direct ownership, one-X/two-projection FP32 computation, all tuning axes, and structural assertions; its sole semantic device change is the final activated-value cast/store.
- Smoke, the four-case full matrix, and repeated smoke all pass under gfx1250 FFM with the required flock, forced compilation, and isolated R3 cache.
- Source, TTGIR, LLVM IR, assembly, metadata, exact output bits, and the FP8 negative control independently agree on an FP32-activated-to-BF16 boundary with no intermediate FP8 requantization.
- The parent and original target files retain their frozen SHA-256 hashes. R3's persistent writes are confined to its owned child and this ledger; no owned bytecode file remains.

## Inferred

- The agreement of unchanged FP32 component captures, exact BF16 final bits, and identical default/capture results supports that validation instrumentation does not alter the semantic computation.
- The R2/R3 IR and canonical opcode contrast supports that the intended type boundary—not a hidden FP8 round-trip or another launch—is responsible for the observed numerical change.
- R3 establishes correctness feasibility only. FFM cannot support a performance, traffic-efficiency, cache, register-pressure, or occupancy claim.

## Unknown

- Performance and downstream W2 behavior remain outside R3; I1 owns full composition.
- Shapes outside `M∈{1,4}`, `top_k∈{1,4}`, exercised `N∈{16,18}`, and exact `K∈{256,512}` tiles are outside this cell's evidence.
- The optional no-bias specialization is accepted by the ABI but was not part of the required matrix.

## Alternatives Considered

- Rounding gate/up before SwiGLU was rejected because it creates another numerical boundary beyond the plan's activated-intermediate store and would not compare directly against R2 at the declared edge.
- Importing TokenSpeed's SiTU parameters was rejected because R2 already froze the activation semantics.
- Emitting E4M3 plus a scale was rejected because R4 owns the strict-A8 sibling.
- Adding a second quantization/dequantization launch was rejected because R3's idea is specifically a direct BF16 W1/W2 boundary with no FP8 requantization.

## Remaining blocker or next dependency

None for R3. I1 may consume this BF16 boundary when it composes the full W1→W2 path; no performance or downstream-correctness result is implied here.
