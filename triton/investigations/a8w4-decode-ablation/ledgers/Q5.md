STATUS: done
CONCLUSION: The gfx1250 one-workgroup fusion dispatched the owned Q5 child and passed 6/6 full FFM cases plus 12/12 public rejection cases against the actual executable Q2→Q3 composition; every one of the seven grouped-route fields and every reconstructed Q2 ID/weight was bit-exact, while final IR proved that no global top-k route buffer exists.

# Q5 — fused biased sigmoid top-k plus grouped route materialization

## Frozen experiment

- Triton source: `/root/triton-mi450` detached at `80e223f93d59359161f5482fbb69bbfab29c0a0b`. Before this ledger was created, local `HEAD` and a live independent lookup of `origin refs/pull/154/head` both resolved to that SHA.
- AITER source: `/root/aiter` at `4a1cc773f34cbfc74387259e51262556ee38edd0`.
- TokenSpeed provenance source: `/root/tokenspeed` at `3e725ac2b785b71f27ff9e9ac3796349c495d225`.
- Plan source: `/root/scripts` at `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf`.
- Requested and coordinator-confirmed launch profile: `gpt-5.6-sol`, reasoning effort `ultra`, `fork_turns=none`.
- Router parent: completed Q2 `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k19_sigmoid_bias_topk_router.py`, SHA-256 `70631a9a346e35931879a3c3781be6a47c113a9e5f57be952602951968d74d9f`.
- Grouping parent: completed Q3 `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k20_precomputed_topk_group_route.py`, SHA-256 `33d1afba612af921df7ec8fe5dbab31c8a43423d1df4f665660aa9b0095ee791`.
- Fusion provenance: TokenSpeed `_fused_biased_grouped_topk`, `_fused_biased_grouped_route_small_m`, `_biased_grouped_route_small_m`, `gluon_biased_grouped_route_supported`, and `gluon_biased_grouped_fused_route` in `/root/tokenspeed/tokenspeed-kernel-amd/python/tokenspeed_kernel_amd/ops/moe/fused_mxfp_gfx950.py:9266-9382,9508-9619,11131-11194,11230-11251,11279-11320`.
- Owned child: `/root/triton-mi450/third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k22_fused_biased_group_route.py`.
- Owned ledger: this file.
- One allowed idea: remove only the global materialization boundary between Q2 and Q3 by keeping Q2's selected expert IDs and FP32 weights as in-program values and feeding them directly into Q3's histogram/prefix/schedule/stable-rank construction in the same workgroup.
- Donor compatibility boundary: TokenSpeed proves the fused work ownership and register/SSA handoff, but its helper adds `N_GROUP/TOPK_GROUP` filtering and returns gates cast to `X_DTYPE`. Those are not the completed Q2→Q3 contract. Q5 therefore does not port group-limited selection or router-dtype output gates; it preserves Q2's global biased choice and FP32 materialized-weight boundary exactly.
- Public input ABI: rank-2 CUDA `router_logits[M,E]` in `{float16,bfloat16,float32}`, rank-1 `correction_bias[E]`, integer `top_k`, boolean `normalize_topk_weights`, and float `routed_scaling_factor`. The supported Q2/Q3 intersection is `1 <= M <= 16`, `1 <= top_k <= E <= 1024`, and `G=M*top_k <= 64`. Non-contiguous logits/bias are accepted and made contiguous before launch, matching Q2.
- Public output ABI is Q3's seven-field `GroupedRoute`: `slice_sizes[E]`, `slice_offsets[E+1]`, `block_offsets[5,E+1]`, `block_schedule[5,MAXBLK]`, `gather_indices[G]`, `scatter_indices[G]`, and FP32 `gates[G]`. Integer fields are `int32`; routes are stable by `(expert_id, original_flat_route_index)`; every block-size row `(16,32,64,128,256)` is emitted.
- Numerical contract: load stored logits and cast to FP32; compute sigmoid in FP32; round scores to the router dtype before correction-bias addition, selection, and gate extraction; use FP32 correction bias only for choice; resolve exact ties by smaller expert ID; emit the selected un-biased router-dtype-rounded gates as FP32. If normalization is enabled, re-round selected gates to the router dtype, normalize with Q2's zero-denominator guard, and apply `routed_scaling_factor`. If disabled, preserve Q2's donor-specific asymmetry and ignore `routed_scaling_factor`.
- Intrinsic launch restriction: exactly one workgroup. `NW_C` is derived as one warp for `M<=2` and four warps otherwise, matching Q2 and the fused donor; required cases exercise both. `MP`, `EP`, `GP`, `TKP`, and `MAXBLKP` are derived powers of two, not tuning axes.
- Required actual comparator: on every CUDA input, call completed Q2 `invoke_sigmoid_bias_topk_route_gluon` with the exact bias/normalization/scale arguments, pass its materialized FP32 weights and `int32` IDs to completed Q3 `gluon_precomputed_topk_group_route`, and compare every Q5 field. Reconstruct token-major IDs/weights from Q5's grouped outputs and require exact equality with Q2.
- Required full FFM cases:
  - `bias-flip-m1-k1-f32-unnormalized`: `M=1,E=8,top_k=1`, correction bias changes the winner from expert 0 to expert 1 while the emitted gate remains expert 1's un-biased score; normalization disabled and scale 2.827 must be ignored.
  - `tied-m1-k4-f16-normalized`: `M=1,E=8,top_k=4`, exact tied choices, normalization enabled, scale 1.75; exercises smaller-ID order and one warp.
  - `extreme-m4-k1-f32-normalized`: `M=4,E=8,top_k=1`, `-1000/1000` logits, normalization enabled, scale 0.5; exercises sigmoid saturation and four warps.
  - `rounding-m4-k4-bf16-unnormalized`: `M=4,E=13,top_k=4`, BF16 post-sigmoid ties that differ from unrounded FP32 choice, normalization disabled, scale 2.827 ignored; exercises `E=13 -> EP=16`.
  - `padding-m3-k3-f32-normalized`: `M=3,E=5,top_k=3`, correction-biased repeated expert groups, normalization enabled, scale 1.25; exercises `G=9 -> GP=16` and `E=5 -> EP=8`.
  - `max-routes-m16-k4-f16-normalized`: `M=16,E=13,top_k=4`, deterministic repeated experts, normalization enabled, scale 1.25; exercises `G=64`, stable ranks, and the public route-count boundary.
- Smoke is `bias-flip-m1-k1-f32-unnormalized`, because it proves owned fused dispatch, bias-only selection, un-biased gate emission, ignored unnormalized scale, seven-field grouping, and the one-warp specialization against actual Q2→Q3.
- Public pre-launch rejection matrix: rank-1 logits, unsupported `float64`, `M=0`, `M=17`, `top_k=0`, `top_k>E`, `E=0`, `E=1025`, `M*top_k=80`, rank-2 bias, wrong bias length, and CPU inputs.
- Exact smoke command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/Q5 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k22_fused_biased_group_route.py --suite smoke
```

- Exact full command:

```bash
cd /root/triton-mi450
PYTHONPATH=/root/triton-mi450/python:/root/triton-mi450/python/triton_kernels PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 TRITON_ALWAYS_COMPILE=1 TRITON_CACHE_DIR=/tmp/a8w4-decode-ablation/Q5 flock /data/lock/amd-gpu.lock /root/scripts/tools/run_on_model.sh --backend ffm -- python3 third_party/amd/python/examples/gluon/moe_a8w4_decode_ablation/k22_fused_biased_group_route.py --suite full
```

## Parent → child ownership change

- Before Q5, Q2 program 0 loads logits/bias and globally stores `M x top_k` FP32 weights and `int32` IDs; Q3 program 0 reloads those tensors and stores seven grouped fields.
- After Q5, one grid program owns Q2's selection/weight state and Q3's grouping state. Selected routes stay in-program from Q2 selection through Q3 histogram and stable-rank placement; only the seven final fields are global outputs.
- Observed global traffic is one logits-tile load, one bias-vector load, and stores of the seven final fields. No top-k ID/weight pointer exists in the fused kernel signature. Every final TTGIR specialization contains exactly two `tt.load` operations rooted at `Logits` and `CorrectionBias`, and 29 statically unrolled `tt.store` operations rooted in the seven final output pointers; selected IDs/weights pass through SSA `tt.gather` values rather than a global store/load boundary.
- Q2 selection and Q3 grouping collectives lower through compiler-managed dynamic LDS. Final metadata reports 128 bytes of shared memory for five specializations and 256 bytes for the `G=64` specialization, with zero global and profile scratch throughout. LLVM's external address-space-3 `@global_smem` plus LDS atomics/barriers and ISA `ds_*` operations prove that this is real dynamic LDS, not a register-only inference.

## Source diff classification

- Essential fusion from Q2: Q5 retains Q2's stored-logit-to-FP32 load, FP32 sigmoid, router-dtype rounding before bias/selection, FP32 correction-bias addition for choice only, repeated maximum selection, smaller-expert tie break, un-biased gate extraction, and the exact optional-normalization branch. It preserves Q2's asymmetry that `routed_scaling_factor` applies only when normalization is enabled. It removes Q2's `TopkIds`/`TopkWeights` output pointers, output strides, and two global stores per selected slot.
- Essential fusion into Q3: Q5 removes Q3's `TopkIds`/`TopkWeights` input pointers, input strides, and two global loads. It flattens Q2's `[MP,TKP]` selected tiles into token-major `[GP]` `idx`/`vals` tensors with layout conversion and SSA `tt.gather`, then feeds them directly to Q3's validity mask, histogram, prefix sums, five schedule rows, stable-rank match, final permutation/gate stores, and deterministic tail.
- Essential shared ownership: Q2's derived one/four-warp policy becomes the fused program's layout policy, so Q3's otherwise identical wave32 `LE/LG/LB/LT` layouts are parameterized by `NW_C`. This is required because one program now owns both phases; it is not an exposed tuning axis.
- Preserved Q3 behavior: from `valid = ...` through the rejected-tail stores, Q5 retains Q3's ordering and metadata logic. `GateScal` remains FP32, exactly matching the dtype materialized by Q2 and consumed by the actual Q2→Q3 composition.
- Mechanical launch support: allocate only Q3's seven output tensors, derive `MP/EP/GP/TKP/MAXBLKP`, launch grid `(1,)`, preserve the shared bounds, make non-contiguous logits/bias contiguous as Q2 does, and expose compiled launch metadata for dispatch proof.
- Mechanical verification support: import completed Q2 and Q3 only in the host harness, execute them in sequence on the same CUDA inputs, compare all seven outputs, invert Q5's stable permutation to recover Q2's intermediates, assert the FFM/cache environment, run 12 public rejection examples, and tear down the FFM process. Neither parent is called by the public Q5 implementation.
- Scope audit of the final device body found exactly two source `gl.load` sites, 13 syntactic `gl.store` sites that unroll to 29 TTGIR stores, and no softmax, group-limited selector constants, matmul/GEMM, dot, MFMA, WMMA, source atomic, top-k pointer, or M=1-only branch. Python AST parsing, tab scan, and trailing-whitespace scan passed.

## FFM cases and evidence

The final-source smoke command exited 0 and reported:

```text
DISPATCH_PROOF {"ablation_id":"Q5","compiled_kernel":"_fused_biased_group_route_small_m","forbidden_arguments":[],"grid":[1],"num_warps":1,"one_workgroup":true,"selected_child":".../k22_fused_biased_group_route.py","template_file":".../k22_fused_biased_group_route.py","result":"PASS"}
CASE_RESULT {"case":"bias-flip-m1-k1-f32-unnormalized","bias_changed_top1":true,"unbiased_ids":[[0]],"recovered_ids":[[1]],"gates":[0.5],"scale_control_equal":true,"field_equality":{"slice_sizes":"EXACT","slice_offsets":"EXACT","block_offsets":"EXACT","block_schedule":"EXACT","gather_indices":"EXACT","scatter_indices":"EXACT","gates":"EXACT"},"recovered_weight_exact":true,"max_absolute_error":0.0,"max_relative_error":0.0,"rms_relative_error":0.0,"assert_close":"PASS"}
SUITE_RESULT {"ablation_id":"Q5","cases_passed":1,"cases_total":1,"result":"PASS","suite":"smoke"}
```

The final-source full command then exited 0 and reported `SUITE_RESULT {"ablation_id":"Q5","cases_passed":6,"cases_total":6,"result":"PASS","suite":"full"}`.

| Case | Derived shape / launch | Exact distinctive outputs from the actual Q2→Q3 comparator | All seven fields | Gate max abs / max rel / RMS rel | Result |
|---|---|---|---|---|---|
| `bias-flip-m1-k1-f32-unnormalized` | `G=1,GP=1,E=8,EP=8,MAXBLK=1`, one warp | Unbiased top-1 was expert 0; bias selected expert 1; recovered ID `[[1]]`; FP32 gate `[0.5]` remained expert 1's un-biased sigmoid; scale 2.827 control equaled scale 1.0 | EXACT | `0 / 0 / 0` | PASS |
| `tied-m1-k4-f16-normalized` | `G=4,GP=4,E=8,EP=8,MAXBLK=4`, one warp | Exact tie recovered IDs `[[0,1,2,3]]`; sizes `[1,1,1,1,0,0,0,0]`; every schedule row `[0,1,2,3]`; FP32 gates `[0.4375,0.4375,0.4375,0.4375]` after normalize and scale 1.75 | EXACT | `0 / 0 / 0` | PASS |
| `extreme-m4-k1-f32-normalized` | `G=4,GP=4,E=8,EP=8,MAXBLK=4`, four warps | Saturated-logit IDs `[[0],[1],[0],[7]]`; sizes `[2,1,0,0,0,0,0,1]`; every schedule row `[0,1,7,-1]`; grouped FP32 gates `[0.5,0.5,0.5,0.5]` | EXACT | `0 / 0 / 0` | PASS |
| `rounding-m4-k4-bf16-unnormalized` | `G=16,GP=16,E=13,EP=16,MAXBLK=13`, four warps | Router-rounded row-0 IDs `[3,4,0,1]` versus unrounded FP32 `[3,4,1,0]`; all 16 recovered IDs/weights exact; scale 2.827 control equaled scale 1.0 | EXACT | `0 / 0 / 0` | PASS |
| `padding-m3-k3-f32-normalized` | `G=9,GP=16,E=5,EP=8,MAXBLK=5`, four warps | IDs `[[1,0,2],[1,0,2],[4,3,1]]`; sizes `[2,3,2,1,1]`; offsets `[0,2,5,7,8,9]`; gather `[0,1,0,1,2,0,1,2,2]`; scatter `[1,4,0,3,8,2,5,7,6]` | EXACT | `0 / 0 / 0` | PASS |
| `max-routes-m16-k4-f16-normalized` | `G=64,GP=64,E=13,EP=16,MAXBLK=16`, four warps | Sizes `[5,5,5,5,5,5,5,5,5,4,5,5,5]`; offsets terminate at 64; every schedule row is experts `[0..12]` followed by three `-1` pads; all 64 recovered IDs/weights exact after normalize and scale 1.25 | EXACT | `0 / 0 / 0` | PASS |

Every case shape- and dtype-checked all seven fields before exact value comparison. Every integer output was exact `int32`; grouped gates were exact FP32. Reconstructing token-major selected routes from `slice_offsets` and `scatter_indices` reproduced actual Q2 IDs and FP32 weights exactly. All five block-size rows `(16,32,64,128,256)` were compared in every case.

Adversarial checks against the headline:

| Claim | Refutation test | Outcome |
|---|---|---|
| The comparator is the requested unfused implementation, not a rewritten host oracle | For every case, call completed Q2 `invoke_sigmoid_bias_topk_route_gluon`, pass those exact materialized tensors to completed Q3 `gluon_precomputed_topk_group_route`, and record both parent kernel names | Survived |
| The owned child is the single fused dispatch | Match the JIT template filename to `__file__`, require `_fused_biased_group_route_small_m`, and require grid `(1,)` | Survived |
| IDs and weights do not cross a hidden global intermediate | Reject top-k pointers in the Python kernel signature, inspect all six TTGIR signatures, require only the logits/bias loads, and trace all 29 stores to seven final outputs | Survived |
| Correction bias affects choice but does not leak into emitted gates | Force unbiased expert 0 to lose to biased expert 1 and require the grouped gate to remain expert 1's un-biased value `0.5` | Survived |
| Router-dtype rounding was not silently removed | Use BF16 scores where rounded and unrounded FP32 choices disagree, then require Q5 to recover Q2's rounded IDs `[3,4,0,1]` rather than `[3,4,1,0]` | Survived |
| Exact ties retain Q2's smaller-expert ordering | Use a four-way FP16 tie with `top_k=4`, then recover selected order `[0,1,2,3]` from grouped outputs | Survived |
| Q2's donor-specific scale asymmetry survives fusion | Compare actual Q2 at scale 2.827 and scale 1.0 for both unnormalized cases; exercise normalized scales 0.5, 1.25, and 1.75 separately | Survived exactly |
| Stable grouping and all five Q3 schedule rows survive fusion | Use deliberately repeated/interleaved experts through `G=64` and compare all seven fields to executable Q3 | Survived exactly |
| GP/EP padding cannot create a route or expert | Execute `G=9 -> GP=16`, `E=5 -> EP=8`, and `E=13 -> EP=16`, then require exact terminal counts, permutations, and schedules | Survived |
| Both inherited Q2 warp layouts compile and execute | Run `M=1` with one warp and `M={3,4,16}` with four warps across FP16/BF16/FP32 | Survived |
| Q4 softmax, TokenSpeed group filtering, Q6 shortcut, and GEMM did not leak into Q5 | Scan the final device body for those operations/arguments and M=1-only branching | Survived |
| Unsupported work rejects before launch | Execute 12 rank/dtype/shape/bound/bias/device cases through the public wrapper | Survived, 12/12 |

Final-source cache and IR evidence:

- The Q5 cache contains exactly six fused specialization artifact sets: `4JQ4UOTLG6R5J7VJFIX34EM6APBXRRBBMBKX2NOECJ6MBA7XZJJA` (FP32 M=1/top-k=1, one warp, 128-byte shared), `DVKA72QTEXRUC2QPTYESXNRFNDPRJVCB3WSGBGDFGTMCTE7WBVMQ` (FP16 M=1/top-k=4, one warp, 128-byte shared), `YTA22TRT3NTG7SCCMPECTLWR7MM5Z3WQDPFBBSEERF3WGBXR4C2Q` (FP32 M=4/top-k=1, four warps, 128-byte shared), `DFER4BADN6SIPAVCQB5TTJTCMM7IHDVHK7Y4WPWWAA7NFTFPG7EQ` (BF16 M=4/top-k=4, four warps, 128-byte shared), `L7243EZVQYWMM5VPGKJPJ5655QF3QP4KRGU7IY3U4EVFX5XO23ZA` (FP32 M=3/top-k=3, four warps, 128-byte shared), and `TQKFHOS5WR32RD6MQH4S5JJN7FB7KATZAA3ENWQFSNLVCIHAQTWA` (FP16 M=16/top-k=4/G=64, four warps, 256-byte shared).
- Every metadata record names `_fused_biased_group_route_small_m`, targets HIP `gfx1250` with warp size 32, uses one CTA, and reports `global_scratch_size=0` and `profile_scratch_size=0`; `num_warps` is exactly one or four according to `M`.
- Every TTGIR module identifies the owned Q5 source and has the runtime semantic pointer signature `Logits`, `CorrectionBias`, `SliceSizes`, `SliceOffs`, `BlockOffs`, `BlockSched`, `GatherIndx`, `ScatterIndx`, and FP32 `GateScal`. There is no `TopkIds`/`TopkWeights` or `N_GROUP/TOPK_GROUP/EXPERTS_PER_GROUP` pointer/argument. Each module has exactly two `tt.load` operations and 29 final stores after static unrolling; there are no TTGIR local load/store operations.
- FP16/BF16 TTGIR visibly contains FP32 `math.exp`, the router-dtype `arith.truncf`/`arith.extf` boundary, FP32 bias choice, smaller-ID `arith.minsi`, exact selected-gate handling, SSA `tt.gather`, `tt.histogram`, both Q3 scans, stable-rank reduction, final permutation/gate stores, and deterministic tail stores. Normalized specializations additionally show the selected-gate router-dtype re-round and denominator reduction; unnormalized specializations omit that branch and scale.
- LLVM defines `amdgpu_kernel void @_fused_biased_group_route_small_m` with two read-only global pointers and exactly seven write-only global pointers; two runtime-injected scratch/profile pointers are marked `readnone`. LLVM also declares external address-space-3 `@global_smem` and emits LDS accesses, address-space-3 histogram atomics, and barriers for compiler-lowered reductions, layout conversion, histogram, and scans. This is real dynamic LDS and not a hidden global route tensor.
- ISA declares `.amdgcn_target "amdgcn-amd-amdhsa-unknown-gfx1250"`, exports `_fused_biased_group_route_small_m`, and emits `ds_*` operations; multiwarp specializations also emit barrier signal/wait instructions. `.amdhsa_group_segment_fixed_size 0`, `.amdhsa_private_segment_fixed_size 0`, and `uses_flat_scratch=0` coexist with the 128/256-byte launch metadata because the shared allocation is dynamic; global scratch and private spill allocation remain zero.

## Accepted and rejected examples

- Accepted and executed bias-only selection boundary: `M=1,E=8,top_k=1,float32`, normalization disabled and scale 2.827. Bias changed the winner from expert 0 to expert 1; Q5 emitted exact un-biased gate 0.5 and ignored scale exactly as Q2.
- Accepted and executed router-rounding/padding boundary: `M=4,E=13,top_k=4,bfloat16`, normalization disabled and scale 2.827. It recovered the BF16-rounded IDs rather than the unrounded FP32 adversary, exercised `EP=16`, retained 16 valid routes, and matched all seven actual-composition fields exactly.
- Accepted and executed public route-count boundary: `M=16,E=13,top_k=4,float16`, normalization enabled, exactly 64 routes. Every final field and every reconstructed Q2 intermediate was exact.
- Rejected and executed through the public wrapper before any Q5 launch:
  - rank-1 logits: `router_logits must be rank 2`.
  - float64 logits: `router_logits dtype must be float16, bfloat16, or float32`.
  - `M=0` and `M=17`: `M must satisfy 1 <= M <= 16`.
  - `top_k=0` and `top_k>E`: `top_k must satisfy 1 <= top_k <= E`.
  - `E=0` and `E=1025`: `E must satisfy 1 <= E <= 1024`.
  - `M*top_k=80`: `M * top_k must be at most 64`.
  - rank-2 bias and bias with the wrong length: `correction_bias must be a rank-1 tensor with E elements`.
  - CPU inputs: `router_logits and correction_bias must be CUDA tensors`.
- Deliberately outside Q5: non-finite-logit semantics, TokenSpeed group-limited selection, Q4 softmax, an M=1-only shortcut, externally supplied/invalid/duplicate top-k routes, grouped GEMM, timing, and physical B0 validation.

## Observed

- The completed Q2/Q3 sources and ledgers and the exact TokenSpeed fused helper/kernel/wrapper were inspected at their pinned states.
- The TokenSpeed fused donor's group-limited choice and `X_DTYPE` gate output are incompatible with actual Q2→Q3; only its one-program boundary removal is transferable.
- Local Triton remains detached at `HEAD=80e223f93d59359161f5482fbb69bbfab29c0a0b`, and a final independent live lookup of `origin refs/pull/154/head` returned the same SHA. AITER remains `4a1cc773f34cbfc74387259e51262556ee38edd0`, TokenSpeed remains `3e725ac2b785b71f27ff9e9ac3796349c495d225`, and the plan repository remains `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf`.
- Final parent hashes remain `70631a9a346e35931879a3c3781be6a47c113a9e5f57be952602951968d74d9f` for Q2 and `33d1afba612af921df7ec8fe5dbab31c8a43423d1df4f665660aa9b0095ee791` for Q3. The final Q5 child SHA-256 is `14682fd6de3c8213fca67879f2d66b010743a973ff95474937fb7f7582084eab`.
- Final-source serialized smoke and full suites both ran under gfx1250 FFM with `TRITON_ALWAYS_COMPILE=1`, exact cache `/tmp/a8w4-decode-ablation/Q5`, and `flock /data/lock/amd-gpu.lock`.
- Smoke passed 1/1, full passed 6/6, and the public rejection matrix passed 12/12 in each invocation. Every grouped field and reconstructed parent intermediate was exact with zero recorded gate error.
- Exact dispatch, TTGIR, LLVM, ISA, dynamic-LDS, and zero-scratch evidence is recorded above. The artifact audit passed all six specialization sets.
- Original target hashes remain `33a065856b1b3cc67b4f6d0a7c9777a1c19ef67e745071ebc5caeb654698fcf4` for `moe_decode_gfx1250.py`, `391788b2c47cd118e566000b7e5da7e264d2dddb05e6054f1b84fdd4c05c313a` for `moe_common_gfx1250.py`, and `b825608f89a704fe979533a4badd6483e96cd79568e28b5c83cdf903ee922887` for `moe_gfx1250.py`.
- Q5 intentionally edited only the owned child and this ledger. It did not edit either parent, the registry, shared investigation ledger, runner, original target sources, or another cell. The FFM runner updated its untracked `hsakmt_counters.csv` side artifact in the workspace; it was not manually edited, deleted, or claimed as owned because its pre-Q5 provenance was not frozen.
- Q5 performed no build, tuning, timing, Git stage, commit, amend, stash, reset, or push.

## Inferred

- Q2's unique valid top-k IDs satisfy Q3's single-block-per-expert invariant under `M<=16`; the terminal count `G` observed in every finite-logit case is consistent with that composition boundary.
- The owned-source signature, executable parent composition, exact output matrix, and final IR jointly support the headline that Q5 removes the global Q2→Q3 ID/weight boundary without changing the tested Q2 selection/numerics or Q3 ordering/metadata behavior.
- Dynamic LDS is compiler-managed implementation traffic for collectives and layout conversion, not a reintroduced global route tensor. The evidence does not imply that the fused kernel is faster.

## Unknown

- Supported configurations inside the declared bounds beyond the six-case matrix, non-finite input logits/bias, physical B0 behavior, and downstream integration remain untested.
- Whether eliminating two global route tensors and one launch offsets the fused program's larger live state and dynamic-LDS collective cost is unknown.
- Performance is intentionally unknown because FFM has no meaningful timing and Q5 performed no timing.

## Alternatives Considered

- Copying TokenSpeed's grouped-biased helper verbatim was rejected because `N_GROUP/TOPK_GROUP` filtering changes Q2 selection and its final `X_DTYPE` cast changes the Q2→Q3 gate ABI.
- Calling Q2 then Q3 in Q5's public path was rejected because it preserves the exact materialization boundary Q5 owns; those calls are used only by the required executable comparator.
- Importing Q4, adding Q6's M=1 shortcut, or adding grouped GEMM was rejected because each changes scope beyond the single boundary-removal idea.

## Remaining blocker or next dependency

No Q5 correctness or IR blocker remains. Downstream cells may consume the proved fused biased grouped-route ABI under their own contracts; Q6 still owns any M=1 shortcut, and grouped GEMM remains outside Q5. No build, tuning, timing, Git write, or shared-source edit was performed.
