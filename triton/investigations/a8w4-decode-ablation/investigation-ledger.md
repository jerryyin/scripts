# A8W4 decode ablation execution — investigation ledger

## Goal and done-claims

- CLAIM-1: H0 is an execution-equivalent self-contained snapshot of PR #154 and passes the 20 source tiny-M cases plus five sparse joint-configuration cases under gfx1250 FFM. | refutation test: compare the frozen helper closure against the pinned sources, prove the child dispatches, run all 25 cases with unique cache state, and test at least one rejected configuration boundary | status: survived
- CLAIM-2: Every required implementation row has one independently attributable source change and passes its required FFM correctness matrix across multiple legal tuning values or a tested intrinsic restriction. | refutation test: review every parent→child diff for extra ideas, prove child dispatch, rerun one accepted and one boundary/adversarial case, and reject any ledger without numerical evidence | status: survived
- CLAIM-3: The routing and route-direct families preserve their declared numerical and ownership contracts rather than passing through a parent or mismatched reference. | refutation test: compare intermediate boundaries and metadata, exercise rejected/non-local routes, and inspect ownership-specific source invariants for Q1–Q6 and R1–R9 | status: survived
- CLAIM-4: I1 and I2 compose the independently verified W1 and W2 ideas into complete BF16-boundary and strict-A8 decode paths. | refutation test: run end-to-end stage-boundary and final-output checks for M/top-k coverage and compare each integrated stage with its independently tested donor | status: survived
- CLAIM-5: A1 finds no omitted corpus kernel, missing provenance, untested active cell, modified original target, or unsupported performance claim. | refutation test: independently rescan launchable decode entries, registry/source/ledger bijections, original-source hashes, and every completion gate | status: survived

## Established facts

- The execution plan is commit `4b0fb8262133cb60d5d548ca420b1ddf4c7cafdf` on `/root/scripts` branch `users/jerryyin/a8w4-decode-ablation-plan`; verified with `git rev-parse HEAD` before execution.
- `/root/triton-mi450` local HEAD and live `refs/pull/154/head` both resolve to `80e223f93d59359161f5482fbb69bbfab29c0a0b`; verified before execution.
- `/root/aiter` resolves to `4a1cc773f34cbfc74387259e51262556ee38edd0` and `/root/tokenspeed` resolves to `3e725ac2b785b71f27ff9e9ac3796349c495d225`; verified before execution.
- All four repositories were clean before execution, and the ablation source directory plus per-cell ledger directory contained no experiment artifacts.
- H0 now owns a self-contained `k00_pr154_baseline.py`, the shared `run_ffm.py` harness, and `ledgers/H0.md`. A location-insensitive AST comparison matches all nine decode-reachable common definitions plus `_matmul_decode` to their pinned parents; the child has no import of an original target module. This proves source fidelity, not runtime correctness.
- The H0 harness defines the exact 20-case tiny-M source matrix plus C1-C5, checks the three parent hashes before launch, rebuilds metadata and launch state per case, uses the pinned dequantized reference and source `maxtol=0.03` / effective `rmstol=0.004`, and records selected-child dispatch.
- `/data/lock` was absent at execution start. The coordinator initialized it with mode `0777` and verified that `/data/lock/amd-gpu.lock` can be acquired; it is no longer an execution blocker.
- At execution start the container had the gfx1250 FFM package and ROCm/PyTorch environment but no compatible Triton extension. The user restored GitHub access and authorized the local build, resolving that runtime blocker.
- The user restored GitHub access and explicitly authorized the long local build. PR #154 built successfully against release `llvm-build-bb14ae08-1`; the editable extension imports from `/root/triton-mi450/python/triton/_C/libtriton.so`, and `compile_commands.json` resolves to the matching local build.
- H0 passed its required smoke → 25-case full → adversarial-smoke sequence under gfx1250 FFM. All invocations proved selected-child dispatch, pinned parent hashes, ten-definition AST fidelity, and the non-ragged rejection boundary. The coordinator independently reran the adversarial smoke with the same PASS result.
- Runtime refutation corrected three harness assumptions without changing the frozen kernel: plain FP8 activations do not take an MX scale dimension; A8W4 plain-FP8 output uses source `maxtol=0.03` with effective `rmstol=0.004`; auxiliary metrics must compare after the same one-byte reference cast as `assert_close`.
- Planned C1/C3 gather+`BLOCK_K=128` variants were independently compiler-rejected because the TDM gather padding interval 256 does not divide innermost dimension 128. Scatter replacements retained every planned shape and tuning value; default gather+`BLOCK_K=256` cases passed.
- Q1 ports TokenSpeed's bounded softmax router to gfx1250 wave32 while excluding correction bias (owned by Q2). Its one-workgroup child passed 4/4 FFM cases with exact stable IDs, maximum gate error `5.96e-08`, both derived 1/4-warp paths, and six public rejection boundaries; the coordinator independently reran the tied-ID smoke.
- G2 changes only the H0 WMMA warp layout plus donor compatibility guards: four-warp mixed M/N ownership becomes all-N at `BLOCK_M=16`. Child TTGIR contains the intended bases, 25/25 legal FFM cases pass, incompatible `BLOCK_M` and eight-warp cases reject, and the coordinator independently reran smoke.
- Q2 preserves TokenSpeed's sigmoid+bias routing boundary: router-dtype-rounded sigmoid gates, FP32 correction bias used only for selection, unbiased emitted gates, and normalization/scale behavior matching the donor. Its wave32 child passed 4/4 FFM cases and eight rejections with exact IDs and zero recorded gate error; the coordinator independently reran the bias-flip smoke.
- R1 establishes the route-direct family with a changed, explicit ABI: E4M3 activation plus per-token FP32 scale, packed E2M1/UE8M0 selected-expert weights, global route IDs, optional FP32 bias, and FP32 per-route rows. Its `(route,N tile)` wave-GEMV child passed 4/4 full plus repeated smoke, emits packed-FP4 scaled-upcast but no WMMA/MFMA, allocates zero LDS/scratch, and rejects five ownership/precision boundaries; the coordinator independently reran smoke.
- G3 implements AITER's fixed three-N-tile persistent walk at its `BLOCK_M=16` compatibility boundary while retaining H0's numerical path. The child logically contracts the N grid, decodes routing and constructs descriptors once per three-tile group, handles one- and two-tile tails, and passed 25/25 FFM cases plus TTGIR structure checks and an independent smoke rerun.
- R2 preserves R1's `(route,N tile)` ownership while one activation load feeds separate selected-expert gate/up FP32 wave reductions and a fused `SiLU(gate) * up` epilogue. Its smoke/full/repeated-smoke sequence passed across both legal `BLOCK_N`/warp and `BLOCK_K` values, exact component checks, eight rejection boundaries, zero LDS/scratch and no matrix instructions; the coordinator's smoke rerun passed.
- G4 replaces the general source-level ragged offset helper with a direct AITER-format packed schedule load and expert/block unpack. It passed 25/25 FFM cases, repeated smoke, a nonzero-block-ID adversary, and the coordinator smoke rerun. Adversarial TTGIR comparison refuted a generated-code novelty claim: H0 already canonicalizes to the same load/mask/shift/load/multiply sequence.
- R3 adds only the FP32-activated-to-BF16 W1 store boundary to R2. Four full cases are bitwise exact to the declared BF16 reference with live rounding, eight rejections and repeated smoke pass, and generated-code audits show one BF16 conversion/store with no FP8 requantization; the coordinator smoke rerun passed.
- G5 transports AITER's packed-weight preshuffle into H0 while keeping scale layout and logical WMMA input independent: global bytes use the donor permutation, an LDS-local inverse reconstructs the original tile, and physical zero padding preserves logical K tails. All 25 FFM cases and independent smoke pass with exact output, including `K=416`; host mapping and four intrinsic layout rejections also pass.
- Q3 ports TokenSpeed's one-workgroup precomputed-route materializer to wave32 and freezes deterministic invalid-route tails. Five full cases and the coordinator smoke rerun match an independent stable-sort oracle exactly for histograms, prefixes, five block schedules, gather/scatter indices, and gates; 14 public rejections pass. Generated code uses one CTA/warp, zero scratch, and compiler-lowered 128–256-byte LDS rather than a register-only implementation.
- R4 replaces only R2's final FP32 store with AITER's supplied-scale static E4M3FN boundary. Four full cases and independent smoke match every host FP8 byte and scale bit, 11 rejections pass, and the generated path contains one scalar load/divide and native FP8 conversion/store with no BF16 or dynamic per-32 quantization.
- G6 replaces H0's generic multibuffer input TDM pipeline with explicit wave32 X/W/scale loads, one value-initialized X/W LDS stage, and immediate scaled-WMMA consumption. The 25-case matrix and coordinator smoke are exact; emitted TTGIR has no input TDM copies/gathers, retains one output descriptor, and the intrinsic `NUM_BUFFERS=1` boundary is compiler-rejected at two.
- Q4 fuses Q1's full-row softmax/stable top-k contract with Q3's grouped metadata materialization. Five cases and coordinator smoke exactly match the executable two-kernel composition for all seven fields and reconstructed routes; generated code has no global top-k pointers or intermediate route traffic, while honestly retaining compiler-lowered 128–256-byte dynamic LDS.
- R5 changes W2 ownership from route rows to one `(token,output tile)` program that loops top-k, applies FP32 routing weights, accumulates locally, and stores once. Four full cases and coordinator smoke pass per-route, weighted, prefix, and final references; production IR/assembly has one final FP32 store, no capture or combine buffers, zero LDS/scratch, and no atomics/matrix operations.
- Q5 fuses Q2's router-dtype-rounded sigmoid/correction-bias selection with Q3 grouping while preserving bias-only choice, un-biased gates, and normalization/scale asymmetry. Six cases and coordinator smoke exactly match executable Q2→Q3 for all fields and reconstructed routes; final IR has no top-k intermediate, uses 128–256-byte dynamic LDS, and reports zero scratch.
- G7 preserves grouped scaled-WMMA but replaces per-route output storage with route-gated FP32 atomic additions into caller-zeroed token output. Five full cases and coordinator smoke isolate all 34 route contributions and validate token sums under a derived FP32 order bound; generated TTGIR/ISA contains FP32 atomics and no semantic output TDM route store.
- R6 preserves R1's route ownership and direct selected-expert streaming while replacing its elementwise K reduction with TokenSpeed's duplicated-row direct scaled-matrix idea. Because gfx1250 does not expose the donor's CDNA4 scaled MFMA, the child honestly uses the target's legal 16-row `gl.amd.gfx1250.wmma_scaled` primitive. Four full cases, repeated smoke, and the coordinator smoke pass; all duplicated rows and the production row are bitwise equal, host and R1 controls have zero observed error, and production emits one row-zero store plus the exact scaled-WMMA ISA with no reduction, MFMA, atomics, LDS, or scratch.
- Q6 specializes Q3 for one token with unique valid route IDs: direct expert-by-slot membership and below-expert reductions replace the general histogram, scans, and route-pair stable-rank construction, while offset-indexed stores preserve Q3's expert-grouped ABI rather than TokenSpeed's slot order. Five full cases and coordinator smoke match Q3 exactly on all seven fields; 19 invalid or unsupported boundaries reject before child launch.
- G8 adds AITER's donor-exact dynamic per-32 MXFP8 boundary after H0's FP32 bias/activation while retaining grouped scaled-WMMA ownership and the value TDM store. Five full cases, repeated smoke, and coordinator smoke compare 8,448 E4M3 bytes plus 264 UE8M0 bytes exactly against an independent parent-FP32-to-donor-bit oracle, including zero and unequal-range groups, post-SwiGLU reduction, complete-group N tails, and every inherited tuning axis.
- R9 removes R6's precomputed-route input and recomputes Q1-compatible normalized stable softmax/top-k in every `(token,N tile,slot)` program before immediately consuming the selected expert. The narrow one-wave `BLOCK_N=16,E<=32` specialization avoids introducing cross-wave cooperation; four full cases, repeated smoke, and coordinator smoke show bitwise-identical decisions across programs, exact R6/host projections, scaled-WMMA codegen, and zero LDS/scratch.
- R7 retains R5's output-owned top-k fold but replaces each route's elementwise wave reduction with the legal gfx1250 duplicated-row scaled-WMMA path. Four full cases, repeated smoke, and coordinator smoke match all R5 route/weighted/prefix/final boundaries; production keeps one final store with no route/combine buffer, reduction, LDS, or scratch. Both entries disable FP contraction so observable FP32 boundaries remain identical with and without diagnostics.
- I1 composes accepted R3 with an owned BF16-input adaptation of R5's output-owned W2 as an explicit two-launch decode path. Four full cases, repeated smoke, and coordinator smoke preserve 25,088 standalone-R3 BF16 bytes bitwise and validate every W1 gate/up/activation plus W2 route/weighted/prefix/final boundary; W2 consumes BF16 directly with no scale operand or FP8 conversion and stores once.
- I2 composes the exact accepted R4 and R5 public paths as two launches, using a caller-supplied bitwise-repeated static scale whose first view feeds R4 and full vector feeds R5 without device-side transformation. Four full cases, repeated smoke, and coordinator smoke preserve all 12,544 tested E4M3 bytes and scale bits exactly and validate every W1, dequantized, W2 route/weighted/prefix, and final boundary.
- R8 changes only R7's K load/compute source order to a paired register-lookahead schedule with a serial odd-fragment epilogue. Four full cases, repeated smoke, and coordinator smoke are bitwise equal to R7 at every boundary. The compiler hoists all K=512 operand triplets before the first WMMA, so correctness/source-order transfer is established but preserved pairwise native overlap and performance are not.
- A1 independently closed all eight audit gates on the final evidence snapshot. The 20-file selected corpus plus its imported AITER routing body resolve all 33 static launch sites and 31 lexical targets; the registry/source/ledger graph contains 26 rows, 25 implementation modules plus two H0 support files, and 26 ledgers with no missing or duplicate ownership.
- A1 counted 233 passing full-matrix cases and at least 283 successful accepted invocations including smoke/repeat runs across the 25 implementation cells. Every cell has an adversarial or rejected boundary, and no FFM-derived performance or ranking claim survives.
- The final provenance pass corrected broad registered-wrapper mappings, the AITER wrapper anchor, R9's donor path string, and the Q6 source characterization. Q6's standalone flat-M1 kernel is launchable, but its sole caller branch is unreachable at the pinned TokenSpeed revision because the containing helper requires `M>=4` while the branch requires `M==1`.

## Hypotheses

- H1: The pinned PR helper closure can be made self-contained without semantic edits. | status: supported | decisive experiment: H0 source-diff audit plus 25-case FFM suite | evidence: ten AST matches, child dispatch proof, 25/25 FFM PASS, and adversarial smoke PASS
- H2: The planned donor mechanisms can be expressed on gfx1250 without adding uncatalogued supporting optimizations. | status: supported | decisive experiment: one-change implementation and FFM review per active row, followed by A1 corpus/provenance audit | evidence: all 25 implementation cells passed their frozen FFM matrices and one-change review; A1 found no uncatalogued mechanism or ownership gap
- H3: After a one-time pinned compiler build, the shared FFM harness can compile and run every ownership family without further compiler rebuilds. | status: supported | decisive experiment: first grouped, routing, and route-direct roots | evidence: grouped H0/G2/G3, routing Q1/Q2, and route-direct R1 all compile and pass without a compiler rebuild

## Variables in play

- Source pins: Triton PR head, AITER SHA, TokenSpeed SHA, and Cursor writeup.
- Cell identity, parent source, ownership model, routing contract, math primitive, intermediate format, and output boundary.
- M, N, K, top-k, expert distribution, gather/scatter direction, activation dtype, weight/scale layout, block sizes, warps, and buffers.
- FFM environment, unique Triton cache directory, reference implementation, tolerance, and explicit child dispatch.

## Next decisive experiment

- The correctness-first round is complete. Any next experiment is a separately authorized AM/B0 performance phase: measure the surviving exact source variants, jointly tune their exposed parameters, and test crossover policy without treating FFM timing as evidence.

## Dead ends

- Exhaustive local-runtime recovery found no existing `libtriton.so`, wheel, build tree, compatible alternate Python, live mapped extension, or usable container runtime. Repeating the same search cannot unblock H0.
- Anonymous access to the PR-pinned private LLVM release returned HTTP 404. The user restored authenticated access, after which the pinned build succeeded; this path is resolved and should not be retried anonymously.
