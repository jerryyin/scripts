# Verbose notes

Longer GenericSwizzling root-cause drafts, kept for audit history. The `s_nop`
"timing-pad fix" claims they once contained were removed (that lead was
falsified). The active explanation is the top-level `../../README.md` and
`../../ISSUE.md`.
